/* OpenStation v1.1.0 (64cbb98053e4fe49a901ded5867316e0048fc76e) selected UI components. GPL-2.0-or-later. */
(()=>{function n(i,...o){return{__wpdHtml:!0,strings:i,values:o}}function $e(i){return!!i&&i.__wpdHtml===!0}var Se="$$wpd$$",_=/\$\$wpd\$\$(\d+)\$\$/g;function Te(i){let o=i[0];for(let e=1;e<i.length;e++)o+=`${Se}${e-1}$$`+i[e];return o}var ae=new WeakMap;function re(i){let o=ae.get(i);if(o)return o;let e=document.createElement("template");e.innerHTML=Te(i);let t=[],s=(d,l)=>{if(d.nodeType===Node.ELEMENT_NODE){let v=d;for(let k of Array.from(v.attributes)){let y=k.name,m=k.value,b=y[0];if(_.test(m))if(_.lastIndex=0,b==="@"){let p=_.exec(m);_.lastIndex=0,t.push({path:l,kind:"event",name:y.slice(1),valueIndex:p?Number(p[1]):0}),v.removeAttribute(y)}else if(b==="."){let p=_.exec(m);_.lastIndex=0,t.push({path:l,kind:"prop",name:y.slice(1),valueIndex:p?Number(p[1]):0}),v.removeAttribute(y)}else if(b==="?"){let p=_.exec(m);_.lastIndex=0,t.push({path:l,kind:"bool",name:y.slice(1),valueIndex:p?Number(p[1]):0}),v.removeAttribute(y)}else{let p=[],w=[],x=0,C;for(_.lastIndex=0;(C=_.exec(m))!==null;)p.push(m.slice(x,C.index)),w.push(Number(C[1])),x=C.index+C[0].length;p.push(m.slice(x)),t.push({path:l,kind:"attr",name:y,template:p,valueIndices:w}),v.setAttribute(y,"")}}}let c=Array.from(d.childNodes),h=0;for(let v=0;v<c.length;v++){let k=c[v],y=v+h;if(k.nodeType===Node.TEXT_NODE){let m=k.textContent||"";if(!_.test(m)){_.lastIndex=0;continue}_.lastIndex=0;let b=k.parentNode,p=0,w,x=[],C=[];for(_.lastIndex=0;(w=_.exec(m))!==null;){w.index>p&&x.push(document.createTextNode(m.slice(p,w.index)));let A=document.createTextNode("");x.push(A),C.push({path:[...l,y+x.length-1],kind:"node",valueIndex:Number(w[1])}),p=w.index+w[0].length}p<m.length&&x.push(document.createTextNode(m.slice(p)));for(let A of x)b.insertBefore(A,k);b.removeChild(k),h+=x.length-1,t.push(...C)}else s(k,[...l,y])}};s(e.content,[]);let r={template:e,buildParts:d=>{let l=[];for(let c of t){let h=d;for(let v of c.path)h=h.childNodes[v];c.kind==="node"?l.push({kind:"node",valueIndex:c.valueIndex,child:{anchor:h,state:null}}):c.kind==="attr"?l.push({kind:"attr",element:h,name:c.name,template:c.template,valueIndices:c.valueIndices}):c.kind==="event"?l.push({kind:"event",valueIndex:c.valueIndex,element:h,name:c.name}):c.kind==="prop"?l.push({kind:"prop",valueIndex:c.valueIndex,element:h,name:c.name}):c.kind==="bool"&&l.push({kind:"bool",valueIndex:c.valueIndex,element:h,name:c.name})}return l}};return ae.set(i,r),r}var ne=new WeakMap;function Ae(i,o){for(let e of i.nodes)if(e.parentNode!==o)return!1;return!0}function Y(i,o){let e=ne.get(o);if(e&&e.strings===i.strings&&Ae(e,o)){K(e.parts,i.values);return}let t=re(i.strings),s=t.template.content.cloneNode(!0),a=t.buildParts(s),r=Array.from(s.childNodes);for(;o.firstChild;)o.removeChild(o.firstChild);o.appendChild(s),K(a,i.values),ne.set(o,{strings:i.strings,parts:a,nodes:r})}function K(i,o){for(let e of i)if(e.kind==="node")X(e.child,o[e.valueIndex]);else if(e.kind==="attr"){let t=e.template[0];for(let s=0;s<e.valueIndices.length;s++)t+=le(o[e.valueIndices[s]]),t+=e.template[s+1];t!==e.last&&(e.last=t,t===""?e.element.removeAttribute(e.name):e.element.setAttribute(e.name,t))}else if(e.kind==="event"){let t=o[e.valueIndex];t!==e.current&&(e.current&&e.element.removeEventListener(e.name,e.current),t&&e.element.addEventListener(e.name,t),e.current=t)}else if(e.kind==="prop"){let t=o[e.valueIndex];t!==e.last&&(e.last=t,e.element[e.name]=t)}else if(e.kind==="bool"){let t=!!o[e.valueIndex];t!==e.last&&(e.last=t,t?e.element.setAttribute(e.name,""):e.element.removeAttribute(e.name))}}function X(i,o){if(o==null||o===!1){i.state&&(P(i.state),i.state=null);return}if(Array.isArray(o)){Ie(i,o);return}if($e(o)){Le(i,o);return}if(o instanceof Node){Ee(i,o);return}Pe(i,le(o))}function Ee(i,o){let e=i.state;e?.shape==="node"&&e.node===o||(e&&P(e),W(i,[o]),i.state={shape:"node",node:o})}function Pe(i,o){let e=i.state;if(e?.shape==="text"){e.text!==o&&(e.node.textContent=o,e.text=o);return}e&&P(e);let t=document.createTextNode(o);W(i,[t]),i.state={shape:"text",node:t,text:o}}function Le(i,o){let e=i.state;if(e?.shape==="template"&&e.strings===o.strings){K(e.parts,o.values);return}e&&P(e);let t=re(o.strings),s=t.template.content.cloneNode(!0),a=t.buildParts(s),r=Array.from(s.childNodes);W(i,[s]),K(a,o.values),i.state={shape:"template",strings:o.strings,parts:a,nodes:r}}function Ie(i,o){let e=i.state;if(e?.shape==="array"&&e.entries.length===o.length){for(let s=0;s<o.length;s++)X(e.entries[s],o[s]);return}e&&P(e);let t=[];for(let s of o){let a=document.createTextNode("");W(i,[a]);let r={anchor:a,state:null};X(r,s),t.push(r)}i.state={shape:"array",entries:t}}function W(i,o){let e=i.anchor.parentNode;if(e)for(let t of o)e.insertBefore(t,i.anchor)}function P(i){if(i.shape==="text"){i.node.remove();return}if(i.shape==="template"){for(let o of i.parts)o.kind==="node"&&o.child.state&&(P(o.child.state),o.child.state=null);for(let o of i.nodes)o.parentNode&&o.parentNode.removeChild(o);return}if(i.shape==="node"){i.node.parentNode&&i.node.parentNode.removeChild(i.node);return}for(let o of i.entries)o.state&&P(o.state),o.anchor.remove()}function le(i){return i==null||i===!1?"":String(i)}var E=class E extends HTMLElement{constructor(){super();this._renderScheduled=!1;this._propValues={};this.constructor.shadow?(this.attachShadow({mode:"open"}),this._renderRoot=this.shadowRoot):this._renderRoot=this,this._installPropAccessors()}static get observedAttributes(){return this.props.map(de)}connectedCallback(){this._adoptStyles(),this.requestUpdate()}attributeChangedCallback(e,t,s){if(t===s)return;let a=ze(e);this._propValues[a]=s,this.requestUpdate()}get classNames(){return Array.from(this.classList)}set classNames(e){if(e==null){this.removeAttribute("class");return}let s=(Array.isArray(e)?e:String(e).split(/\s+/)).map(a=>String(a).trim()).filter(a=>a!=="");this.className=s.join(" ")}requestUpdate(){this._scheduleRender()}emit(e,t){return this.dispatchEvent(new CustomEvent(e,{detail:t,bubbles:!0,composed:!0}))}_installPropAccessors(){let e=this.constructor;for(let t of e.props){if(Object.getOwnPropertyDescriptor(this,t))continue;let s=de(t);Object.defineProperty(this,t,{get:()=>t in this._propValues?this._propValues[t]:this.getAttribute(s),set:a=>{let r;a==null||a===!1?r=null:a===!0?r="":r=String(a),this._propValues[t]=r,r===null?this.removeAttribute(s):this.setAttribute(s,r),this.requestUpdate()},enumerable:!0,configurable:!0})}}_scheduleRender(){this._renderScheduled||!this.isConnected||(this._renderScheduled=!0,queueMicrotask(()=>{this._renderScheduled=!1,this.isConnected&&Y(this.render(),this._renderRoot)}))}_adoptStyles(){let e=this.constructor;if(e.styles.length!==0)if(e.shadow&&this.shadowRoot){let t=e.styles.map(s=>s.sheet).filter(s=>s!==null);if(this.shadowRoot.adoptedStyleSheets=t,t.length!==e.styles.length){for(let s of e.styles)if(!s.sheet){let a=document.createElement("style");a.textContent=s.cssText,this.shadowRoot.appendChild(a)}}}else this._adoptLightStyles(e)}_adoptLightStyles(e){if(!E._lightStylesAdopted.has(e)){E._lightStylesAdopted.add(e);for(let t of e.styles){let s=document.createElement("style");s.dataset.osUi=this.tagName.toLowerCase(),s.textContent=t.cssText,document.head.appendChild(s)}}}};E.props=[],E.styles=[],E.shadow=!0,E._lightStylesAdopted=new WeakSet;var g=E;function f(i,o){customElements.get(i)||customElements.define(i,o)}function de(i){return i.replace(/[A-Z]/g,o=>"-"+o.toLowerCase())}function ze(i){return i.replace(/-([a-z])/g,(o,e)=>e.toUpperCase())}var De=(()=>{try{return typeof new CSSStyleSheet().replaceSync=="function"}catch{return!1}})();function u(i,...o){let e=i[0];for(let t=1;t<i.length;t++){let s=o[t-1];if(typeof s=="string"||typeof s=="number")e+=String(s);else if(s&&s.__wpdCss)e+=s.cssText;else throw new TypeError("[os-ui] css`` interpolations must be strings, numbers, or other css`` results. Got: "+typeof s);e+=i[t]}if(De){let t=new CSSStyleSheet;return t.replaceSync(e),{__wpdCss:!0,sheet:t,cssText:e}}return{__wpdCss:!0,sheet:null,cssText:e}}function ce(i){let o=[],e=[],t=null,s=i.parentElement;for(;s&&!(s===document.body||s===document.documentElement);){let r=s.id||"";if(r.startsWith("wp-window-")){t=r.slice(10);break}if(s.tagName.toLowerCase()==="os-tabpanel"){let d=s.getAttribute("for");d&&e.unshift(d)}s=s.parentElement}t&&o.push(Q(t));for(let r of e)o.push("tab-"+Q(r));let a=i.getAttribute("label");return a&&o.push(Q(a)),o.length===0?"os-unnamed":"os-"+o.filter(r=>r!=="").join("-")}function Q(i){return i.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")}function B(i){if(i.id)return i.id;let o=ce(i);return i.id=o,o}var Ne="linear-gradient( 124deg, #afa2e8 0%, #f5a8ea 46%, #8ee9f7 100% )",$=u`
	:host {
		--_holo-fill: var( --os-ui-holo-fill, ${Ne} );
		--_holo-ink: var( --os-ui-holo-ink, #0c0b0f );
		--_holo-sheen: var(
			--os-ui-holo-sheen,
			linear-gradient(
				124deg,
				rgba( 159, 214, 255, 0.1 ) 0%,
				rgba( 236, 155, 255, 0.12 ) 34%,
				rgba( 242, 82, 252, 0.1 ) 58%,
				rgba( 147, 240, 198, 0.09 ) 100%
			)
		);
		--_holo-edge: var(
			--os-ui-holo-edge,
			linear-gradient(
				124deg,
				rgba( 154, 242, 255, 0.7 ) 0%,
				rgba( 236, 155, 255, 0.85 ) 38%,
				rgba( 242, 82, 252, 0.7 ) 62%,
				rgba( 159, 152, 255, 0.6 ) 100%
			)
		);
		--_holo-edge-quiet: var(
			--os-ui-holo-edge-quiet,
			linear-gradient(
				124deg,
				rgba( 154, 242, 255, 0.22 ) 0%,
				rgba( 236, 155, 255, 0.28 ) 38%,
				rgba( 242, 82, 252, 0.22 ) 62%,
				rgba( 159, 152, 255, 0.2 ) 100%
			)
		);
		--_holo-glow: var(
			--os-ui-holo-glow,
			0 0 0 1px rgba( 242, 82, 252, 0.28 ), 0 2px 10px rgba( 242, 82, 252, 0.22 )
		);
		--_holo-glow-strong: var(
			--os-ui-holo-glow-strong,
			0 0 0 1px rgba( 242, 82, 252, 0.42 ), 0 4px 18px rgba( 242, 82, 252, 0.38 ),
				0 1px 3px rgba( 12, 11, 15, 0.6 )
		);
		--_holo-track: var( --os-ui-holo-track, rgba( 255, 251, 255, 0.16 ) );
		--_holo-track-edge: var( --os-ui-holo-track-edge, #8c8f94 );
		/*
		 * The press ring's colour. The DIM Pulse, not Pulse: this one
		 * expands to ten pixels past the control and is on screen for
		 * a third of a second, which is exactly the "spread rather
		 * than stated" case the dim exists for.
		 */
		--_holo-ring-color: var( --os-ui-accent-dim, #2271b1 );
		/*
		 * One focus ring for the whole kit. Three layers, and each
		 * earns its place: a Void spacer so the ring never touches the
		 * control it is describing, the Pulse ring itself, and a soft
		 * bloom so the ring survives landing on a bright mesh — where a
		 * flat 2 px line would simply vanish into the pink.
		 */
		--_holo-focus: var(
			--os-ui-focus-ring,
			0 0 0 2px rgba( 12, 11, 15, 0.9 ), 0 0 0 4px #f252fc,
				0 0 12px 2px rgba( 242, 82, 252, 0.45 )
		);
		/*
		 * The field ring. A text input already has a border to
		 * thicken and it lives in a column of siblings, so the target
		 * ring above — built to survive a bright mesh — reads as an
		 * alarm on a settings form. This one tightens the field's own
		 * edge to Pulse and adds a soft halo outside it.
		 */
		--_holo-focus-field: var(
			--os-ui-focus-ring-field,
			0 0 0 1px #f252fc, 0 0 0 4px rgba( 242, 82, 252, 0.18 )
		);
		--_holo-t: var( --os-ui-holo-transition, 220ms );
		--_holo-t-fast: var( --os-ui-motion-fast, 140ms );
		--_holo-t-slow: var( --os-ui-motion-slow, 340ms );
		--_holo-t-ambient: var( --os-ui-motion-ambient, 12s );
		--_holo-spring: var(
			--os-ui-ease-spring,
			cubic-bezier( 0.32, 1.5, 0.55, 1 )
		);
		--_holo-ease: var( --os-ui-ease-out, cubic-bezier( 0.22, 0.9, 0.28, 1 ) );
		--_holo-loop: var( --os-ui-ease-loop, cubic-bezier( 0.45, 0, 0.55, 1 ) );
	}
`,Fe=u`
	.os-holo-fill {
		background-color: transparent;
		background-image: var( --_holo-fill );
		background-size: 220% 220%;
		background-position: 22% 28%;
		background-repeat: no-repeat;
		color: var( --_holo-ink );
		transition: background-position var( --_holo-t ) ease,
			box-shadow var( --_holo-t ) ease, filter var( --_holo-t ) ease;
	}

	.os-holo-fill:hover {
		background-position: 74% 66%;
	}

	.os-holo-fill:active {
		background-position: 88% 82%;
		filter: brightness( 0.94 );
	}

	/*
	 * The tilt is the whole effect, so when motion is unwelcome the
	 * mesh simply stops moving — it does not stop being a mesh. A
	 * control that lost its fill under reduced-motion would lose its
	 * state, not just its animation.
	 */
	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-fill {
			transition-duration: 1ms;
		}

		.os-holo-fill:hover,
		.os-holo-fill:active {
			background-position: 22% 28%;
		}
	}
`,Z=u`
	.os-holo-sheen {
		position: relative;
		isolation: isolate;
	}

	.os-holo-sheen::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: -1;
		border-radius: inherit;
		background-image: var( --_holo-sheen );
		background-size: 200% 200%;
		background-position: 20% 30%;
		opacity: 0;
		pointer-events: none;
		transition: opacity var( --_holo-t ) ease,
			background-position var( --_holo-t ) ease;
	}

	.os-holo-sheen:hover::before,
	.os-holo-sheen:focus-visible::before {
		opacity: 1;
		background-position: 76% 68%;
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-sheen::before {
			transition-duration: 1ms;
		}

		.os-holo-sheen:hover::before,
		.os-holo-sheen:focus-visible::before {
			background-position: 20% 30%;
		}
	}
`,J=u`
	.os-holo-edge {
		position: relative;
	}

	.os-holo-edge::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		padding: 1px;
		background-image: var( --_holo-edge-quiet );
		opacity: 0;
		pointer-events: none;
		/*
		 * Two masks: one clipped to the content box, one covering the
		 * whole element. "exclude" keeps the difference — the 1 px
		 * padding ring — which is the border. The -webkit- pair comes
		 * first for Safari, which still ships the prefixed property and
		 * spells the operation "xor" rather than "exclude".
		 */
		-webkit-mask: linear-gradient( #000 0 0 ) content-box,
			linear-gradient( #000 0 0 );
		-webkit-mask-composite: xor;
		mask: linear-gradient( #000 0 0 ) content-box, linear-gradient( #000 0 0 );
		mask-composite: exclude;
		transition: opacity var( --_holo-t ) ease;
	}

	.os-holo-edge:hover::after,
	.os-holo-edge:focus-visible::after {
		background-image: var( --_holo-edge );
		opacity: 1;
	}

	.os-holo-edge.is-lit::after {
		background-image: var( --_holo-edge );
		opacity: 1;
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-edge::after {
			transition-duration: 1ms;
		}
	}
`,ee=u`
	.os-holo-glint {
		position: absolute;
		inset: 0;
		border-radius: inherit;
		/* Clips the band to the control's shape. On the span rather
		   than the control, so the control keeps its own overflow. */
		overflow: hidden;
		pointer-events: none;
	}

	.os-holo-glint::before {
		content: '';
		position: absolute;
		/*
		 * Oversized and rotated, so the band is a plain rectangle that
		 * happens to cross the box diagonally. Translating a clipped
		 * rectangle is a composited move; animating gradient stops
		 * would repaint every frame.
		 */
		top: -60%;
		bottom: -60%;
		width: 45%;
		inset-inline-start: -60%;
		background: linear-gradient(
			90deg,
			transparent 0%,
			rgba( 255, 251, 255, 0.14 ) 45%,
			rgba( 255, 251, 255, 0.22 ) 50%,
			rgba( 255, 251, 255, 0.14 ) 55%,
			transparent 100%
		);
		transform: rotate( 18deg ) translateX( 0 );
		opacity: 0;
	}

	/*
	 * Two forms, because the glint is stamped in two places.
	 *
	 * Inside a control (a button, a key) it is a child of that
	 * element, and the "hover >" form reaches it. Directly in a
	 * component's shadow root (a card, a tile) its parent is the
	 * shadow ROOT, which no selector matches — that form finds nothing
	 * there. The :host( :hover ) > form is the one that does, and the
	 * two are mutually exclusive in practice, so both can always be
	 * present.
	 */
	:hover > .os-holo-glint::before,
	:focus-visible > .os-holo-glint::before,
	:host( :hover ) > .os-holo-glint::before,
	:host( :focus-visible ) > .os-holo-glint::before {
		animation: os-holo-glint var( --_holo-t-slow ) var( --_holo-ease );
	}

	@keyframes os-holo-glint {
		0% {
			opacity: 0;
			transform: rotate( 18deg ) translateX( 0 );
		}

		15% {
			opacity: 1;
		}

		85% {
			opacity: 1;
		}

		100% {
			opacity: 0;
			/*
			 * Far enough that the band has cleared a very wide box.
			 * Viewport units rather than a percentage of the element:
			 * a percentage translate on a 45%-wide band means one thing
			 * on a 40px chip and another on a 900px row, and the chip's
			 * glint would finish before it was visible.
			 */
			transform: rotate( 18deg ) translateX( 240vw );
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		:hover > .os-holo-glint::before,
		:focus-visible > .os-holo-glint::before,
		:host( :hover ) > .os-holo-glint::before,
		:host( :focus-visible ) > .os-holo-glint::before {
			animation: none;
		}
	}
`,te=u`
	.os-holo-ring {
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: 0 0 0 0 var( --_holo-ring-color );
		opacity: 0;
		pointer-events: none;
	}

	/* Both forms, for the same reason as the glint above. */
	:active:not( :disabled ) > .os-holo-ring,
	:host( :active:not( [ disabled ] ) ) > .os-holo-ring {
		animation: os-holo-ring var( --_holo-t-slow ) var( --_holo-ease );
	}

	@keyframes os-holo-ring {
		0% {
			opacity: 0.45;
			box-shadow: 0 0 0 0 var( --_holo-ring-color );
		}

		100% {
			opacity: 0;
			box-shadow: 0 0 0 10px transparent;
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		:active:not( :disabled ) > .os-holo-ring,
		:host( :active:not( [ disabled ] ) ) > .os-holo-ring {
			animation: none;
		}
	}
`,Me=u`
	.os-holo-shimmer {
		background-image: var( --_holo-fill );
		background-size: 300% 300%;
		background-repeat: no-repeat;
		animation: os-holo-shimmer 2.4s var( --_holo-loop ) infinite;
	}

	@keyframes os-holo-shimmer {
		0% {
			background-position: 0% 50%;
		}

		100% {
			background-position: 100% 50%;
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-shimmer {
			animation: none;
			background-position: 30% 50%;
		}
	}
`,Re=u`
	.os-holo-enter {
		animation: os-holo-enter var( --_holo-t ) var( --_holo-spring );
	}

	@keyframes os-holo-enter {
		from {
			opacity: 0;
			transform: scale( 0.96 );
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-enter {
			animation: none;
		}
	}
`,V=u`
	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ),
	select,
	textarea {
		transition: background-color var( --_holo-t ) ease,
			border-color var( --_holo-t ) ease, box-shadow var( --_holo-t ) ease;
	}

	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ):hover:not(
			:disabled
		),
	select:hover:not( :disabled ),
	textarea:hover:not( :disabled ) {
		border-color: var( --os-ui-border-strong, #8c8f94 );
	}

	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ):focus,
	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ):focus-visible,
	select:focus,
	select:focus-visible,
	textarea:focus,
	textarea:focus-visible {
		outline: none;
		border-color: var( --os-ui-accent, #2271b1 );
		box-shadow: var( --_holo-focus-field );
	}

	/*
	 * Placeholders go one step further down the Shade ramp than muted
	 * body text. At the same value they compete with a real entry and
	 * the field reads as filled when it is empty.
	 */
	input::placeholder,
	textarea::placeholder {
		color: var( --os-ui-fg-faint, #8c8f94 );
		opacity: 1;
	}

	/*
	 * Selection inside a field.
	 *
	 * A shadow root does not inherit the document's ::selection rule,
	 * so the shell-wide one in desktop.css cannot reach in here — this
	 * has to be restated, and it reads the same two tokens so the two
	 * cannot drift.
	 *
	 * It used to read --os-ui-accent-soft, which is a 14% wash meant
	 * for a hover tint. As a selection it was ~1.2:1 against the field
	 * behind it: technically present, effectively invisible. Both
	 * halves are set, because leaving the text colour to the UA is how
	 * a selection ends up as black-on-violet.
	 */
	input::selection,
	textarea::selection {
		background: var( --os-ui-selection-bg, rgba( 159, 152, 255, 0.6 ) );
		color: var( --os-ui-selection-fg, #fffbff );
	}

	@media ( prefers-reduced-motion: reduce ) {
		input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ),
		select,
		textarea {
			transition-duration: 1ms;
		}
	}
`,se=u`
	input[ type='checkbox' ],
	input[ type='radio' ] {
		appearance: none;
		-webkit-appearance: none;
		position: relative;
		flex: 0 0 auto;
		box-sizing: border-box;
		width: 16px;
		height: 16px;
		margin: 0;
		padding: 0;
		/*
		 * The unchecked box's boundary carries the whole control: an
		 * empty checkbox IS its outline. --_holo-track-edge rather
		 * than --os-ui-border-strong because the latter is Silver, at
		 * 2.03:1 against Obsidian — under the 3:1 WCAG 1.4.11 asks of
		 * a control boundary, and visibly so on a dark panel.
		 */
		border: 1px solid var( --_holo-track-edge );
		border-radius: 4px;
		background-color: var( --_holo-track );
		background-image: none;
		background-repeat: no-repeat;
		cursor: inherit;
		transition: background-color var( --_holo-t ) ease,
			border-color var( --_holo-t ) ease, box-shadow var( --_holo-t ) ease;
	}

	input[ type='radio' ] {
		border-radius: 50%;
	}

	input[ type='checkbox' ]:hover:not( :disabled ),
	input[ type='radio' ]:hover:not( :disabled ) {
		border-color: var( --os-ui-accent, #2271b1 );
	}

	input[ type='checkbox' ]:focus-visible,
	input[ type='radio' ]:focus-visible {
		outline: none;
		box-shadow: var( --_holo-focus );
	}

	/*
	 * Checked wears the accent, flat. The mesh is reserved for hero
	 * surfaces, and a checkbox in a settings list is not one: a
	 * column of forms where every ticked box is iridescent has no
	 * identity moments left to spend.
	 */
	input[ type='checkbox' ]:checked,
	input[ type='checkbox' ]:indeterminate,
	input[ type='radio' ]:checked {
		border-color: transparent;
		background-color: var( --os-ui-accent, #2271b1 );
	}

	input[ type='checkbox' ]:checked:focus-visible,
	input[ type='radio' ]:checked:focus-visible {
		box-shadow: var( --_holo-focus );
	}

	/*
	 * The tick. Two borders of a box, rotated — the oldest trick in
	 * CSS and still the only one that scales with the font and takes
	 * its colour from a custom property.
	 */
	input[ type='checkbox' ]:checked::after {
		content: '';
		position: absolute;
		inset-inline-start: 50%;
		top: 46%;
		width: 3.5px;
		height: 7.5px;
		border: solid var( --os-ui-fg-on-accent, #fff );
		border-width: 0 2px 2px 0;
		transform: translate( -50%, -50% ) rotate( 45deg );
		animation: os-holo-tick 180ms cubic-bezier( 0.3, 1.4, 0.6, 1 );
	}

	/* Indeterminate: a bar, not a tick. Same ink, same box. */
	input[ type='checkbox' ]:indeterminate::after {
		content: '';
		position: absolute;
		inset-inline-start: 50%;
		top: 50%;
		width: 8px;
		height: 2px;
		border: 0;
		border-radius: 1px;
		background: var( --os-ui-fg-on-accent, #fff );
		transform: translate( -50%, -50% );
	}

	input[ type='radio' ]:checked::after {
		content: '';
		position: absolute;
		inset-inline-start: 50%;
		top: 50%;
		width: 6px;
		height: 6px;
		border-radius: 50%;
		background: var( --os-ui-fg-on-accent, #fff );
		transform: translate( -50%, -50% );
		animation: os-holo-tick 180ms cubic-bezier( 0.3, 1.4, 0.6, 1 );
	}

	input[ type='checkbox' ]:disabled,
	input[ type='radio' ]:disabled {
		cursor: not-allowed;
	}

	/*
	 * The tick lands rather than appears. 180ms with a slight
	 * overshoot, which is short enough to feel like a response to the
	 * click and not like an animation being played at you.
	 */
	@keyframes os-holo-tick {
		from {
			opacity: 0;
			transform: translate( -50%, -50% ) rotate( 45deg ) scale( 0.4 );
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		input[ type='checkbox' ]:checked::after,
		input[ type='radio' ]:checked::after {
			animation: none;
		}
	}
`,oe=u`
	@keyframes os-holo-drift {
		0% {
			background-position: 16% 26%;
		}

		50% {
			background-position: 84% 74%;
		}

		100% {
			background-position: 16% 26%;
		}
	}

	.os-holo-alive {
		animation: os-holo-drift var( --_holo-t-ambient ) var( --_holo-loop ) infinite;
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-alive {
			animation: none;
		}
	}
`,nt=u`
	${$}
	${Fe}
	${Z}
	${J}
	${ee}
	${te}
	${Me}
	${Re}
	${V}
	${se}
	${oe}
`;var ue=u`
	${$}
	${Z}
	${J}
	${ee}
	${te}
	${oe}

	:host {
		display: inline-flex;
	}
	:host( [ fill-cell ] ) {
		display: flex;
		width: 100%;
	}
	button {
		appearance: none;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 6px;
		padding: var( --os-ui-button-padding, 6px 12px );
		border-radius: var( --os-ui-button-border-radius, 6px );
		font: inherit;
		font-weight: 500;
		cursor: pointer;
		transition: background-color var( --_holo-t ) ease, color var( --_holo-t ) ease,
			border-color var( --_holo-t ) ease, box-shadow var( --_holo-t ) ease,
			transform 80ms ease;
		/* Ghost (default) */
		background: var( --os-ui-button-bg, transparent );
		/* Desktop-theme texture slot: unset resolves to none. Declared
		   on the base rule so every variant inherits it — the variant
		   rules below override background-COLOR only. */
		background-image: var( --os-ui-button-bg-image, none );
		background-repeat: var( --os-ui-button-bg-image-repeat, repeat );
		background-size: var( --os-ui-button-bg-image-size, auto );
		background-position: var( --os-ui-button-bg-image-position, center );
		color: var( --os-ui-button-fg, var( --os-ui-fg, #1d2327 ) );
		border: var(
			--os-ui-button-border,
			1px solid var( --os-ui-border, #c3c4c7 )
		);
	}
	:host( [ fill-cell ] ) button {
		width: 100%;
		min-height: var( --os-ui-button-min-height, 44px );
	}
	button:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}
	/*
	 * A press should move. One pixel is enough to read as travel and
	 * small enough that a toolbar of them does not look like it is
	 * bouncing; it is also the cheapest possible transform, so a grid
	 * of forty keypad buttons costs nothing to animate.
	 */
	button:active:not( :disabled ) {
		transform: translateY( 1px );
	}
	button:hover:not( :disabled ) {
		background-color: var( --os-ui-button-bg-hover, var( --os-ui-hover, rgba( 0, 0, 0, 0.04 ) ) );
	}
	/*
	 * One focus ring for the whole kit, replacing the browser default.
	 * It has to survive landing on a bright mesh, which a flat outline
	 * does not — see the note on --_holo-focus.
	 */
	button:focus-visible {
		outline: none;
		box-shadow: var( --_holo-focus );
	}
	/* Every lit layer is suppressed while the button is unavailable —
	   a disabled control that lights up under the pointer is
	   advertising an action it will not take. */
	button:disabled::before,
	button:disabled::after,
	button:disabled .os-holo-glint {
		opacity: 0 !important;
	}
	/* Primary */
	:host( [ variant='primary' ] ) button {
		background-color: var( --os-ui-button-bg, var( --wp-admin-theme-color, #2271b1 ) );
		color: var( --os-ui-button-fg, var( --os-ui-fg-on-accent, #fff ) );
		border: var( --os-ui-button-border, 1px solid transparent );
	}
	:host( [ variant='primary' ] ) button:hover:not( :disabled ) {
		filter: brightness( 1.06 );
		background-color: var( --os-ui-button-bg, var( --wp-admin-theme-color, #2271b1 ) );
		box-shadow: var( --_holo-glow );
	}
	/*
	 * Holo — the hero CTA. The mesh itself, with Void ink on it (every
	 * mesh in the brand is a light surface; see --os-ui-holo-ink), and
	 * the tilt-on-hover that makes it read as foil rather than as a
	 * pink rectangle. One per surface, at most.
	 */
	:host( [ variant='holo' ] ) button {
		background-color: transparent;
		background-image: var( --_holo-fill );
		background-size: 220% 220%;
		background-position: 22% 28%;
		background-repeat: no-repeat;
		color: var( --os-ui-button-fg, var( --_holo-ink ) );
		border: var( --os-ui-button-border, 1px solid transparent );
		box-shadow: var( --_holo-glow );
		font-weight: 600;
		transition: background-position var( --_holo-t ) ease,
			box-shadow var( --_holo-t ) ease, filter var( --_holo-t ) ease,
			transform 80ms ease;
	}
	:host( [ variant='holo' ] ) button:hover:not( :disabled ) {
		background-color: transparent;
		background-position: 74% 66%;
		box-shadow: var( --_holo-glow-strong );
	}
	:host( [ variant='holo' ] ) button:active:not( :disabled ) {
		background-position: 88% 82%;
		filter: brightness( 0.94 );
	}
	/* The sheen has nothing to add over a full mesh, and reads as haze. */
	:host( [ variant='holo' ] ) button::before {
		display: none;
	}
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ variant='holo' ] ) button:hover:not( :disabled ),
		:host( [ variant='holo' ] ) button:active:not( :disabled ) {
			background-position: 22% 28%;
		}
	}
	/* Secondary — quiet filled control. Neutral chrome, no underline.
	 * Semantic fit for "not the primary action but also not a
	 * destructive one" (AC / ± / % on a calculator; Cancel in a
	 * two-button dialog). */
	:host( [ variant='secondary' ] ) button {
		background-color: var( --os-ui-button-bg, var( --os-ui-hover, rgba( 0, 0, 0, 0.06 ) ) );
		color: var( --os-ui-button-fg, var( --os-ui-fg, #1d2327 ) );
		border: var( --os-ui-button-border, 1px solid transparent );
	}
	:host( [ variant='secondary' ] ) button:hover:not( :disabled ) {
		background-color: var( --os-ui-button-bg-hover, var( --os-ui-hover, rgba( 0, 0, 0, 0.1 ) ) );
	}
	/* Danger */
	:host( [ variant='danger' ] ) button {
		background-color: var( --os-ui-button-bg, transparent );
		color: var( --os-ui-button-fg, var( --os-ui-danger, #d63638 ) );
		border: var( --os-ui-button-border, 1px solid currentColor );
	}
	:host( [ variant='danger' ] ) button:hover:not( :disabled ) {
		background-color: var( --os-ui-danger, #d63638 );
		color: var( --os-ui-fg-on-accent, #fff );
	}
	/*
	 * Danger keeps its own edge. The iridescent hairline says "brand
	 * moment" and a delete button is the one place that is the wrong
	 * sentence — the border should stay red all the way through the
	 * hover, which is the only warning the user gets.
	 */
	:host( [ variant='danger' ] ) button::after {
		display: none;
	}
	/* Link */
	:host( [ variant='link' ] ) button {
		background-color: transparent;
		background-image: none;
		color: var( --os-ui-button-fg, var( --wp-admin-theme-color, #2271b1 ) );
		border: 0;
		padding: 0;
		text-decoration: underline;
	}
	/* No chrome means nothing to put an edge, a film, a highlight or a
	   ring on — a link is text, and text does not catch the light. */
	:host( [ variant='link' ] ) button::before,
	:host( [ variant='link' ] ) button::after,
	:host( [ variant='link' ] ) .os-holo-glint,
	:host( [ variant='link' ] ) .os-holo-ring {
		display: none;
	}
	:host( [ variant='link' ] ) button:active:not( :disabled ) {
		transform: none;
	}
	:host( [ busy ] ) button {
		pointer-events: none;
		opacity: 0.75;
	}
	/*
	 * A busy holo button keeps drifting. It is the one place in the kit
	 * where the ambient animation is load-bearing rather than
	 * decorative: the mesh moving is what says the work is still
	 * running, next to a spinner that says the same thing more quietly.
	 */
	:host( [ variant='holo' ][ busy ] ) button {
		animation: os-holo-drift 12s ease-in-out infinite;
	}
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ variant='holo' ][ busy ] ) button {
			animation: none;
		}
	}
	.os-button__spinner {
		box-sizing: border-box;
		display: inline-block;
		width: 12px;
		height: 12px;
		border: 2px solid currentColor;
		border-right-color: transparent;
		border-radius: 50%;
		animation: os-button-spin 0.6s linear infinite;
		flex-shrink: 0;
	}
	@keyframes os-button-spin {
		to {
			transform: rotate( 360deg );
		}
	}
`;var L=class extends g{render(){let o=this.disabled!==null,e=this.busy!==null,t=this.type||"button";return n`
			<button
				part="button"
				class="os-holo-edge os-holo-sheen"
				type=${t}
				?disabled=${o||e}
				aria-busy=${e?"true":"false"}
			>
				${e?n`<span class="os-button__spinner" aria-hidden="true"></span>`:""}
				<slot></slot>
				<!--
					The two motion layers. Elements rather than
					pseudo-elements because the sheen and the hairline
					have already taken this button's ::before and
					::after — see the pseudo-element budget note in
					src/ui/holo.ts. Both are inert and aria-hidden.
				-->
				<span class="os-holo-glint" aria-hidden="true"></span>
				<span class="os-holo-ring" aria-hidden="true"></span>
			</button>
		`}};L.props=["variant","disabled","type","busy","fill-cell"],L.styles=[ue],L.help={title:"Button",summary:"Thin wrapper around <button> with consistent variant styling and a slot for the label.",status:"stable",props:[{name:"variant",type:"'holo' | 'primary' | 'secondary' | 'ghost' | 'danger' | 'link'",default:"ghost",description:"Visual weight of the button. Use primary for the single attention-grabbing action per surface, and holo \u2014 the Holomesh fill \u2014 only for a hero call to action."},{name:"disabled",type:"boolean attribute",description:"Disable pointer + keyboard interaction and dim the chrome."},{name:"type",type:"'button' | 'submit' | 'reset'",default:"button",description:"Forwarded to the underlying native <button>."},{name:"busy",type:"boolean attribute",description:"Marks the button as in-progress (e.g., awaiting a fetch)."},{name:"fill-cell",type:"boolean attribute",description:"Grow to fill the parent flex/grid cell. Useful for tiled keypads."}],slots:[{name:"(default)",description:"Button label."}],parts:[{name:"button",description:"Underlying <button> element."}],cssProps:[{name:"--os-ui-button-bg",description:"Background color."},{name:"--os-ui-button-bg-hover",description:"Hover wash (ghost + secondary variants)."},{name:"--os-ui-button-fg",description:"Text color."},{name:"--os-ui-button-border",description:"Border shorthand."},{name:"--os-ui-button-border-radius",default:"6px"},{name:"--os-ui-button-padding",default:"6px 12px"},{name:"--os-ui-button-min-height",description:"Minimum height when fill-cell is set."}],example:n`
			<os-cluster gap="8">
				<os-button variant="holo">Holo</os-button>
				<os-button variant="primary">Primary</os-button>
				<os-button variant="secondary">Secondary</os-button>
				<os-button variant="ghost">Ghost</os-button>
				<os-button variant="danger">Danger</os-button>
				<os-button variant="link">Link</os-button>
			</os-cluster>
		`};f("os-button",L);var he=u`
	${$}
	${se}

	:host {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		font-size: 12px;
		color: var( --os-ui-fg, #1d2327 );
		cursor: pointer;
		/*
		 * A label row is a target, not a text run. Selecting the text
		 * on a fast double-toggle leaves the row highlighted and is
		 * never what anyone meant by clicking it twice.
		 */
		-webkit-user-select: none;
		user-select: none;
	}
	label {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		cursor: pointer;
	}
	:host( [ disabled ] ) {
		opacity: 0.5;
		cursor: not-allowed;
	}
	:host( [ disabled ] ) label,
	:host( [ disabled ] ) input[ type='checkbox' ] {
		cursor: not-allowed;
	}
`;var I=class extends g{render(){let o=this.label||"",e=this.checked!==null,t=this.disabled!==null;return n`
			<label>
				<input
					type="checkbox"
					?checked=${e}
					?disabled=${t}
					@change=${s=>this._onChange(s)}
				/>
				<span class="os-checkbox-label__text">${o}</span>
			</label>
		`}_onChange(o){if(this.disabled!==null)return;let e=o.target.checked;e?this.setAttribute("checked",""):this.removeAttribute("checked"),this.emit("os-checkbox-change",{checked:e})}};I.props=["label","checked","disabled"],I.styles=[he],I.help={title:"Checkbox label",summary:"Opinionated label-row variant of <os-checkbox>: label text + checkbox in a single aligned row. Use when you want the shipped layout without any layout work.",status:"stable",props:[{name:"label",type:"string",description:"Visible label text, paired with the checkbox via a native <label>."},{name:"checked",type:"boolean attribute",description:"Reflects and controls the checked state."},{name:"disabled",type:"boolean attribute",description:"When present, the checkbox is not interactive and dimmed."}],events:[{name:"os-checkbox-change",description:"Fires when the user toggles the checkbox.",detail:"{ checked: boolean }"}],cssProps:[{name:"--os-ui-fg",description:"Label colour."}],example:n`
			<os-checkbox-label label="Reduce motion" checked></os-checkbox-label>
		`};f("os-checkbox-label",I);var pe=u`
	:host {
		display: inline-flex;
		align-items: center;
		gap: 6px;
		font-size: var( --os-ui-save-status-font-size, 11px );
		line-height: 1;
		color: var( --os-ui-save-status-fg, currentColor );
		vertical-align: middle;
		min-width: 0;
		opacity: 1;
		pointer-events: auto;
	}

	/*
	 * Indicator is ALWAYS visible — like a real modem's "ready" LED.
	 * Idle state shows a hollow ring tinted by the user's accent
	 * (so the title bar always reads "alive, ready"); activity
	 * phases swap to a solid fill plus the chaotic modem-blink with
	 * a soft glow. The accent ring keeps idle calm; the blink reads
	 * as data flowing.
	 *
	 * The ring honours --os-ui-save-status-idle-color first; falls
	 * back to a translucent admin-theme accent (color-mix blends
	 * the live --wp-admin-theme-color toward transparent at ~55%
	 * opacity) so the dot always picks up the active accent
	 * automatically.
	 */
	.os-save-status__indicator {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: var( --os-ui-save-status-size, 12px );
		height: var( --os-ui-save-status-size, 12px );
		border-radius: 50%;
		flex-shrink: 0;
		box-sizing: border-box;
		background: var( --os-ui-save-status-bg, transparent );
		border: 2px solid
			var(
				--os-ui-save-status-idle-color,
				color-mix(
					in srgb,
					var( --wp-admin-theme-color, #2271b1 ) 55%,
					transparent
				)
			);
		color: var( --wp-admin-theme-color, #2271b1 );
		transition:
			background-color 0.2s ease,
			border-color 0.2s ease,
			box-shadow 0.2s ease;
	}

	/* Phase: pending / saving — primary-tinted blinking dot.
	 * Default cadence is a smooth pulse; animation="modem" swaps in
	 * the burst-style activity blink that mimics a 1990s data modem
	 * — short bursts of stutters with quiet pauses between, never
	 * the same shape twice in a row. */
	:host( [ phase='pending' ] ) .os-save-status__indicator,
	:host( [ phase='saving' ] ) .os-save-status__indicator {
		background: var(
			--os-ui-save-status-bg,
			var( --wp-admin-theme-color, #2271b1 )
		);
		border-color: transparent;
		color: var( --wp-admin-theme-color, #2271b1 );
		animation: os-save-status-pulse 1.2s ease-in-out infinite;
	}

	/* Modem-light animation — chaotic on/off bursts that mimic a
	 * 1990s modem's TX/RX LED while data flows. Two cycles ride
	 * over each other (a fast "stutter" track + a slower "burst"
	 * track) so the pattern never repeats cleanly within a few
	 * seconds. The eye reads it as data, not a metronome. */
	:host( [ animation='modem' ][ phase='pending' ] ) .os-save-status__indicator,
	:host( [ animation='modem' ][ phase='saving' ] ) .os-save-status__indicator {
		background: var(
			--os-ui-save-status-bg,
			var( --wp-admin-theme-color, #2271b1 )
		);
		border-color: transparent;
		color: var( --wp-admin-theme-color, #2271b1 );
		/* Slow, sparse cadence — a real modem's data LED isn't a
		 * strobe. Two tracks at slightly-different periods drift
		 * against each other so the pattern never reads as
		 * metronomic; the combined pattern only truly repeats every
		 * 7.2s (the LCM of the 1.8s and 2.4s periods). */
		animation:
			os-save-status-modem-stutter 1.8s ease-in-out infinite,
			os-save-status-modem-glow    2.4s ease-in-out infinite;
	}

	/*
	 * Stutter track — controls the dot opacity. Sparse, irregular
	 * pattern: a single quick flash, a long quiet, a doublet, more
	 * quiet, a longer flash, fade. Fewer blinks per cycle than the
	 * previous "burst" design — the eye reads it as a calm,
	 * intermittent activity LED rather than a strobe.
	 *
	 * Off-cycle opacity is 0.2 (not 0) so the dot stays present as
	 * a "dim glow" between flashes — closer to how a real LED looks
	 * dimming/brightening than a hard binary cut.
	 */
	@keyframes os-save-status-modem-stutter {
		/* Quick flash. */
		0%, 4%    { opacity: 1; }

		/* Long quiet. */
		5%, 30%   { opacity: 0.22; }

		/* Doublet — two short on-pulses with a beat between. */
		31%, 36%  { opacity: 1; }
		37%, 39%  { opacity: 0.22; }
		40%, 44%  { opacity: 1; }

		/* Quiet. */
		45%, 67%  { opacity: 0.22; }

		/* Single longer flash — the "burst" of the cycle. */
		68%, 76%  { opacity: 1; }

		/* Long quiet through the end. */
		77%, 100% { opacity: 0.22; }
	}

	/*
	 * Glow track — small halo timed to the brighter peaks. Lower
	 * blur radius (4px) and zero spread so the halo decorates the
	 * dot without dominating the title bar. Three soft pulses per
	 * cycle, drifting against the stutter.
	 */
	@keyframes os-save-status-modem-glow {
		0%, 12%   { box-shadow: 0 0 0 0 transparent; }
		13%, 22%  { box-shadow: 0 0 4px 0 currentColor; }
		23%, 50%  { box-shadow: 0 0 0 0 transparent; }
		51%, 58%  { box-shadow: 0 0 4px 0 currentColor; }
		59%, 84%  { box-shadow: 0 0 0 0 transparent; }
		85%, 94%  { box-shadow: 0 0 5px 0 currentColor; }
		95%, 100% { box-shadow: 0 0 0 0 transparent; }
	}

	/* Reduced-motion: disable the chaotic blink. Keep a solid lit
	 * dot so the user still sees "something is happening" — same
	 * affordance, calmer animation. */
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ phase='pending' ] ) .os-save-status__indicator,
		:host( [ phase='saving' ] ) .os-save-status__indicator,
		:host( [ animation='modem' ][ phase='pending' ] ) .os-save-status__indicator,
		:host( [ animation='modem' ][ phase='saving' ] ) .os-save-status__indicator {
			animation: none;
			opacity: 0.85;
		}
	}

	/* Phase: saved — solid green, no pulse. The hollow ring
	 * "fills in" momentarily before auto-clearing back to idle.
	 *
	 * The second fallback is the SEMANTIC colour, not --os-ui-surface
	 * — routing it through the surface token was the classic
	 * fallback-chain collapse: the palette declares --os-ui-surface
	 * (Obsidian), so the green dot resolved to the same near-black as
	 * the panel behind it and vanished. */
	:host( [ phase='saved' ] ) .os-save-status__indicator {
		background: var( --os-ui-save-status-saved-bg, var( --os-ui-success-fg, #1d6f42 ) );
		border-color: transparent;
		color: var( --os-ui-save-status-saved-bg, var( --os-ui-success-fg, #1d6f42 ) );
	}

	/* Phase: failed — solid red, gentle attention pulse. */
	:host( [ phase='failed' ] ) .os-save-status__indicator {
		background: var( --os-ui-save-status-failed-bg, var( --os-ui-danger, #d63638 ) );
		border-color: transparent;
		color: var( --os-ui-save-status-failed-bg, var( --os-ui-danger, #d63638 ) );
		animation: os-save-status-pulse 0.8s ease-in-out 2;
	}

	/*
	 * Variant: 'ring' — the outline IS the indicator.
	 *
	 * The default treatment fills the dot for every phase that isn't
	 * idle, which makes three of the five states the same shape in
	 * three colours. The ring keeps the outline and moves the COLOUR
	 * through it, spending the filled state on exactly one thing:
	 * success. That's what makes "it worked" the only phase that
	 * reads at a glance, which is the phase users are actually
	 * waiting for.
	 *
	 * Built for the window title bar, where the ring replaced the app
	 * icon — a duplicate of the dock tile below it — so the only mark
	 * in that corner is one that means something.
	 */
	/*
	 * The ring carries NO resting fill, and this rule is what
	 * guarantees it.
	 *
	 * --os-ui-save-status-bg is the dot's background — the base rule
	 * reads it as the background, defaulting to transparent. A
	 * consumer setting it to colour the ring's in-flight outline
	 * therefore painted that colour as a solid idle fill: one token
	 * meaning two different things one rule apart, which is why the
	 * ring wants a name of its own (--os-ui-save-status-ring-color,
	 * below) and why the fill is pinned off here regardless of what
	 * anyone sets. Only saved reinstates a background, at higher
	 * specificity.
	 */
	:host( [ variant='ring' ] ) .os-save-status__indicator {
		background: transparent;
	}

	:host( [ variant='ring' ][ phase='pending' ] ) .os-save-status__indicator,
	:host( [ variant='ring' ][ phase='saving' ] ) .os-save-status__indicator {
		background: transparent;
		border-color: var(
			--os-ui-save-status-ring-color,
			var( --os-ui-save-status-bg, var( --wp-admin-theme-color, #2271b1 ) )
		);
		animation: os-save-status-ring-pulse 1.6s
			var( --os-ui-ease-loop, ease-in-out ) infinite;
	}

	/*
	 * Failure keeps the ring open and tints the glyph rather than
	 * the fill. A filled red disc and a filled accent disc are the
	 * same silhouette, and colour alone is not a distinction every
	 * user can make.
	 */
	/*
	 * Failure keeps the ring open and tints the glyph rather than
	 * the fill, and arrives with two quick swells — an alert, not a
	 * heartbeat, so it stops rather than continuing.
	 */
	:host( [ variant='ring' ][ phase='failed' ] ) .os-save-status__indicator {
		background: transparent;
		border-color: var(
			--os-ui-save-status-failed-bg,
			var( --os-ui-danger, #d63638 )
		);
		animation: os-save-status-ring-alert 0.62s
			var( --os-ui-ease-out, ease-out ) 1;
	}

	:host( [ variant='ring' ][ phase='failed' ] ) .os-save-status__glyph {
		color: var(
			--os-ui-save-status-failed-bg,
			var( --os-ui-danger, #d63638 )
		);
	}

	/*
	 * Success is the one state that fills, so it is also the one that
	 * gets a gesture: the ring overshoots slightly and settles, the
	 * way a thing does when it lands. The colours cross-fade under it
	 * on the base rule's own transition — border to transparent, fill
	 * to the accent — so the outline doesn't blink out and reappear as
	 * a disc.
	 */
	:host( [ variant='ring' ][ phase='saved' ] ) .os-save-status__indicator {
		background: var(
			--os-ui-save-status-saved-bg,
			var( --wp-admin-theme-color, #2271b1 )
		);
		border-color: transparent;
		animation: os-save-status-ring-land 0.42s
			var( --os-ui-ease-out, ease-out ) 1;
	}

	/*
	 * The glyph arrives after the shape it sits in — a check that
	 * fades up into a ring already filling reads as one event, where
	 * both appearing together reads as a swap. The backwards fill-mode
	 * holds the 0% frame during the delay, so the glyph can't flash at
	 * full size before the animation starts.
	 */
	:host( [ variant='ring' ][ phase='saved' ] ) .os-save-status__glyph,
	:host( [ variant='ring' ][ phase='failed' ] ) .os-save-status__glyph {
		animation: os-save-status-glyph-in 0.24s
			var( --os-ui-ease-out, ease-out ) 0.08s 1 backwards;
	}

	/*
	 * The pulse breathes the ring rather than blinking it — opacity
	 * and scale only, both compositor properties, and neither
	 * reaching zero. A ring that disappears between beats reads as a
	 * fault; one that breathes reads as work continuing.
	 */
	@keyframes os-save-status-ring-pulse {
		0%, 100% { opacity: 0.45; scale: 0.9; }
		50%      { opacity: 1;    scale: 1; }
	}

	/* Overshoot and settle. Small — 6% — because the ring is 16px in
	 * a title bar, and at that size a big bounce reads as a wobble. */
	@keyframes os-save-status-ring-land {
		0%   { scale: 0.82; }
		55%  { scale: 1.06; }
		100% { scale: 1; }
	}

	/* Two swells, decaying. Never past 1.08 for the same reason. */
	@keyframes os-save-status-ring-alert {
		0%   { scale: 0.9;  }
		25%  { scale: 1.08; }
		50%  { scale: 0.98; }
		75%  { scale: 1.04; }
		100% { scale: 1;    }
	}

	@keyframes os-save-status-glyph-in {
		0%   { opacity: 0; scale: 0.4; }
		100% { opacity: 1; scale: 1;   }
	}

	/*
	 * Reduced motion: every phase still changes colour and fill, and
	 * none of them move. The ring holds lit instead of breathing, and
	 * the outcomes arrive without their gesture — the gesture is
	 * emphasis, the fill and the glyph are the information.
	 */
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ variant='ring' ] ) .os-save-status__indicator,
		:host( [ variant='ring' ] ) .os-save-status__glyph {
			animation: none;
			opacity: 1;
			scale: 1;
		}
	}

	@keyframes os-save-status-pulse {
		0%, 100% { opacity: 0.55; transform: scale( 0.9 ); }
		50%      { opacity: 1;    transform: scale( 1 ); }
	}

	/* Mode: 'pill' shows a label next to the dot. Defaults to 'dot'
	 * (no label, indicator only) for the smallest footprint. */
	:host( [ mode='pill' ] ) .os-save-status {
		display: inline-flex;
		align-items: center;
		gap: 6px;
		padding: 2px 10px;
		border-radius: 999px;
		background: var( --os-ui-save-status-pill-bg, transparent );
		font-weight: 500;
		white-space: nowrap;
	}
	:host( [ mode='pill' ][ phase='saving' ] ) .os-save-status,
	:host( [ mode='pill' ][ phase='pending' ] ) .os-save-status {
		background: var( --os-ui-save-status-pill-bg, var( --os-ui-hover, rgba( 0, 0, 0, 0.04 ) ) );
		color: var( --os-ui-save-status-pill-fg, var( --os-ui-fg-muted, #50575e ) );
	}
	:host( [ mode='pill' ][ phase='saved' ] ) .os-save-status {
		background: var( --os-ui-save-status-pill-bg, rgba( 30, 132, 73, 0.12 ) );
		color: var( --os-ui-save-status-pill-fg, var( --os-ui-success-fg, #1d6f42 ) );
	}
	:host( [ mode='pill' ][ phase='failed' ] ) .os-save-status {
		background: var( --os-ui-save-status-pill-bg, rgba( 214, 54, 56, 0.12 ) );
		color: var( --os-ui-save-status-pill-fg, var( --os-ui-danger-hover, #a02622 ) );
	}

	.os-save-status__label {
		min-width: 0;
		max-width: 200px;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	/* Mode: 'icon' renders a glyph (check / cross) inside the dot
	 * for the saved / failed phases. The svg inherits color from the
	 * indicator's foreground. */
	:host( [ phase='saved' ] ) .os-save-status__glyph,
	:host( [ phase='failed' ] ) .os-save-status__glyph {
		display: inline-block;
		color: var( --os-ui-fg-on-accent, #fff );
		/* Proportional to the indicator so a resized ring keeps its
		 * glyph in the same relationship to the stroke. */
		width: calc( var( --os-ui-save-status-size, 12px ) * 0.66 );
		height: calc( var( --os-ui-save-status-size, 12px ) * 0.66 );
	}
	.os-save-status__glyph {
		display: none;
	}
	.os-save-status__glyph svg {
		display: block;
		width: 100%;
		height: 100%;
	}
`;var ge="os-settings-save-lifecycle",Oe=2200,qe=6e3,z=class extends g{constructor(){super(...arguments);this._autoTimer=null;this._docListener=null}connectedCallback(){super.connectedCallback(),this.auto!==null&&this._installAutoListener()}disconnectedCallback(){this._removeAutoListener(),this._autoTimer!==null&&(window.clearTimeout(this._autoTimer),this._autoTimer=null)}attributeChangedCallback(e,t,s){if(super.attributeChangedCallback(e,t,s),(e==="auto"||e==="event")&&(this._removeAutoListener(),this.auto!==null&&this._installAutoListener()),e==="phase"){this._scheduleAutoClear();let a={phase:this.phase??"idle",error:this.error??void 0};this.emit("os-save-status-change",a)}}render(){let e=this.phase??"idle",t=this.mode??"dot",a=(this.error??"")||this._labelForPhase(e);return a?this.setAttribute("title",a):this.removeAttribute("title"),this.setAttribute("aria-live",e==="failed"?"assertive":"polite"),this.setAttribute("role",e==="failed"?"alert":"status"),n`
			<span class="os-save-status">
				<span class="os-save-status__indicator" aria-hidden="true">
					<span class="os-save-status__glyph">${this._renderGlyph(e)}</span>
				</span>
				${t==="pill"?n`<span class="os-save-status__label"
							>${this._labelForPhase(e)}</span
					  >`:n``}
			</span>
		`}_renderGlyph(e){return e==="saved"?He():e==="failed"?Ue():""}_labelForPhase(e){switch(e){case"pending":case"saving":return this["saving-label"]??"Saving\u2026";case"saved":return this["saved-label"]??"Saved";case"failed":return(this.error??"")||"Couldn\u2019t save";default:return this["idle-label"]??""}}_installAutoListener(){let e=this.event||ge;this._docListener=t=>{let s=t.detail;!s||typeof s.phase!="string"||(this.phase=s.phase,s.error?this.error=s.error:s.phase!=="failed"&&this.error&&this.removeAttribute("error"))},document.addEventListener(e,this._docListener)}_removeAutoListener(){if(!this._docListener)return;let e=this.event||ge;document.removeEventListener(e,this._docListener),this._docListener=null}_scheduleAutoClear(){this._autoTimer!==null&&(window.clearTimeout(this._autoTimer),this._autoTimer=null);let e=this.phase??"idle",t=this._autoClearMsFor(e);t<=0||(this._autoTimer=window.setTimeout(()=>{this._autoTimer=null,this.phase="idle"},t))}_autoClearMsFor(e){if(e==="saved"){let t=this["auto-clear-saved-ms"];return parseInt(t||"",10)||Oe}if(e==="failed"){let t=this["auto-clear-failed-ms"];return parseInt(t||"",10)||qe}return 0}};z.props=["phase","mode","variant","animation","auto","event","error","saving-label","saved-label","idle-label","auto-clear-saved-ms","auto-clear-failed-ms"],z.styles=[pe],z.help={title:"Save status",summary:'Tiny status indicator for "is this change saved yet?" affordances. Three layouts (dot / icon / pill), five phases, optional auto-listen to a save-lifecycle CustomEvent so every input in the panel inherits feedback for free.',status:"stable",props:[{name:"phase",type:"'idle' | 'pending' | 'saving' | 'saved' | 'failed'",default:"idle",description:"Current lifecycle phase. Set manually for one-off integrations, or rely on `auto` to populate it from a CustomEvent."},{name:"mode",type:"'dot' | 'icon' | 'pill'",default:"dot",description:"Layout. `dot` is the smallest (10\xD710 colored dot); `icon` adds a glyph inside on saved/failed; `pill` adds an inline label."},{name:"variant",type:"'dot' (default) | 'ring'",description:"Treatment. The default fills the indicator for every non-idle phase; `ring` keeps the outline open and moves the colour through it, filling only on `saved` \u2014 so success is the one state that reads at a glance. Failure tints the glyph rather than the fill, since a filled red disc and a filled accent disc are the same silhouette. This is what the window title bar wears."},{name:"animation",type:"'pulse' | 'modem'",default:"pulse",description:"Animation cadence during the saving phase. `pulse` (default) is a smooth ease-in-out; `modem` is an irregular activity-LED blink with a soft glow \u2014 suits a 'data-flowing' affordance in window title bars."},{name:"auto",type:"boolean attribute",description:'Subscribe to a CustomEvent on `document` and populate phase + error from its detail. Default event name is `os-settings-save-lifecycle`; override with `event="\u2026"`.'},{name:"event",type:"string",default:"os-settings-save-lifecycle",description:"CustomEvent name to listen on when `auto` is set."},{name:"error",type:"string",description:"Error message shown in `pill` mode and exposed as the host title attribute (so dot/icon modes still surface the message via tooltip)."},{name:"saving-label",type:"string",default:"Saving\u2026",description:"Pill-mode label shown during `pending` / `saving`."},{name:"saved-label",type:"string",default:"Saved",description:"Pill-mode label shown during `saved`."},{name:"idle-label",type:"string",description:'Optional pill-mode label shown during `idle` (e.g. "All changes saved"). When unset, the pill collapses to invisible while idle.'},{name:"auto-clear-saved-ms",type:"integer",default:"2200",description:"How long the `saved` phase stays visible before auto-fading back to `idle`."},{name:"auto-clear-failed-ms",type:"integer",default:"6000",description:"How long the `failed` phase stays visible before auto-fading back to `idle`."}],events:[{name:"os-save-status-change",description:"Fires when the phase changes (manually or via auto-listen).",detail:"{ phase, error }"}],cssProps:[{name:"--os-ui-save-status-size",description:"Indicator diameter (default 12px). The glyph scales with it."},{name:"--os-ui-save-status-idle-color",description:"Ring colour at rest."},{name:"--os-ui-save-status-bg",description:'Indicator background color (saving/pending phase). Under `variant="ring"` set `--os-ui-save-status-ring-color` instead \u2014 this one is a *background*, and the ring has none until it succeeds.'},{name:"--os-ui-save-status-ring-color",description:'`variant="ring"` only: the outline colour while a request is in flight. Falls back to `--os-ui-save-status-bg`.'},{name:"--os-ui-save-status-saved-bg",description:"Indicator background on saved."},{name:"--os-ui-save-status-failed-bg",description:"Indicator background on failed."},{name:"--os-ui-save-status-pill-bg",description:"Pill background (mode=pill)."},{name:"--os-ui-save-status-pill-fg",description:"Pill foreground (mode=pill)."}],example:n`
			<os-cluster gap="12">
				<os-save-status phase="pending"></os-save-status>
				<os-save-status phase="saving"></os-save-status>
				<os-save-status phase="saved"></os-save-status>
				<os-save-status phase="failed"></os-save-status>
				<os-save-status mode="pill" phase="saving"></os-save-status>
				<os-save-status mode="pill" phase="saved"></os-save-status>
				<os-save-status mode="pill" phase="failed" error="Network error."></os-save-status>
				<os-save-status variant="ring" mode="icon" phase="saving"></os-save-status>
				<os-save-status variant="ring" mode="icon" phase="saved"></os-save-status>
				<os-save-status variant="ring" mode="icon" phase="failed"></os-save-status>
			</os-cluster>
		`};f("os-save-status",z);function He(){return n`
		<svg
			viewBox="0 0 12 12"
			aria-hidden="true"
			focusable="false"
			fill="none"
			stroke="currentColor"
			stroke-width="2"
			stroke-linecap="round"
			stroke-linejoin="round"
		>
			<path d="M2.5 6 L5 8.5 L9.5 4" />
		</svg>
	`}function Ue(){return n`
		<svg
			viewBox="0 0 12 12"
			aria-hidden="true"
			focusable="false"
			fill="currentColor"
		>
			<path
				d="M5 2 H7 V7 H5 z M5 8.5 H7 V10.5 H5 z"
			/>
		</svg>
	`}var me=u`
	:host {
		display: inline-block;
		--_color: var(
			--os-ui-spinner-color,
			var( --wp-admin-theme-color, #21759b )
		);
		/*
		 * Falls back to the ON-ACCENT foreground, not to the accent.
		 * The mark is drawn OVER the disc, and the disc resolves to
		 * --wp-admin-theme-color; the palette sets that and --os-ui-accent
		 * to the same Pulse, so pointing both aliases at an accent
		 * painted the W in the disc's own colour and the spinner became
		 * a plain filled circle. The pre-brand literals (#21759b disc,
		 * #fff mark) had hidden that they were ever the same idea.
		 */
		--_accent: var(
			--os-ui-spinner-accent,
			var( --os-ui-fg-on-accent, #fff )
		);
		--_size: var( --os-ui-spinner-size, 48px );
		width: var( --_size );
		height: var( --_size );
		/* SVG inner elements use currentColor for the primary fill /
		   stroke; inheriting the host's text color makes a single
		   variable drive every ring. */
		color: var( --_color );
		vertical-align: middle;
		line-height: 0;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	/* The inline indicator lives beside a line of text: text-sized by
	   default, and tinted from the text rather than from the admin
	   theme color, so it can't lose contrast against a surface the
	   component knows nothing about. Both are plain defaults — the
	   size / color attributes still win, since those reflect onto
	   inline styles on the host. */
	:host( [ preset='inline' ] ) {
		--os-ui-spinner-color: currentColor;
		--os-ui-spinner-size: 16px;
	}

	.root,
	.root svg {
		display: block;
		width: 100%;
		height: 100%;
	}

	/* The W mark inside the disc — accent color, defaults to white,
	   configurable via the host's --os-ui-spinner-accent (or the
	   shorthand "accent" attribute). */
	.root svg .mark {
		fill: var( --_accent );
	}

	@keyframes os-spinner-spin {
		to {
			transform: rotate( 360deg );
		}
	}
	@keyframes os-spinner-scale {
		0%,
		100% {
			transform: scale( 1 );
		}
		50% {
			transform: scale( 1.045 );
		}
	}
	@keyframes os-spinner-opacity {
		0%,
		100% {
			opacity: 1;
		}
		50% {
			opacity: 0.7;
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		.root svg [ style*='animation' ] {
			animation: none !important;
		}
	}
`;var be=Object.freeze({classic:{sp1:12,sp2:24,sp3:40,a1:28,a2:15,a3:8,gap:4,dir2:1,dir3:-1,pulse:"none",dots:0},comet:{sp1:8,sp2:14,sp3:26,a1:50,a2:28,a3:12,gap:3,dir2:1,dir3:1,pulse:"none",dots:5},orbit:{sp1:10,sp2:10,sp3:32,a1:50,a2:50,a3:8,gap:5,dir2:-1,dir3:-1,pulse:"opacity",dots:3},pulse:{sp1:6,sp2:18,sp3:30,a1:20,a2:12,a3:6,gap:4,dir2:1,dir3:-1,pulse:"both",dots:8},inline:{sp1:8,sp2:8,sp3:8,a1:25,a2:25,a3:25,gap:0,dir2:1,dir3:1,pulse:"none",dots:0}}),S=61.26,T=61.26,fe=58.453,Be='<path d="m8.708 61.26c0 20.802 12.089 38.779 29.619 47.298l-25.069-68.686c-2.916 6.536-4.55 13.769-4.55 21.388z"/><path d="m96.74 58.608c0-6.495-2.333-10.993-4.334-14.494-2.664-4.329-5.161-7.995-5.161-12.324 0-4.831 3.664-9.328 8.825-9.328.233 0 .454.029.681.042-9.35-8.566-21.807-13.796-35.489-13.796-18.36 0-34.513 9.42-43.91 23.688 1.233.037 2.395.063 3.382.063 5.497 0 14.006-.667 14.006-.667 2.833-.167 3.167 3.994.337 4.329 0 0-2.847.335-6.015.501l19.138 56.925 11.501-34.493-8.188-22.434c-2.83-.166-5.511-.501-5.511-.501-2.832-.166-2.5-4.496.332-4.329 0 0 8.679.667 13.843.667 5.496 0 14.006-.667 14.006-.667 2.835-.167 3.168 3.994.337 4.329 0 0-2.853.335-6.015.501l18.992 56.494 5.242-17.517c2.272-7.269 4.001-12.49 4.001-16.989z"/><path d="m62.184 65.857-15.768 45.819c4.708 1.384 9.687 2.141 14.846 2.141 6.12 0 11.989-1.058 17.452-2.979-.141-.225-.269-.464-.374-.724z"/><path d="m107.376 36.046c.226 1.674.354 3.471.354 5.404 0 5.333-.996 11.328-3.996 18.824l-16.053 46.413c15.624-9.111 26.133-26.038 26.133-45.426.001-9.137-2.333-17.729-6.438-25.215z"/>',D=class extends g{constructor(){super(...arguments);this._paintScheduled=!1}connectedCallback(){super.connectedCallback(),this._schedulePaint()}render(){return n`<div class="root" part="root"></div>`}requestUpdate(){super.requestUpdate(),this._schedulePaint()}_schedulePaint(){this._paintScheduled||!this.isConnected||(this._paintScheduled=!0,queueMicrotask(()=>{this._paintScheduled=!1,this.isConnected&&this._paint()}))}_paint(){this._syncCssVars();let e=this.shadowRoot?.querySelector(".root");e&&(e.innerHTML=this._buildSvg())}_syncCssVars(){let e=(t,s,a)=>{let r=this.getAttribute(t);r===null?this.style.removeProperty(s):this.style.setProperty(s,a?a(r):r)};e("color","--os-ui-spinner-color"),e("accent","--os-ui-spinner-accent"),e("size","--os-ui-spinner-size",t=>/^-?\d+(\.\d+)?$/.test(t.trim())?`${t}px`:t)}_effectiveConfig(){let e=this.getAttribute("preset")??"classic",t=be[e]??be.classic,s=(d,l)=>{let c=this.getAttribute(d);if(c===null)return l;let h=parseFloat(c);return Number.isFinite(h)?h:l},a=(d,l)=>{let c=this.getAttribute(d);if(c===null)return l;let h=c.toLowerCase();return h==="-1"||h==="ccw"||h==="reverse"?-1:1},r=()=>{let d=this.getAttribute("pulse");return d==="scale"||d==="opacity"||d==="both"||d==="none"?d:t.pulse};return{sp1:s("sp1",t.sp1),sp2:s("sp2",t.sp2),sp3:s("sp3",t.sp3),a1:s("a1",t.a1),a2:s("a2",t.a2),a3:s("a3",t.a3),gap:s("gap",t.gap),dir2:a("dir2",t.dir2),dir3:a("dir3",t.dir3),pulse:r(),dots:Math.max(0,Math.floor(s("dots",t.dots)))}}_buildInlineSvg(e){let t=ve(this.getAttribute("label")??"Loading"),s=9,a=(e.sp1/10).toFixed(2);return`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" role="img" aria-label="${t}"><circle cx="12" cy="12" r="${s}" fill="none" stroke="currentColor" stroke-width="2.4" stroke-opacity="0.22"/><circle cx="12" cy="12" r="${s}" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-dasharray="${j(s,e.a1)}" style="transform-origin:12px 12px;animation: os-spinner-spin ${a}s linear infinite"/></svg>`}_buildSvg(){let e=this._effectiveConfig();if(this.getAttribute("preset")==="inline")return this._buildInlineSvg(e);let t=ve(this.getAttribute("label")??"Loading"),s=e.gap*3+14,a=-s,r=122.52+s*2,d=fe+e.gap+2,l=d+e.gap+2,c=l+e.gap+1.5,h=`animation: os-spinner-spin ${(e.sp1/10).toFixed(2)}s linear infinite`,v=`animation: os-spinner-spin ${(e.sp2/10).toFixed(2)}s linear infinite${e.dir2<0?" reverse":""}`,k=`animation: os-spinner-spin ${(e.sp3/10).toFixed(2)}s linear infinite${e.dir3<0?" reverse":""}`,y=(e.sp1*1.8/10).toFixed(1),m=(e.sp1*2.3/10).toFixed(1),b="";e.pulse==="scale"?b=`animation: os-spinner-scale ${y}s ease-in-out infinite`:e.pulse==="opacity"?b=`animation: os-spinner-opacity ${m}s ease-in-out infinite`:e.pulse==="both"&&(b=`animation: os-spinner-scale ${y}s ease-in-out infinite, os-spinner-opacity ${m}s ease-in-out infinite`);let p="";if(e.dots>0){let w=c+e.gap+1,x=2*Math.PI*w,C=1.6,A=(e.sp1*.65/10).toFixed(2);for(let H=0;H<e.dots;H++){let G=-(H/e.dots)*x;p+=`<circle cx="${S}" cy="${T}" r="${w.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="${C}" stroke-dasharray="${C.toFixed(2)} ${(x-C).toFixed(2)}" stroke-dashoffset="${G.toFixed(2)}" stroke-linecap="round" stroke-opacity="0.65" style="transform-origin:${S}px ${T}px;animation: os-spinner-spin ${A}s linear infinite"/>`}}return`<svg xmlns="http://www.w3.org/2000/svg" viewBox="${a} ${a} ${r} ${r}" role="img" aria-label="${t}"><g style="transform-origin:${S}px ${T}px${b?";"+b:""}"><circle cx="${S}" cy="${T}" r="${fe}" fill="currentColor"/><g class="mark">${Be}</g></g><circle cx="${S}" cy="${T}" r="${d.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="0.6" stroke-opacity="0.2"/><circle cx="${S}" cy="${T}" r="${d.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="2.2" stroke-dasharray="${j(d,e.a1)}" stroke-linecap="round" style="transform-origin:${S}px ${T}px;${h}"/><circle cx="${S}" cy="${T}" r="${l.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="0.5" stroke-opacity="0.15"/><circle cx="${S}" cy="${T}" r="${l.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="1.6" stroke-opacity="0.8" stroke-dasharray="${j(l,e.a2)}" stroke-linecap="round" style="transform-origin:${S}px ${T}px;${v}"/><circle cx="${S}" cy="${T}" r="${c.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="0.4" stroke-opacity="0.12"/><circle cx="${S}" cy="${T}" r="${c.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="1.0" stroke-opacity="0.6" stroke-dasharray="${j(c,e.a3)}" stroke-linecap="round" style="transform-origin:${S}px ${T}px;${k}"/>`+p+"</svg>"}};D.props=["preset","size","color","accent","sp1","sp2","sp3","a1","a2","a3","gap","dir2","dir3","pulse","dots","label"],D.styles=[me],D.help={title:"Spinner",summary:"Animated WordPress-mark loading indicator with five curated presets and full per-attribute overrides. The `inline` preset swaps the mark for a bare arc sized for text-adjacent use. CSS variables drive disc + accent colors and size; reduced-motion preferences are respected.",status:"stable",props:[{name:"preset",type:'"classic" | "comet" | "orbit" | "pulse" | "inline"',default:"classic",description:"Visual personality. Every other attribute defaults to the preset's value and can be overridden individually. `inline` is the odd one out: a bare rotating arc with no WordPress mark, defaulting to 16px and to `currentColor`, for use beside a line of text where the mark would be illegible."},{name:"size",type:"integer (px) or CSS length",default:'48 (16 for preset="inline")',description:"Sets `--os-ui-spinner-size`. Bare numbers are treated as px; pass a CSS length (e.g. `2em`) to opt into ems / rems."},{name:"color",type:"CSS color",description:"Disc + ring + dot color. Sets `--os-ui-spinner-color`. Default inherits the WP admin theme color."},{name:"accent",type:"CSS color",default:"#fff",description:"Color of the W mark inside the disc. Sets `--os-ui-spinner-accent`. Default white \u2014 change for dark-on-light or themed marks."},{name:"sp1, sp2, sp3",type:"integer (deciseconds)",description:"Per-ring rotation duration in tenths-of-a-second (12 \u2192 1.2s). Higher = slower."},{name:"a1, a2, a3",type:"integer (0-100)",description:"Per-ring arc length as a percentage of the ring circumference."},{name:"gap",type:"integer",description:"Gap between concentric rings (units approximate to px at 120-viewport)."},{name:"dir2, dir3",type:'"1" | "-1" | "cw" | "ccw"',description:"Per-ring direction; ring 1 is always clockwise."},{name:"pulse",type:'"none" | "scale" | "opacity" | "both"',description:"Pulse animation applied to the disc + W mark."},{name:"dots",type:"integer",description:"Outer trailing dot count. Sensible values: 0, 3, 5, 8."},{name:"label",type:"string",default:"Loading",description:'Accessible name for the SVG (`role="img"` + `aria-label`).'}],cssProps:[{name:"--os-ui-spinner-color",default:"var(--wp-admin-theme-color, #21759b)"},{name:"--os-ui-spinner-accent",default:"var(--os-ui-accent, #fff)"},{name:"--os-ui-spinner-size",default:"48px"}],example:n`<os-spinner preset="comet" size="80"></os-spinner>`};function j(i,o){let e=2*Math.PI*i,t=o/100*e,s=e-t;return`${t.toFixed(2)} ${s.toFixed(2)}`}function ve(i){return String(i).replace(/&/g,"&amp;").replace(/"/g,"&quot;").replace(/</g,"&lt;").replace(/>/g,"&gt;")}f("os-spinner",D);var ye=u`
	:host {
		display: grid;
		grid-template-columns: repeat(
			var( --os-ui-swatch-grid-cols, 4 ),
			1fr
		);
		gap: 12px;
	}
	:host( [ mode='row' ] ) {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 10px;
	}
`;var N=class extends g{render(){let o=this.label||"",e=this.columns||"";return e&&this.style.setProperty("--os-ui-swatch-grid-cols",e),this.setAttribute("role","radiogroup"),o&&this.setAttribute("aria-label",o),n`<slot></slot>`}};N.props=["label","columns","mode"],N.styles=[ye],N.help={title:"Swatch grid",summary:"Flex grid container for <os-swatch> children. Emits radiogroup semantics so screen readers announce the tiles as a unit.",status:"stable",props:[{name:"label",type:"string",description:'aria-label describing the group (e.g. "Accent color").'},{name:"columns",type:"CSS grid track template",description:"Overrides the default column track via --os-ui-swatch-grid-cols."},{name:"mode",type:"string",description:"Optional rendering variant forwarded to child swatches."}],slots:[{name:"(default)",description:"<os-swatch> children."}],cssProps:[{name:"--os-ui-swatch-grid-cols",description:"Grid column template."}],example:n`
			<os-swatch-grid label="Wallpaper">
				<os-swatch value="a" preview="linear-gradient(135deg,#f093fb,#f5576c)"></os-swatch>
				<os-swatch value="b" preview="linear-gradient(135deg,#4facfe,#00f2fe)"></os-swatch>
				<os-swatch value="c" preview="linear-gradient(135deg,#43e97b,#38f9d7)"></os-swatch>
			</os-swatch-grid>
		`};f("os-swatch-grid",N);var we=u`
	${$}

	:host {
		display: block;
		width: 100%;
		aspect-ratio: 4 / 3;
	}
	:host( [ size='small' ] ) {
		display: inline-block;
		width: 32px;
		height: 32px;
		aspect-ratio: 1 / 1;
		flex: 0 0 auto;
	}
	/*
	 * Accent variant: the rounded square, at the guide's inner radius.
	 *
	 * A circle is the obvious shape for a colour chip and the wrong one
	 * here. Everything else in this panel that can be picked is a
	 * rounded rectangle (wallpaper tiles, theme cards, layout cards),
	 * so a row of discs read as a legend rather than as a row of
	 * choices. Slightly smaller than the plain small size for the same
	 * reason: a colour is the least of the decisions on this page.
	 */
	:host( [ variant='accent' ] ) {
		display: inline-block;
		width: 28px;
		height: 28px;
		aspect-ratio: 1 / 1;
		flex: 0 0 auto;
	}
	/*
	 * Both selectors name the small size as well, which is not
	 * redundant: the circle comes from the size-only button rule
	 * further down this sheet, and at equal specificity the later rule
	 * wins. Naming both attributes puts this one above it whatever the
	 * order.
	 */
	:host( [ size='small' ][ variant='accent' ] ) button {
		border-radius: 8px;
	}
	/*
	 * The accent chip's ring holds the surface colour as a gap between
	 * chip and ring, so the ring reads as drawn AROUND the colour
	 * rather than retinting its edge, which matters more here than on
	 * any tile, because the colour is the entire content.
	 */
	:host( [ size='small' ][ variant='accent' ] )
		button[ aria-pressed='true' ] {
		box-shadow: 0 0 0 2px var( --os-ui-surface-sunken, #f0f0f1 ),
			0 0 0 4px var( --os-ui-accent, #2271b1 );
	}
	/*
	 * Keyboard focus outranks the selection ring while it is visible.
	 * The focus ring is a strict superset of the selection ring's
	 * shape (gap, accent, bloom), so nothing is lost while it shows;
	 * without this the selected chip is the one chip focus cannot be
	 * seen on, because the selected rules above outrank the plain
	 * focus rule below.
	 */
	:host( [ size='small' ][ variant='accent' ] )
		button[ aria-pressed='true' ]:focus-visible {
		box-shadow: var( --_holo-focus );
	}
	/*
	 * Wallpaper variant: 16:10 aspect (the mockup's desk proportion),
	 * and positions slotted overlay content (e.g. a label chip) at the
	 * bottom-left so it reads like a photo-corner caption. Caller owns
	 * the label's own visual treatment; we just place it.
	 */
	:host( [ variant='wallpaper' ] ) {
		aspect-ratio: 16 / 10;
	}
	:host( [ variant='wallpaper' ] ) button {
		display: flex;
		align-items: flex-end;
		justify-content: flex-start;
		padding: 8px;
		overflow: hidden;
	}
	button {
		appearance: none;
		/* Anchor absolutely-positioned slotted overlays (the wallpaper
		 * grid's live-preview layer) to the tile. */
		position: relative;
		width: 100%;
		height: 100%;
		padding: 0;
		border-radius: 10px;
		border: 2px solid transparent;
		cursor: pointer;
		/* The tile's base, visible wherever the swatch image is
		   transparent or still loading. A fixed light grey punched a
		   hole in a dark station, so it follows the surface ramp. */
		background-color: var( --os-ui-surface-sunken, #eee );
		background-size: cover;
		background-position: center;
		transition: transform 0.15s ease, border-color 0.15s ease,
			box-shadow 0.15s ease;
	}
	:host( [ size='small' ] ) button {
		border-radius: 50%;
	}
	button:hover {
		transform: scale( 1.04 );
	}
	button:focus-visible {
		outline: none;
		box-shadow: var( --_holo-focus );
	}
	/*
	 * Chosen. A flat accent ring, not the mesh: the brand reserves
	 * meshes for hero surfaces, and in OpenStation Preferences that
	 * budget is spent on the sidebar's selected-row edge. A grid of
	 * mesh-ringed tiles beside it is wallpaper.
	 *
	 * box-shadow rather than border so the ring sits OUTSIDE the
	 * tile, which matters for a wallpaper swatch: the artwork is the
	 * content, and a ring drawn on top of it would crop the thing the
	 * user is choosing.
	 */
	button[ aria-pressed='true' ] {
		border-color: transparent;
		box-shadow: 0 0 0 2px var( --os-ui-accent, #2271b1 );
	}
	/* See the accent-variant note: focus wins while it is visible. */
	button[ aria-pressed='true' ]:focus-visible {
		box-shadow: var( --_holo-focus );
	}
	/*
	 * Wallpaper variant uses a softer lift to pair with the
	 * larger visible surface — hover scale on a 200 px tile can
	 * feel cartoonish.
	 */
	:host( [ variant='wallpaper' ] ) button:hover {
		transform: translateY( -2px );
	}
`;var F=class extends g{render(){let o=this.selected!==null,e=this.label||"",t=this.preview||"";return n`
			<button
				type="button"
				aria-pressed=${o?"true":"false"}
				aria-label=${e}
				title=${e}
				style="background: ${t}"
				@click=${()=>this._onPick()}
			>
				<slot></slot>
			</button>
		`}_onPick(){this.emit("os-pick",{value:this.value})}};F.props=["value","label","selected","preview","size","variant"],F.styles=[we],F.help={title:"Swatch",summary:"Selectable color/wallpaper tile. Renders as an aria-pressed button with a background driven by the preview attribute.",status:"stable",props:[{name:"value",type:"string",description:"Identifier emitted on the os-pick event when the swatch is clicked."},{name:"label",type:"string",description:"aria-label + title for the button."},{name:"selected",type:"boolean attribute",description:"Marks the swatch as the active choice within a swatch-grid."},{name:"preview",type:"CSS background value",description:"Raw CSS background (color, gradient, url()) painted on the tile."},{name:"size",type:"string",description:"Visual size hint (e.g. sm, md, lg). Consumed by the stylesheet."},{name:"variant",type:"string",description:"Optional visual variant (e.g. color vs wallpaper)."}],slots:[{name:"(default)",description:"Optional overlay content rendered inside the tile."}],events:[{name:"os-pick",description:"Fires when the swatch is clicked.",detail:"{ value: string }"}],example:n`
			<os-swatch-grid label="Accent">
				<os-swatch value="red" preview="#ef4444" label="Red" selected></os-swatch>
				<os-swatch value="blue" preview="#3b82f6" label="Blue"></os-swatch>
				<os-swatch value="green" preview="#10b981" label="Green"></os-swatch>
			</os-swatch-grid>
		`};f("os-swatch",F);var xe=u`
	:host {
		display: inline-flex;
		max-width: 100%;
	}

	.os-tag-input {
		display: inline-flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var( --os-ui-tag-input-gap, 4px );
		padding: var( --os-ui-tag-input-padding, 2px );
		min-height: 24px;
		max-width: 100%;
	}

	.os-tag-input__chips {
		display: inline-flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var( --os-ui-tag-input-gap, 4px );
		min-width: 0;
	}

	/* The "+ Add" trigger is intentionally minimal — a single small
	 * button that doesn't compete with the chips. It expands into
	 * the inline input when clicked. */
	.os-tag-input__add {
		appearance: none;
		display: inline-flex;
		align-items: center;
		gap: 3px;
		padding: 1px 8px;
		min-height: 22px;
		font: inherit;
		font-size: 11px;
		font-weight: 500;
		line-height: 1;
		color: var( --os-ui-tag-input-add-fg, var( --os-ui-fg-muted, #50575e ) );
		background: transparent;
		border: 1px dashed var( --os-ui-tag-input-add-border, var( --os-ui-border, #c3c4c7 ) );
		border-radius: 999px;
		cursor: pointer;
		transition:
			background-color 0.12s ease,
			color 0.12s ease,
			border-color 0.12s ease;
	}
	.os-tag-input__add:hover:not( :disabled ) {
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.04 ) );
		color: var( --os-ui-tag-input-add-fg-hover, var( --os-ui-fg, #1d2327 ) );
		border-color: var( --os-ui-tag-input-add-border-hover, var( --os-ui-border-strong, #8c8f94 ) );
	}
	.os-tag-input__add:focus-visible {
		outline: none;
		border-style: solid;
		box-shadow: var(
			--os-ui-focus-ring,
			0 0 0 2px rgba( 12, 11, 15, 0.9 ), 0 0 0 4px #2271b1
		);
	}
	.os-tag-input__add:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}
	.os-tag-input__add svg {
		display: block;
	}

	/* Editor — the inline input + the floating suggestions popover. */
	.os-tag-input__editor {
		position: relative;
		display: inline-flex;
		align-items: center;
		flex: 0 1 auto;
		min-width: 120px;
	}

	.os-tag-input__input {
		appearance: none;
		font: inherit;
		font-size: 12px;
		line-height: 1.4;
		padding: 2px 8px;
		border: 1px solid var( --os-ui-tag-input-input-border, var( --os-ui-border, #2271b1 ) );
		border-radius: 999px;
		background: var( --os-ui-tag-input-input-bg, var( --os-ui-surface, #fff ) );
		color: var( --os-ui-tag-input-input-fg, var( --os-ui-fg, #1d2327 ) );
		min-width: 80px;
		max-width: 240px;
	}
	.os-tag-input__input:focus {
		outline: none;
		border-color: var( --os-ui-accent, #2271b1 );
		box-shadow: var(
			--os-ui-focus-ring-field,
			0 0 0 1px #2271b1, 0 0 0 4px rgba( 34, 113, 177, 0.18 )
		);
	}

	/* Popover — pinned to the editor, full width by default. Z-index
	 * is locked above os-table sticky-header (which sits at z-index
	 * ~3) but below the global toast layer (z-index ~10000). */
	.os-tag-input__suggestions {
		position: absolute;
		top: calc( 100% + 4px );
		left: 0;
		min-width: 220px;
		max-width: 320px;
		max-height: 240px;
		overflow-y: auto;
		padding: 4px 0;
		background: var( --os-ui-tag-input-pop-bg, var( --os-ui-surface, #fff ) );
		color: var( --os-ui-tag-input-pop-fg, var( --os-ui-fg, #1d2327 ) );
		border: 1px solid var( --os-ui-tag-input-pop-border, var( --os-ui-border, #c3c4c7 ) );
		border-radius: 8px;
		box-shadow:
			0 6px 16px rgba( 0, 0, 0, 0.08 ),
			0 1px 2px rgba( 0, 0, 0, 0.06 );
		z-index: 50;
	}

	.os-tag-input__suggestion-item {
		display: flex;
		align-items: center;
		gap: 6px;
		padding: 6px 12px;
		font-size: 13px;
		cursor: pointer;
		user-select: none;
	}
	.os-tag-input__suggestion-item[ aria-selected='true' ] {
		background: color-mix(
			in srgb,
			var( --wp-admin-theme-color, #2271b1 ) 10%,
			transparent
		);
		color: var( --wp-admin-theme-color, #2271b1 );
	}

	.os-tag-input__suggestion-create {
		font-style: italic;
		color: var( --os-ui-tag-input-create-fg, var( --os-ui-fg-muted, #50575e ) );
		border-top: 1px solid var( --os-ui-tag-input-pop-divider, var( --os-ui-border, #f0f0f1 ) );
	}
	.os-tag-input__suggestion-create[ aria-selected='true' ] {
		color: var( --wp-admin-theme-color, #2271b1 );
	}

	.os-tag-input__suggestion-empty,
	.os-tag-input__suggestion-loading {
		display: flex;
		align-items: center;
		gap: 8px;
		padding: 8px 12px;
		font-size: 12px;
		color: var( --os-ui-tag-input-pop-muted, var( --os-ui-fg-muted, #646970 ) );
	}

	.os-tag-input__suggestion-spinner {
		display: inline-block;
		width: 10px;
		height: 10px;
		border-radius: 50%;
		border: 2px solid currentColor;
		border-top-color: transparent;
		animation: os-tag-input-spin 0.8s linear infinite;
	}

	@keyframes os-tag-input-spin {
		to {
			transform: rotate( 360deg );
		}
	}

	:host( [ disabled ] ) .os-tag-input {
		opacity: 0.6;
		pointer-events: none;
	}
`;var ke=u`
	${$}

	:host {
		display: inline-flex;
		max-width: 100%;
		vertical-align: middle;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	.os-chip {
		display: inline-flex;
		align-items: center;
		gap: var( --os-ui-chip-gap, 4px );
		padding: var( --os-ui-chip-padding, 2px 8px );
		border-radius: var( --os-ui-chip-radius, 999px );
		font-size: var( --os-ui-chip-font-size, 12px );
		line-height: var( --os-ui-chip-line-height, 1.6 );
		font-weight: var( --os-ui-chip-font-weight, 500 );
		background: var( --os-ui-chip-bg, var( --os-ui-surface, #f0f0f1 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-fg, #1d2327 ) );
		border: var( --os-ui-chip-border, 1px solid transparent );
		max-width: 100%;
		box-sizing: border-box;
		transition:
			background-color 0.12s ease,
			color 0.12s ease,
			border-color 0.12s ease,
			transform 0.12s ease,
			opacity 0.12s ease;
	}

	/* Tones — same surface as <os-badge> for consistency. */
	:host( [ tone='accent' ] ) .os-chip {
		background: var(
			--os-ui-chip-bg,
			color-mix( in srgb, var( --wp-admin-theme-color, #2271b1 ) 14%, transparent )
		);
		color: var( --os-ui-chip-fg, var( --wp-admin-theme-color, #2271b1 ) );
	}
	:host( [ tone='positive' ] ) .os-chip {
		background: var( --os-ui-chip-bg, rgba( 30, 132, 73, 0.14 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-success-fg, #1d6f42 ) );
	}
	:host( [ tone='warning' ] ) .os-chip {
		background: var( --os-ui-chip-bg, rgba( 217, 119, 6, 0.18 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-warning-fg, #8a4a06 ) );
	}
	:host( [ tone='danger' ] ) .os-chip {
		background: var( --os-ui-chip-bg, rgba( 214, 54, 56, 0.14 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-danger-hover, #a02622 ) );
	}

	/* Pending shimmer — used by os-tag-input while a REST mutation
	 * is in flight. Subtle pulse so the user sees "this chip isn't
	 * settled yet" without alarming animation. */
	:host( [ pending ] ) .os-chip {
		opacity: 0.65;
		animation: os-chip-pulse 1.2s ease-in-out infinite;
	}

	@keyframes os-chip-pulse {
		0%, 100% { opacity: 0.55; }
		50%      { opacity: 0.95; }
	}

	.os-chip__label {
		max-width: var( --os-ui-chip-label-max, 220px );
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.os-chip__icon {
		display: inline-flex;
		align-items: center;
		flex-shrink: 0;
	}
	.os-chip__icon::slotted( * ) {
		display: inline-flex;
	}

	.os-chip__dismiss {
		appearance: none;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		width: 16px;
		height: 16px;
		margin-inline-start: 2px;
		padding: 0;
		border: 0;
		border-radius: 50%;
		background: transparent;
		color: inherit;
		cursor: pointer;
		opacity: 0.55;
		transition: opacity 0.12s ease, background-color 0.12s ease;
	}
	.os-chip__dismiss:hover,
	.os-chip__dismiss:focus-visible {
		opacity: 1;
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.12 ) );
		outline: none;
	}
	.os-chip__dismiss:focus-visible {
		box-shadow: var( --_holo-focus );
	}
	.os-chip__dismiss[ disabled ] {
		opacity: 0.35;
		cursor: not-allowed;
	}
	.os-chip__dismiss svg {
		display: block;
		width: 10px;
		height: 10px;
	}

	:host( [ disabled ] ) .os-chip {
		opacity: 0.55;
		cursor: not-allowed;
	}

	/* Compact density — half the horizontal padding. Used in dense
	 * lists like the posts-window Tags column. */
	:host( [ size='compact' ] ) .os-chip {
		padding: var( --os-ui-chip-padding, 1px 6px );
		font-size: var( --os-ui-chip-font-size, 11px );
	}

	/*
	 * The holographic hairline, and ONLY on a selected chip.
	 *
	 * Chips arrive in rows of eight and twelve — a tag column, a
	 * filter bar, a category picker — so the treatment here has to be
	 * the one that costs nothing when repeated. An edge on the one
	 * chip the user has chosen reads instantly in a row of otherwise
	 * flat pills; an edge on all of them reads as noise.
	 *
	 * Drawn on a mask-composited ::after rather than a border, because
	 * border-color takes a colour and this is a gradient. Same
	 * technique as .os-holo-edge; written out here because the chip's
	 * frame belongs to .os-chip, an inner element rather than the
	 * host, and the shared class hangs its ring on whatever carries
	 * the class.
	 */
	:host( [ selected ] ) .os-chip {
		position: relative;
		background: var( --os-ui-chip-bg, var( --os-ui-accent-soft, rgba( 34, 113, 177, 0.14 ) ) );
		color: var( --os-ui-chip-fg, var( --os-ui-fg, #1d2327 ) );
	}
	:host( [ selected ] ) .os-chip::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		padding: 1px;
		background-image: var( --_holo-edge );
		pointer-events: none;
		-webkit-mask: linear-gradient( #000 0 0 ) content-box,
			linear-gradient( #000 0 0 );
		-webkit-mask-composite: xor;
		mask: linear-gradient( #000 0 0 ) content-box, linear-gradient( #000 0 0 );
		mask-composite: exclude;
	}
`;var M=class extends g{constructor(){super(...arguments);this._onHostKeyDown=e=>{if(this.dismissible!==null&&(e.key==="Backspace"||e.key==="Delete")){if(e.preventDefault(),this.disabled!==null)return;let a=this.label??"";this.emit("os-chip-dismiss",{label:a})}}}connectedCallback(){super.connectedCallback(),this.addEventListener("keydown",this._onHostKeyDown)}disconnectedCallback(){this.removeEventListener("keydown",this._onHostKeyDown)}render(){let e=this.label??"",t=this.dismissible!==null,s=this.disabled!==null;return n`
			<span part="chip" class="os-chip">
				<span class="os-chip__icon">
					<slot name="icon"></slot>
				</span>
				<span class="os-chip__label">
					${e===""?n`<slot></slot>`:e}
				</span>
				${t?n`
							<button
								part="dismiss"
								class="os-chip__dismiss"
								type="button"
								aria-label=${`Remove ${e||"chip"}`}
								?disabled=${s}
								@click=${a=>this._onDismiss(a)}
							>
								${Ve()}
							</button>
					  `:n``}
			</span>
		`}_onDismiss(e){if(e.stopPropagation(),this.disabled!==null)return;let s=this.label??"";this.emit("os-chip-dismiss",{label:s})}};M.props=["label","tone","size","dismissible","disabled","pending","selected"],M.styles=[ke],M.help={title:"Chip",summary:"Labelled pill primitive with optional leading icon and trailing dismiss button. Tones mirror <os-badge>; pair with <os-tag-input> for full add/remove ergonomics.",status:"stable",props:[{name:"label",type:"string",description:"Visible text. Falls back to the default slot when omitted."},{name:"tone",type:"'neutral' | 'accent' | 'positive' | 'warning' | 'danger'",default:"neutral",description:"Color variant. Mirrors <os-badge> tones."},{name:"size",type:"'default' | 'compact'",default:"default",description:"Vertical density. Compact halves horizontal padding for dense lists."},{name:"dismissible",type:"boolean attribute",description:"Renders a trailing \xD7 button. Click / Enter / Space emits os-chip-dismiss."},{name:"disabled",type:"boolean attribute",description:"Visually mutes the chip and blocks the dismiss button. Useful while a parent is mid-update."},{name:"pending",type:"boolean attribute",description:"Renders a subtle pulse animation while a REST mutation is in flight. Auto-applied by <os-tag-input>; safe to set by hand."},{name:"selected",type:"boolean attribute",description:"Marks the chip as chosen: an accent wash plus the kit\u2019s iridescent hairline. Deliberately the only state that gets the hairline \u2014 chips arrive in rows of a dozen, and an edge on all of them is noise."}],slots:[{name:"(default)",description:"Fallback label when `label` is unset."},{name:"icon",description:"Leading icon (Dashicon, SVG, image). Inherits text color."}],parts:[{name:"chip",description:"The pill container."},{name:"dismiss",description:"The trailing \xD7 button (when `dismissible`)."}],events:[{name:"os-chip-dismiss",description:"Fires when the dismiss button is activated. Detail carries the chip's label so a delegated listener can act without DOM walking.",detail:"{ label: string }"}],cssProps:[{name:"--os-ui-chip-bg",description:"Background color."},{name:"--os-ui-chip-fg",description:"Text color."},{name:"--os-ui-chip-border",description:"Border shorthand."},{name:"--os-ui-chip-padding",description:"Padding shorthand.",default:"2px 8px"},{name:"--os-ui-chip-radius",description:"Corner radius.",default:"999px"},{name:"--os-ui-chip-label-max",description:"Max width of the inner label before ellipsis.",default:"220px"}],example:n`
			<os-cluster gap="6">
				<os-chip label="Neutral"></os-chip>
				<os-chip label="Accent" tone="accent"></os-chip>
				<os-chip label="Positive" tone="positive"></os-chip>
				<os-chip label="Warning" tone="warning"></os-chip>
				<os-chip label="Danger" tone="danger"></os-chip>
				<os-chip label="Dismissible" dismissible></os-chip>
			</os-cluster>
		`};f("os-chip",M);function Ve(){return n`
		<svg
			viewBox="0 0 12 12"
			width="10"
			height="10"
			aria-hidden="true"
			focusable="false"
			fill="none"
			stroke="currentColor"
			stroke-width="1.5"
			stroke-linecap="round"
		>
			<path d="M3 3 L9 9 M9 3 L3 9" />
		</svg>
	`}var R=class extends g{constructor(){super(...arguments);this._value=[];this._suggestions=[];this._suggestionsLoading=!1;this._query="";this._highlight=-1;this._focusedChip=-1;this._onDocumentPointerDown=e=>{!this.isOpen||e.composedPath().includes(this)||this.closeInput()}}get _input(){let e=this.shadowRoot;return e?e.querySelector(".os-tag-input__input"):null}get value(){return this._value}set value(e){this._value=Array.isArray(e)?e.slice():[],this._focusedChip>=this._value.length&&(this._focusedChip=-1),this.requestUpdate()}get suggestions(){return this._suggestions}set suggestions(e){this._suggestions=Array.isArray(e)?e.slice():[],this._highlight=this._suggestions.length>0?0:-1,this._suggestionsLoading=!1,this.requestUpdate()}get suggestionsLoading(){return this._suggestionsLoading}set suggestionsLoading(e){this._suggestionsLoading=!!e,this.requestUpdate()}get query(){return this._query}get isOpen(){return this.open!==null}openInput(){this.isOpen||(this.open="",this._query="",this._highlight=-1,this.emit("os-tag-open",{}),queueMicrotask(()=>{this._input?.focus(),this._emitSuggest("")}))}closeInput(){this.isOpen&&(this.open=null,this._query="",this._suggestions=[],this._highlight=-1,this._suggestionsLoading=!1,this.emit("os-tag-close",{}),this.requestUpdate())}connectedCallback(){super.connectedCallback(),document.addEventListener("pointerdown",this._onDocumentPointerDown,!0)}disconnectedCallback(){document.removeEventListener("pointerdown",this._onDocumentPointerDown,!0)}render(){let e=this.isOpen,t=this.disabled!==null,s=this.readonly!==null,a=this.removable!==null||this.removable===null&&!s,r=this.creatable!==null,d=this["add-label"]||"Add",l=this.placeholder||"Add a tag\u2026";return n`
			<span
				class="os-tag-input"
				role="group"
				aria-label=${this.label??""}
			>
				${this._renderChips(a,t)}
				${this._renderTrailing({isOpen:e,readonly:s,disabled:t,placeholder:l,creatable:r,addLabel:d})}
			</span>
		`}_renderTrailing(e){return e.isOpen?this._renderEditor(e.placeholder,e.creatable):e.readonly||e.disabled?n``:this._renderTrigger(e.addLabel)}_renderChips(e,t){let s=this._value;return s.length===0?n``:n`
			<span class="os-tag-input__chips" role="list">
				${s.map((a,r)=>{let d=a.tone??"neutral";return n`
						<os-chip
							role="listitem"
							size="compact"
							tone=${d}
							label=${a.label}
							?dismissible=${e&&!t}
							?disabled=${t}
							?pending=${!!a.pending}
							tabindex=${r===this._focusedChip?"0":"-1"}
							data-idx=${String(r)}
							@os-chip-dismiss=${l=>this._onChipDismiss(l,a)}
							@focus=${()=>this._focusedChip=r}
						></os-chip>
					`})}
			</span>
		`}_renderTrigger(e){let t=this.disabled!==null;return n`
			<button
				type="button"
				class="os-tag-input__add"
				aria-label=${e}
				aria-haspopup="listbox"
				aria-expanded="false"
				?disabled=${t}
				@click=${()=>this.openInput()}
			>
				${Ke()}
				<span>${e}</span>
			</button>
		`}_renderEditor(e,t){let s=this._suggestions.length>0||this._suggestionsLoading||t&&this._query.trim().length>0;return n`
			<span class="os-tag-input__editor">
				<input
					class="os-tag-input__input"
					type="text"
					autocomplete="off"
					autocapitalize="off"
					spellcheck="false"
					placeholder=${e}
					.value=${this._query}
					aria-autocomplete="list"
					aria-expanded=${s?"true":"false"}
					aria-activedescendant=${this._highlight>=0?`os-tag-suggestion-${this._highlight}`:""}
					@input=${a=>this._onInput(a)}
					@keydown=${a=>this._onInputKeyDown(a)}
					@blur=${a=>this._onInputBlur(a)}
				/>
				${s?this._renderSuggestions(t):n``}
			</span>
		`}_renderSuggestions(e){let t=this._query.trim(),s=this._suggestions,a=e&&t.length>0&&!s.some(r=>r.label.toLowerCase()===t.toLowerCase())&&!this._value.some(r=>r.label.toLowerCase()===t.toLowerCase());return n`
			<div
				class="os-tag-input__suggestions"
				role="listbox"
			>
				${this._suggestionsLoading?n`
							<div class="os-tag-input__suggestion-loading">
								<span class="os-tag-input__suggestion-spinner" aria-hidden="true"></span>
								<span>Searching…</span>
							</div>
					  `:n``}
				${s.length===0&&!this._suggestionsLoading&&!a?n`
							<div class="os-tag-input__suggestion-empty">
								${t.length>0?"No matches.":"Type to search."}
							</div>
					  `:n``}
				${s.map((r,d)=>{let l=d===this._highlight;return n`
						<div
							id=${`os-tag-suggestion-${d}`}
							role="option"
							aria-selected=${l?"true":"false"}
							class="os-tag-input__suggestion-item"
							@mousedown=${c=>{c.preventDefault(),this._addSuggestion(r,!1)}}
							@mouseenter=${()=>{this._highlight=d,this.requestUpdate()}}
						>
							<span>${r.label}</span>
						</div>
					`})}
				${a?n`
							<div
								id=${`os-tag-suggestion-${s.length}`}
								role="option"
								aria-selected=${this._highlight===s.length?"true":"false"}
								class="os-tag-input__suggestion-item os-tag-input__suggestion-create"
								@mousedown=${r=>{r.preventDefault(),this._addSuggestion({label:t},!0)}}
								@mouseenter=${()=>{this._highlight=s.length,this.requestUpdate()}}
							>
								Create "${t}"
							</div>
					  `:n``}
			</div>
		`}_onChipDismiss(e,t){e.stopPropagation(),this.emit("os-tag-remove",{tag:t})}_onInput(e){let t=e.target.value;this._query=t,this._emitSuggest(t)}_emitSuggest(e){let t=parseInt(this["min-query"]||"0",10)||0;if(e.length<t){this._suggestions=[],this._suggestionsLoading=!1,this.requestUpdate();return}this._suggestionsLoading=!0,this.requestUpdate(),this.emit("os-tag-suggest",{query:e})}_onInputKeyDown(e){let t=this.creatable!==null,s=this._suggestions,a=this._query.trim(),r=t&&a.length>0&&!s.some(l=>l.label.toLowerCase()===a.toLowerCase())&&!this._value.some(l=>l.label.toLowerCase()===a.toLowerCase()),d=s.length+(r?1:0);switch(e.key){case"ArrowDown":{if(d===0)return;e.preventDefault(),this._highlight=this._highlight+1>=d?0:this._highlight+1,this.requestUpdate();return}case"ArrowUp":{if(d===0)return;e.preventDefault(),this._highlight=this._highlight<=0?d-1:this._highlight-1,this.requestUpdate();return}case"Enter":{if(e.preventDefault(),this._highlight>=0&&this._highlight<s.length){this._addSuggestion(s[this._highlight],!1);return}if(this._highlight===s.length&&r){this._addSuggestion({label:a},!0);return}if(r&&a.length>0){this._addSuggestion({label:a},!0);return}return}case"Escape":{e.preventDefault(),this.closeInput();return}case"Backspace":{if(this._query===""&&this._value.length>0){e.preventDefault();let l=this._value.length-1;this._focusedChip===l?(this.emit("os-tag-remove",{tag:this._value[l]}),this._focusedChip=-1):(this._focusedChip=l,this.requestUpdate())}return}default:this._focusedChip!==-1&&(this._focusedChip=-1)}}_onInputBlur(e){queueMicrotask(()=>{this.shadowRoot?.activeElement||this.closeInput()})}_addSuggestion(e,t){if(this._value.some(a=>a.label.toLowerCase()===e.label.toLowerCase())){this._query="",this._highlight=-1,this._suggestions=[],this.requestUpdate(),this._input?.focus();return}this.emit("os-tag-add",{tag:e,isNew:t}),this._query="",this._highlight=-1,this._suggestions=[],this._suggestionsLoading=!1,this.requestUpdate(),queueMicrotask(()=>{this._input?.focus()})}};R.props=["label","placeholder","add-label","creatable","removable","disabled","readonly","size","min-query","open"],R.styles=[xe],R.help={title:"Tag input",summary:"Multi-tag picker with autocomplete and free-form creation. Purely presentational \u2014 emits os-tag-suggest / os-tag-add / os-tag-remove and lets the consumer drive REST + optimistic UI.",status:"stable",props:[{name:"label",type:"string",description:"Accessible label for the inline input."},{name:"placeholder",type:"string",description:"Native placeholder for the inline input.",default:"Add a tag\u2026"},{name:"add-label",type:"string",description:'Label of the trigger button. The button draws its own plus icon, so this is text only \u2014 passing "+ Add" puts two pluses on screen.',default:"Add"},{name:"creatable",type:"boolean attribute",description:"Allow Enter on a non-matching query to emit `os-tag-add` with `isNew: true`. Off by default \u2014 opt in for taxonomies the user is allowed to extend."},{name:"removable",type:"boolean attribute",description:"Show \xD7 on every chip and emit `os-tag-remove` on click. On by default; switch off for read-only views."},{name:"disabled",type:"boolean attribute",description:"Disables the entire control. Chips render but the trigger / input / dismiss buttons are inert."},{name:"readonly",type:"boolean attribute",description:'Hides the "+" trigger and chip \xD7 buttons. Same as setting `creatable=false` and `removable=false` together.'},{name:"size",type:"'default' | 'compact'",default:"default",description:"Density preset. Compact suits dense table cells."},{name:"min-query",type:"integer (string)",default:"0",description:"Minimum query length before `os-tag-suggest` fires. Set to 1 or 2 for taxonomies with thousands of terms."},{name:"open",type:"boolean attribute",description:"Two-way reflected: present while the inline input is showing. Setting it externally opens / closes the picker."}],events:[{name:"os-tag-suggest",description:"Fires when the user types in the input. Consumer fetches suggestions and assigns them back via `el.suggestions = [\u2026]`.",detail:"{ query: string }"},{name:"os-tag-add",description:"Fires when the user picks a suggestion or, with `creatable`, presses Enter on a free-form value. Consumer mutates `value`.",detail:"{ tag: OsTagItem; isNew: boolean }"},{name:"os-tag-remove",description:"Fires when \xD7 on a chip is activated. Consumer mutates `value`.",detail:"{ tag: OsTagItem }"},{name:"os-tag-open",description:"Fires when the inline input opens.",detail:"{}"},{name:"os-tag-close",description:"Fires when the inline input closes.",detail:"{}"}],cssProps:[{name:"--os-ui-tag-input-gap",description:"Gap between chips / between chips and trigger.",default:"4px"},{name:"--os-ui-tag-input-padding",description:"Padding around the chip row.",default:"2px"},{name:"--os-ui-tag-input-add-fg",description:'Foreground color of the "+ Add" trigger.'},{name:"--os-ui-tag-input-pop-bg",description:"Suggestions popover background."}],example:n`
			<os-tag-input
				label="Tags"
				placeholder="Add a tag…"
				creatable
			></os-tag-input>
		`};f("os-tag-input",R);function Ke(){return n`
		<svg
			viewBox="0 0 12 12"
			width="9"
			height="9"
			aria-hidden="true"
			focusable="false"
			fill="none"
			stroke="currentColor"
			stroke-width="2"
			stroke-linecap="round"
		>
			<path d="M6 2 L6 10 M2 6 L10 6" />
		</svg>
	`}var _e=u`
	${$}
	${V}

	/*
	 * Host is block-level flex so the field fills its parent cell
	 * (grid row col=N, flex container, plain block container). An
	 * inline-flex default would leave the native <input> at its
	 * intrinsic width while the host spans the full cell, which
	 * looks wrong inside a os-row.
	 */
	:host {
		display: flex;
		flex-direction: column;
		gap: 4px;
		font-size: 13px;
		color: var( --os-ui-fg, #1d2327 );
		min-width: 0;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	.os-text-field__label {
		font-size: 12px;
		color: var( --os-ui-fg-muted, #646970 );
	}

	.os-text-field__row {
		position: relative;
		display: flex;
		align-items: center;
		width: 100%;
	}

	input {
		appearance: none;
		-webkit-appearance: none;
		display: block;
		width: 100%;
		min-width: 0;
		box-sizing: border-box;
		padding: 7px 10px;
		background: var( --os-window-bg, #fff );
		border: 1px solid var( --os-ui-border, #dcdcde );
		border-radius: 6px;
		font: inherit;
		font-size: 13px;
		color: var( --os-ui-fg, #1d2327 );
	}

	/* Suffix slot for units / currency badges — rendered when the
	 * component has a suffix attribute. Inline-end anchored so RTL
	 * locales flip automatically via logical properties. */
	.os-text-field__suffix {
		position: absolute;
		inset-inline-end: 10px;
		top: 50%;
		transform: translateY( -50% );
		pointer-events: none;
		font-size: 12px;
		color: var( --os-ui-fg-muted, #646970 );
	}

	/* Reveal (show/hide) toggle — only rendered on password-type fields
	 * that carry the reveal attribute. Sits at the inline-end of the
	 * row; the input grows extra padding when the button is present so
	 * typed text doesn't slide under it. */
	.os-text-field__row--has-reveal input {
		padding-inline-end: 36px;
	}

	.os-text-field__reveal {
		position: absolute;
		inset-inline-end: 0;
		top: 0;
		bottom: 0;
		width: 34px;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 0;
		border: none;
		background: transparent;
		color: var( --os-ui-fg-muted, #646970 );
		cursor: pointer;
		border-radius: 0 6px 6px 0;
		transition: color 0.12s ease;
	}
	.os-text-field__reveal:hover {
		color: var( --os-ui-accent, #2271b1 );
	}
	.os-text-field__reveal:focus-visible {
		outline: none;
		color: var( --os-ui-accent, #2271b1 );
		/* Inset, because the button sits flush inside the field's own
		   border — a ring outside it would trace the field, not the
		   button, and read as the wrong thing having focus. */
		box-shadow: inset 0 0 0 2px var( --os-ui-accent, #2271b1 );
		border-radius: 0 6px 6px 0;
	}
	.os-text-field__reveal:disabled {
		opacity: 0.45;
		cursor: not-allowed;
	}

	/* CSS-only password mask. We render type="password" declarations
	 * as actual type="text" inputs so Chrome / Edge / Firefox password
	 * managers never recognise them as credentials (they were ignoring
	 * autocomplete="new-password" and still offering to save / update
	 * the password for fields that are really API keys). The dots are
	 * applied via -webkit-text-security (the original Webkit extension,
	 * Chromium / Safari support it; Firefox 119+ ships the standard
	 * text-security). Older Firefox versions fall back to the input's
	 * letter-spacing trick — wide enough that the user sees the value
	 * exists but the characters bunch into an unreadable run.
	 *
	 * The reveal toggle simply removes this class. */
	.os-text-field__input--masked {
		-webkit-text-security: disc;
		text-security: disc;
	}
	@supports not ( ( -webkit-text-security: disc ) or ( text-security: disc ) ) {
		.os-text-field__input--masked {
			font-family: text-security-disc, "password", monospace;
			letter-spacing: 0.2em;
		}
	}

	input:disabled {
		opacity: 0.55;
		cursor: not-allowed;
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.03 ) );
	}

	/*
	 * Invalid outranks the shared focus ring — see the note on
	 * :where() in holoField. An invalid field focuses in red, because
	 * the ring is the only thing on screen saying so at that moment.
	 */
	input[ aria-invalid='true' ],
	input[ aria-invalid='true' ]:hover:not( :disabled ) {
		border-color: var( --os-ui-danger, #d63638 );
	}
	input[ aria-invalid='true' ]:focus,
	input[ aria-invalid='true' ]:focus-visible {
		border-color: var( --os-ui-danger, #d63638 );
		box-shadow: 0 0 0 1px var( --os-ui-danger, #d63638 ),
			0 0 0 4px rgba( 214, 54, 56, 0.18 );
	}

	/* Hide the native spinner on number inputs — the suffix slot and
	 * the keypad (when present) already handle increment / decrement.
	 * Callers that need spinners can unset this by restyling. */
	input[ type='number' ]::-webkit-inner-spin-button,
	input[ type='number' ]::-webkit-outer-spin-button {
		-webkit-appearance: none;
		margin: 0;
	}
	input[ type='number' ] {
		-moz-appearance: textfield;
	}
`;var O=class extends g{constructor(){super(...arguments);this._revealed=!1}connectedCallback(){super.connectedCallback(),B(this)}render(){let e=this.label||"",t=this.value??"",s=this.placeholder||"",a=this.disabled!==null,r=this.readonly!==null,d=this.autocomplete,l=this.type||"text",c=l==="password",h=d||"off";c&&(!d||h==="off")&&(h="new-password");let v=this.maxlength,k=this.minlength,y=this.pattern||"",m=this.name||"",b=this.suffix||"",p=this.invalid!==null,w=this.reveal!==null,x=l==="password",C=x&&!(w&&this._revealed),A;x||w&&this._revealed?A="text":A=l;let H=w?"os-text-field__row os-text-field__row--has-reveal":"os-text-field__row",G=C?"os-text-field__input os-text-field__input--masked":"os-text-field__input",ie=`${this.id||"os-unnamed"}__input`;return n`
			${e?n`<label
						class="os-text-field__label"
						for=${ie}
					>${e}</label>`:n``}
			<span class=${H}>
				<input
					id=${ie}
					class=${G}
					type=${A}
					.value=${t}
					placeholder=${s}
					?disabled=${a}
					?readonly=${r}
					autocomplete=${h}
					maxlength=${v??""}
					minlength=${k??""}
					pattern=${y}
					name=${m}
					aria-invalid=${p?"true":"false"}
					aria-label=${e||""}
					@input=${U=>this._onInput(U)}
					@change=${U=>this._onChange(U)}
					@keydown=${U=>this._onKeyDown(U)}
				/>
				${b?n`<span class="os-text-field__suffix">${b}</span>`:n``}
				${w?this._renderRevealButton(a):n``}
			</span>
		`}_renderRevealButton(e){let t=this._revealed?"Hide":"Show";return n`
			<button
				type="button"
				class="os-text-field__reveal"
				aria-label=${t}
				aria-pressed=${this._revealed?"true":"false"}
				?disabled=${e}
				tabindex="0"
				@click=${()=>this._onToggleReveal()}
			>
				${this._revealed?je():We()}
			</button>
		`}_onToggleReveal(){this._revealed=!this._revealed,this.requestUpdate()}_onInput(e){let t=e.target;this.value=t.value,this.emit("os-input-change",{value:t.value})}_onChange(e){let t=e.target;this.emit("os-input-commit",{value:t.value})}_onKeyDown(e){if(e.key==="Enter"&&!e.shiftKey&&!e.altKey&&!e.metaKey){let t=e.target;this.emit("os-submit",{value:t.value})}}};O.props=["label","value","placeholder","disabled","readonly","autocomplete","type","maxlength","minlength","pattern","name","suffix","invalid","reveal"],O.styles=[_e],O.help={title:"Text field",summary:"Labelled text input primitive. Two-way reflects `value`, emits os-input-change per keystroke, os-input-commit on blur/change, and os-submit on Enter. Optional password reveal toggle.",status:"stable",props:[{name:"label",type:"string",description:"Visible label above the input."},{name:"value",type:"string",description:"Current input value; reflected two-way."},{name:"placeholder",type:"string",description:"Native placeholder string."},{name:"disabled",type:"boolean attribute",description:"Disables the native input."},{name:"readonly",type:"boolean attribute",description:"Marks the input readonly."},{name:"autocomplete",type:"string",default:"off",description:"Forwarded to the native input autocomplete attribute."},{name:"type",type:"string",default:"text",description:"Native input type (text, password, email, search, tel, url)."},{name:"maxlength",type:"integer (string)",description:"Native maxlength."},{name:"minlength",type:"integer (string)",description:"Native minlength."},{name:"pattern",type:"regex string",description:"Native validation pattern."},{name:"name",type:"string",description:"Forwarded to the native input for form submission."},{name:"suffix",type:"string",description:"Text rendered inside the right edge of the input row."},{name:"invalid",type:"boolean attribute",description:"Marks the field aria-invalid and applies the error style."},{name:"reveal",type:"boolean attribute",description:'On type="password" fields, adds an eye-icon toggle that flips the input between hidden and visible text.'}],events:[{name:"os-input-change",description:"Fires on every input keystroke.",detail:"{ value: string }"},{name:"os-input-commit",description:"Fires on the native change event (blur / Enter).",detail:"{ value: string }"},{name:"os-submit",description:"Fires when the user presses Enter (without Shift/Alt/Meta).",detail:"{ value: string }"}],cssProps:[{name:"--os-ui-fg",description:"Text colour."},{name:"--os-ui-fg-muted",description:"Label + suffix colour."},{name:"--os-ui-border",description:"Input outline."},{name:"--os-window-bg",description:"Input background."}],example:n`
			<os-stack gap="8">
				<os-text-field label="Note title" value="Untitled" placeholder="Name this note"></os-text-field>
				<os-text-field type="password" reveal label="API key"></os-text-field>
			</os-stack>
		`};f("os-text-field",O);function We(){return n`
		<svg
			viewBox="0 0 16 16"
			width="14"
			height="14"
			fill="none"
			stroke="currentColor"
			stroke-width="1.5"
			stroke-linecap="round"
			stroke-linejoin="round"
			aria-hidden="true"
			focusable="false"
		>
			<path d="M1 8C1 8 3.5 3 8 3s7 5 7 5-2.5 5-7 5S1 8 1 8z" />
			<circle cx="8" cy="8" r="2" />
		</svg>
	`}function je(){return n`
		<svg
			viewBox="0 0 16 16"
			width="14"
			height="14"
			fill="none"
			stroke="currentColor"
			stroke-width="1.5"
			stroke-linecap="round"
			stroke-linejoin="round"
			aria-hidden="true"
			focusable="false"
		>
			<path d="M1 8C1 8 3.5 3 8 3s7 5 7 5-2.5 5-7 5S1 8 1 8z" />
			<circle cx="8" cy="8" r="2" />
			<line x1="2" y1="2" x2="14" y2="14" />
		</svg>
	`}var Ce=u`
	${$}
	${V}

	:host {
		display: flex;
		flex-direction: column;
		gap: 4px;
		font-size: 13px;
		color: var( --os-ui-fg, #1d2327 );
		min-width: 0;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	.os-textarea__label {
		font-size: 12px;
		color: var( --os-ui-fg-muted, #646970 );
	}

	textarea {
		appearance: none;
		-webkit-appearance: none;
		display: block;
		width: 100%;
		min-width: 0;
		box-sizing: border-box;
		padding: 8px 10px;
		background: var( --os-window-bg, #fff );
		border: 1px solid var( --os-ui-border, #dcdcde );
		border-radius: 6px;
		font: inherit;
		font-size: 13px;
		line-height: 1.45;
		color: var( --os-ui-fg, #1d2327 );
		resize: vertical;
	}

	textarea:disabled {
		opacity: 0.55;
		cursor: not-allowed;
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.03 ) );
	}

	/* Outranks the shared focus ring — see the :where() note in holoField. */
	textarea[ aria-invalid='true' ],
	textarea[ aria-invalid='true' ]:hover:not( :disabled ) {
		border-color: var( --os-ui-danger, #d63638 );
	}
	textarea[ aria-invalid='true' ]:focus,
	textarea[ aria-invalid='true' ]:focus-visible {
		border-color: var( --os-ui-danger, #d63638 );
		box-shadow: 0 0 0 1px var( --os-ui-danger, #d63638 ),
			0 0 0 4px rgba( 214, 54, 56, 0.18 );
	}

	/* Auto-grow mode: hide native resize affordance — we manage rows. */
	:host( [ auto-grow ] ) textarea {
		resize: none;
		overflow: hidden;
	}
`;var q=class extends g{constructor(){super(...arguments);this._textareaEl=null}connectedCallback(){super.connectedCallback(),B(this)}render(){let e=this._attr("label")||"",t=this._attr("value")??"",s=this._attr("placeholder")||"",a=this._boolAttr("disabled"),r=this._boolAttr("readonly"),d=this._attr("aria-label")||e,l=this._attr("name")||"",c=Number(this._attr("rows"))||3,h=this._attr("maxlength"),v=this._attr("minlength"),k=this._boolAttr("invalid"),m=`${this.id||"os-unnamed"}__field`;return n`
			${e?n`<label class="os-textarea__label" for=${m}>${e}</label>`:n``}
			<textarea
				id=${m}
				part="textarea"
				.value=${t}
				placeholder=${s}
				?disabled=${a}
				?readonly=${r}
				rows=${c}
				maxlength=${h??""}
				minlength=${v??""}
				name=${l}
				aria-invalid=${k?"true":"false"}
				aria-label=${d||""}
				@input=${b=>this._onInput(b)}
				@change=${b=>this._onChange(b)}
				@keydown=${b=>this._onKeyDown(b)}
			></textarea>
		`}_attr(e){return this.getAttribute(e)}_boolAttr(e){return this.getAttribute(e)!==null}_onInput(e){let t=e.target;this._textareaEl=t,this.setAttribute("value",t.value),this.emit("os-input-change",{value:t.value}),this._boolAttr("auto-grow")&&this._autosize(t)}_onChange(e){let t=e.target;this.emit("os-input-commit",{value:t.value})}_onKeyDown(e){if(this._boolAttr("submit-on-enter")&&e.key==="Enter"&&!e.shiftKey&&!e.altKey&&!e.metaKey&&!e.ctrlKey){e.preventDefault();let t=e.target;this.emit("os-submit",{value:t.value})}}_autosize(e){let t=Number(this._attr("max-rows"))||8,s=window.getComputedStyle(e),a=parseFloat(s.fontSize)||13,r=s.lineHeight,d=r==="normal"?a*1.45:parseFloat(r)||a*1.45,l=parseFloat(s.paddingTop)||0,c=parseFloat(s.paddingBottom)||0,h=d*t+l+c;e.style.height="auto";let v=Math.min(e.scrollHeight,h);e.style.height=`${v}px`}refreshAutosize(){this._textareaEl&&this._boolAttr("auto-grow")&&this._autosize(this._textareaEl)}focusInput(){(this.shadowRoot??this).querySelector("textarea")?.focus()}clear(){this.setAttribute("value","");let t=(this.shadowRoot??this).querySelector("textarea");t&&(t.value="",this._boolAttr("auto-grow")&&this._autosize(t))}};q.props=["label","value","placeholder","disabled","readonly","ariaLabel","name","rows","maxlength","minlength","invalid","autoGrow","maxRows","submitOnEnter"],q.styles=[Ce],q.help={title:"Textarea",summary:"Multi-line text input. Same event shape as os-text-field. Optional auto-grow up to max-rows; optional submit-on-enter (Enter sends, Shift+Enter newlines).",status:"stable",props:[{name:"label",type:"string",description:"Visible label above the textarea."},{name:"value",type:"string",description:"Current value; reflected two-way."},{name:"placeholder",type:"string",description:"Native placeholder."},{name:"disabled",type:"boolean attribute"},{name:"readonly",type:"boolean attribute"},{name:"aria-label",type:"string",description:"Accessible label when no visible label is rendered."},{name:"name",type:"string",description:"Forwarded to native textarea for form submission."},{name:"rows",type:"integer (string)",default:"3",description:"Initial visible row count."},{name:"maxlength",type:"integer (string)"},{name:"minlength",type:"integer (string)"},{name:"invalid",type:"boolean attribute",description:"Sets aria-invalid + error styling."},{name:"auto-grow",type:"boolean attribute",description:"Grows up to max-rows as the user types."},{name:"max-rows",type:"integer (string)",default:"8"},{name:"submit-on-enter",type:"boolean attribute",description:"Enter fires os-submit; Shift+Enter inserts a newline."}],events:[{name:"os-input-change",description:"Fires on every keystroke.",detail:"{ value: string }"},{name:"os-input-commit",description:"Fires on blur / native change.",detail:"{ value: string }"},{name:"os-submit",description:"Fires on Enter (without Shift) when submit-on-enter is set.",detail:"{ value: string }"}],example:n`
			<os-textarea label="Message" rows="3" auto-grow max-rows="8" submit-on-enter></os-textarea>
		`};f("os-textarea",q);})();
(()=>{var wt=Object.defineProperty;var Nt=(t,n,s)=>n in t?wt(t,n,{enumerable:!0,configurable:!0,writable:!0,value:s}):t[n]=s;var Ee=(t,n,s)=>Nt(t,typeof n!="symbol"?n+"":n,s);var de="odd-app-odd-notes",Z=class extends Error{constructor(s,i,c={}){super(s);Ee(this,"status");Ee(this,"data");this.name="NotesApiError",this.status=i,this.data=c}};function Y(){let t=window.wp?.os;if(!t)throw new Error("ODD Notes requires the OpenStation public API.");return t}function ye(){let t=Y().getWindowConfig(de);if(!t?.restBase||!t.restNonce)throw new Error("Notes window config is missing.");return t}async function V(t,n={}){let s=ye(),i=new URL(t.replace(/^\//,""),s.restBase),c=await Y().fetch(i,{method:n.method??"GET",credentials:"same-origin",signal:n.signal,headers:{Accept:"application/json","X-WP-Nonce":s.restNonce,...n.body!==void 0?{"Content-Type":"application/json"}:{}},body:n.body!==void 0?JSON.stringify(n.body):void 0},{windowId:de,source:"odd-app-odd-notes",silent:n.silent});if(!c.ok){let y=`${c.status} ${c.statusText}`,L={};try{let g=await c.json();y=g.message||y,L=g.data??{}}catch{}throw new Z(y,c.status,L)}return await c.json()}function $e(t){return V("notes",{signal:t})}function Ue(t,n){return V("notes",{method:"POST",body:t,signal:n})}function je(t,n,s){return V(`notes/${t}`,{method:"POST",body:n,signal:s})}function Ke(t,n){return V(`notes/${t}`,{method:"DELETE",signal:n})}async function Be(t,n){return(await V(`notes/${t}/revisions`,{signal:n,silent:!0})).revisions}function Ye(t,n,s){return V(`notes/${t}/revisions/${n}/restore`,{method:"POST",signal:s})}var Lt="odd-app-odd-notes/drafts/v2";function Ve(t){let n=String(t.draftScope??"").trim();if(n)return`site:${n}`;try{let s=new URL(t.restBase);return s.hostname==="playground.wordpress.net"?null:`url:${s.origin}${s.pathname}`}catch{return null}}function Ge(t,n){return t?`${Lt}/${encodeURIComponent(t)}/${n}`:null}function le(){return{entries:{}}}function Tt(t){if(!t||typeof t!="object")return!1;let n=t;return(typeof n.id=="number"||typeof n.id=="string"&&n.id.startsWith("local-"))&&typeof n.title=="string"&&typeof n.body=="string"&&typeof n.color=="string"&&Array.isArray(n.tags)&&n.tags.every(i=>typeof i=="string")&&typeof n.favorite=="boolean"&&typeof n.archived=="boolean"&&typeof n.onDesktop=="boolean"&&typeof n.public=="boolean"&&typeof n.clientUpdatedAt=="number"&&Number.isFinite(n.clientUpdatedAt)}function ze(t,n){try{let s=Ge(t,n);if(!s)return le();let i=window.localStorage.getItem(s);if(!i)return le();let c=JSON.parse(i);if(!c||typeof c.entries!="object"||!c.entries)return le();let y={};for(let L of Object.values(c.entries))Tt(L)&&(y[String(L.id)]=L);return{entries:y}}catch{return le()}}function Xe(t,n,s){try{let i=Ge(t,n);if(!i)return;if(Object.keys(s.entries).length===0){window.localStorage.removeItem(i);return}window.localStorage.setItem(i,JSON.stringify(s))}catch{}}function St(t){return{id:t.id,title:t.title,body:t.body,color:t.color,tags:[...t.tags],favorite:t.favorite,archived:t.archived,onDesktop:t.onDesktop,public:t.public,version:t.version,updatedAtMs:t.updatedAtMs,clientUpdatedAt:Date.now()}}function G(t,n){return{entries:{...t.entries,[String(n.id)]:St(n)}}}function H(t,n){let s={...t.entries};return delete s[String(n)],{entries:s}}function qe(t,n,s){let i=t.entries[String(n)];if(!i)return t;let c={...t.entries};return delete c[String(n)],c[String(s)]={...i,id:s},{entries:c}}function Qe(t,n){return{...t,title:n.title,body:n.body,excerpt:n.body.trim().replace(/\s+/g," ").slice(0,150),color:n.color,tags:[...n.tags],favorite:n.favorite,archived:n.archived,onDesktop:n.onDesktop,public:n.public,updatedAt:new Date(n.clientUpdatedAt).toISOString(),updatedAtMs:n.updatedAtMs??t.updatedAtMs,version:n.version??t.version,wordCount:ee(n.body)}}function Ze(t,n){return t.title===n.title&&t.body===n.body&&t.color===n.color&&t.favorite===n.favorite&&t.archived===n.archived&&t.onDesktop===n.onDesktop&&t.public===n.public&&t.tags.length===n.tags.length&&t.tags.every((s,i)=>s===n.tags[i])}function ee(t){let n=t.trim().match(/\S+/g);return n?n.length:0}function T(t){return typeof t=="string"&&t.startsWith("local-")}function ce(t,n){let s=t.filter(i=>i.id!==n.id);return[n,...s].sort((i,c)=>Date.parse(c.updatedAt)-Date.parse(i.updatedAt))}function et(t,n){let s=t.findIndex(c=>c.id===n.id);if(s===-1)return ce(t,n);let i=[...t];return i[s]=n,i}function tt(t,n,s=Date.now()){let i={...t,...n,updatedAt:new Date(s).toISOString(),updatedAtMs:t.updatedAtMs,version:t.version};i.excerpt=i.body.trim().replace(/\s+/g," ").slice(0,150);let c=i.body.trim().match(/\S+/g);return i.wordCount=c?c.length:0,i}function we(t,n){return t.filter(s=>s.id!==n)}function Ne(t,n,s,i){let c=s.trim().toLocaleLowerCase(),y=c?c.split(/\s+/).map(g=>g.replace(/^#/,"")):[],L=i?.toLocaleLowerCase()??null;return t.filter(g=>{if((n==="archive"?!g.archived:g.archived)||n==="favorite"&&(!g.favorite||!g.canEdit)||n==="desktop"&&(!g.onDesktop||!g.canEdit)||n==="shared"&&g.canEdit||n==="all"&&!g.canEdit||L&&!g.tags.some(D=>D.toLocaleLowerCase()===L))return!1;if(!c)return!0;let ue=[g.title,g.body,g.ownerName,...g.tags].join(`
`).toLocaleLowerCase();return y.every(D=>ue.includes(D))})}function nt(t){let n={all:0,favorite:0,desktop:0,shared:0,archive:0};for(let s of t){if(s.archived){s.canEdit&&n.archive++;continue}if(!s.canEdit){n.shared++;continue}n.all++,s.favorite&&n.favorite++,s.onDesktop&&n.desktop++}return n}function Le(t){let n=new Map;for(let s of t)if(!(!s.canEdit||s.archived))for(let i of s.tags){let c=i.toLocaleLowerCase(),y=n.get(c);n.set(c,{label:y?.label??i,count:(y?.count??0)+1})}return[...n.values()].sort((s,i)=>s.label.localeCompare(i.label))}var At=650,Ct=120,ot={butter:"#fff3a6",blush:"#ffd9e0",sky:"#cde9ff",mint:"#d4f5dc",lilac:"#e6ddff",peach:"#ffe1c4"},Te=new Intl.RelativeTimeFormat(void 0,{numeric:"auto"}),Dt=new Intl.DateTimeFormat(void 0,{month:"short",day:"numeric"}),Mt=new Intl.DateTimeFormat(void 0,{month:"short",day:"numeric",year:"numeric"});function d(t){return window.wp?.i18n?.__(t,"odd-outlandish-desktop-decorator")??t}function F(t,...n){return window.wp?.i18n?.sprintf(t,...n)??t.replace(/%[sd]/g,()=>String(n.shift()??""))}function f(t,n){let s=t.querySelector(n);if(!s)throw new Error(`Notes template is missing ${n}.`);return s}function rt(t,n){t.value=n,t.setAttribute("value",n)}function Se(t){return String(t.detail?.value??"")}function it(t){return!!t.detail?.checked}function st(t,n,s){t.toggleAttribute("checked",n),t.toggleAttribute("disabled",s);let i=t.shadowRoot?.querySelector('input[type="checkbox"]');i&&(i.checked=n,i.disabled=s)}function kt(t,n){let s=new Date().toISOString();return{id:`local-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,title:d("Untitled note"),body:"",excerpt:"",color:n,tags:[],favorite:!1,archived:!1,onDesktop:!1,public:!1,ownerId:t,ownerName:d("You"),ownerAvatar:"",canEdit:!0,createdAt:s,updatedAt:s,updatedAtMs:Date.now(),version:0,wordCount:0}}function at(t){return{title:t.title,body:t.body,color:t.color,tags:[...t.tags],favorite:t.favorite,archived:t.archived,onDesktop:t.onDesktop,public:t.public,...t.version>0?{version:t.version}:{},...t.updatedAtMs>0?{updatedAtMs:t.updatedAtMs}:{}}}function Ae(t,n){return{...t,title:n.title,body:n.body,excerpt:n.body.trim().replace(/\s+/g," ").slice(0,150),color:n.color,tags:[...n.tags],favorite:n.favorite,archived:n.archived,onDesktop:n.onDesktop,public:n.public,wordCount:ee(n.body)}}function dt(t,n){let s=new Date(t.clientUpdatedAt).toISOString();return{id:t.id,title:t.title,body:t.body,excerpt:t.body.trim().replace(/\s+/g," ").slice(0,150),color:t.color,tags:[...t.tags],favorite:t.favorite,archived:t.archived,onDesktop:t.onDesktop,public:t.public,ownerId:n,ownerName:d("You"),ownerAvatar:"",canEdit:!0,createdAt:s,updatedAt:s,updatedAtMs:t.updatedAtMs??t.clientUpdatedAt,version:t.version??0,wordCount:ee(t.body)}}function Ce(t){let n=Date.parse(t);if(Number.isNaN(n))return"";let s=n-Date.now(),i=Math.abs(s);if(i<6e4)return d("just now");if(i<36e5)return Te.format(Math.round(s/6e4),"minute");if(i<864e5)return Te.format(Math.round(s/36e5),"hour");if(i<2592e6)return Te.format(Math.round(s/864e5),"day");let c=new Date(t);return(c.getFullYear()===new Date().getFullYear()?Dt:Mt).format(c)}async function xt(t,n){let s=ye(),i=f(t,"[data-notes-app]"),c=f(i,"[data-notes-list]"),y=f(i,"[data-notes-loading]"),L=f(i,"[data-notes-empty]"),g=f(i,"[data-notes-editor]"),ue=f(i,".os-notes-app__writing"),D=f(i,"[data-notes-title]"),fe=f(i,"[data-notes-body]"),J=f(i,"[data-notes-tags]"),De=f(i,"[data-notes-desktop]"),Me=f(i,"[data-notes-public]"),pe=f(i,"[data-notes-save-status]"),ke=f(i,"[data-notes-stats]"),te=f(i,"[data-notes-owner]"),ne=f(i,"[data-notes-toast]"),z=f(i,"[data-notes-history-panel]"),I=f(i,"[data-notes-history-list]"),lt=f(i,"[data-notes-list-title]"),ct=f(i,"[data-notes-list-summary]"),me=f(i,"[data-notes-tag-list]"),oe=f(i,"[data-notes-favorite]"),ge=f(i,"[data-notes-archive]"),xe=f(i,"[data-notes-history]"),_e=f(i,"[data-notes-delete]"),ut=f(i,"[data-notes-online-dot]"),ft=f(i,"[data-notes-online-label]"),He=Ve(s),m=[],v=null,W="all",A=null,X="",p=ze(He,s.userId),$=!1,q=null,Q=null,U=null,re=new Map,ie=new Set,b=new Set,R=new Map;await Promise.all([customElements.whenDefined("os-tag-input"),customElements.whenDefined("os-swatch")]);function se(e){return e===null?void 0:m.find(o=>o.id===e)}function w(){return se(v)}function k(e,o=""){pe.setAttribute("phase",e),o?pe.setAttribute("error",o):pe.removeAttribute("error")}function P(e,o=!1){ne.textContent=e,ne.hidden=!1,ne.toggleAttribute("data-error",o),q!==null&&window.clearTimeout(q),q=window.setTimeout(()=>{ne.hidden=!0},4200)}function ae(){U!==null&&(window.clearTimeout(U),U=null),Xe(He,s.userId,p)}function x(e=!0){if(e){ae();return}U!==null&&window.clearTimeout(U),U=window.setTimeout(ae,Ct)}function ve(){let e=window.navigator.onLine;ut.toggleAttribute("data-offline",!e),ft.textContent=d(e?"WordPress connected":"Offline \u2014 drafts safe here")}function C(){let e=nt(m);for(let[o,r]of Object.entries(e)){let a=i.querySelector(`[data-notes-count="${o}"]`);a&&(a.textContent=String(r))}i.querySelectorAll("[data-notes-filter]").forEach(o=>{o.classList.toggle("is-active",o.dataset.notesFilter===W&&A===null)}),me.replaceChildren();for(let o of Le(m)){let r=document.createElement("button");r.type="button",r.dataset.notesTag=o.label,r.classList.toggle("is-active",A?.toLocaleLowerCase()===o.label.toLocaleLowerCase());let a=document.createElement("span");a.textContent="#";let l=document.createElement("span");l.textContent=o.label;let u=document.createElement("small");u.textContent=String(o.count),r.append(a,l,u),me.appendChild(r)}}function S(){let e=Ne(m,W,X,A),o={all:d("All notes"),favorite:d("Favorites"),desktop:d("On desktop"),shared:d("Shared notes"),archive:d("Archive")};if(lt.textContent=A?`#${A}`:o[W],ct.textContent=F(d(X?"%d matching notes":"%d notes"),e.length),c.replaceChildren(),e.length===0){let r=document.createElement("div");r.className="os-notes-app__no-results";let a=document.createElement("span");a.className="dashicons dashicons-search",a.setAttribute("aria-hidden","true");let l=document.createElement("p");l.textContent=d(X||A?"No notes match this view.":"Nothing here yet."),r.append(a,l),c.appendChild(r);return}for(let r of e){let a=document.createElement("button");a.type="button",a.className="os-notes-app__note-card",a.dataset.noteId=String(r.id),a.dataset.noteColor=r.color,a.classList.toggle("is-selected",r.id===v),a.classList.toggle("is-local",T(r.id)),a.setAttribute("aria-pressed",r.id===v?"true":"false");let l=document.createElement("span");l.className="os-notes-app__note-marker",l.setAttribute("aria-hidden","true");let u=document.createElement("span");u.className="os-notes-app__note-card-body";let h=document.createElement("span");h.className="os-notes-app__note-card-top";let E=document.createElement("strong");if(E.textContent=r.title||d("Untitled note"),h.appendChild(E),r.favorite){let N=document.createElement("span");N.className="dashicons dashicons-star-filled",N.setAttribute("aria-label",d("Favorite")),h.appendChild(N)}let _=document.createElement("span");_.className="os-notes-app__note-excerpt",_.textContent=r.excerpt||d("A new note, waiting for words.");let B=document.createElement("span");B.className="os-notes-app__note-meta";let We=document.createElement("span");if(We.textContent=Ce(r.updatedAt),B.appendChild(We),r.canEdit){if(r.onDesktop){let N=document.createElement("span");N.className="os-notes-app__desktop-pill",N.textContent=r.public?d("Public"):d("Desktop"),B.appendChild(N)}}else{let N=document.createElement("span");N.className="os-notes-app__shared-pill",N.textContent=r.ownerName,B.appendChild(N)}for(let N of r.tags.slice(0,2)){let be=document.createElement("span");be.className="os-notes-app__tag-pill",be.textContent=`#${N}`,B.appendChild(be)}u.append(h,_,B),a.append(l,u),c.appendChild(a)}}function O(){let e=w();if(!e){L.hidden=!1,g.hidden=!0,z.hidden=!0;return}L.hidden=!0,g.hidden=!1,g.toggleAttribute("data-readonly",!e.canEdit),ue.dataset.notesPaperColor=e.color,rt(D,e.title),rt(fe,e.body),D.toggleAttribute("readonly",!e.canEdit),fe.toggleAttribute("readonly",!e.canEdit),J.value=e.tags.map(r=>({id:r,label:r})),J.toggleAttribute("readonly",!e.canEdit),st(De,e.onDesktop,!e.canEdit),st(Me,e.public,!e.canEdit||!e.onDesktop),oe.toggleAttribute("disabled",!e.canEdit),ge.toggleAttribute("disabled",!e.canEdit),xe.toggleAttribute("disabled",!e.canEdit||T(e.id)),_e.toggleAttribute("disabled",!e.canEdit),oe.setAttribute("title",e.favorite?d("Remove from favorites"):d("Add to favorites"));let o=oe.querySelector(".dashicons");if(o&&(o.className=e.favorite?"dashicons dashicons-star-filled":"dashicons dashicons-star-empty"),ge.setAttribute("title",e.archived?d("Return to notes"):d("Archive")),te.replaceChildren(),e.canEdit){let r=document.createElement("span");r.className="dashicons dashicons-cloud-saved",r.setAttribute("aria-hidden","true");let a=document.createElement("span");a.textContent=T(e.id)?d("Local draft"):F(d("Edited %s"),Ce(e.updatedAt)),te.append(r,a)}else{if(e.ownerAvatar){let a=document.createElement("img");a.src=e.ownerAvatar,a.alt="",te.appendChild(a)}let r=document.createElement("span");r.textContent=F(d("Shared by %s"),e.ownerName),te.appendChild(r)}ke.textContent=F(d("%d words \xB7 %d characters"),e.wordCount,e.body.length),i.querySelectorAll("[data-notes-colors] os-swatch").forEach(r=>{r.toggleAttribute("selected",r.getAttribute("value")===e.color)}),!b.has(String(e.id))&&!ie.has(String(e.id))&&k(T(e.id)?"pending":"idle")}function Ie(){Q!==null&&window.clearTimeout(Q),Q=window.setTimeout(()=>{C(),S()},120)}function j(e,o=!0){m=ce(m,e),o&&(C(),S(),O())}function M(e,o=!1){let r=w();if(!r?.canEdit)return;let a=tt(r,e);m=et(m,a),o&&(C(),S(),O());let l=String(a.id);R.set(l,(R.get(l)??0)+1),b.add(l),p=G(p,a),x(!1),k("pending"),ke.textContent=F(d("%d words \xB7 %d characters"),a.wordCount,a.body.length),Ie(),K(a.id)}function K(e,o=At){let r=String(e),a=re.get(r);a!==void 0&&window.clearTimeout(a),re.set(r,window.setTimeout(()=>{re.delete(r),Re(e)},o))}async function pt(e,o,r){let a=await Y().confirm({title:d("This note changed elsewhere"),message:d("Keep the words from this window? Choose \u201CUse WordPress copy\u201D to discard this local draft."),confirmLabel:d("Keep this draft"),cancelLabel:d("Use WordPress copy")});if(!$){if(a){let l=Ae(r,o);j(l),b.add(String(e)),p=G(p,l),x(),K(e,0);return}j(r),b.delete(String(e)),p=H(p,e),x(),k("saved")}}async function Re(e){let o=String(e);if(ie.has(o)||!b.has(o))return;let r=se(e);if(!r?.canEdit)return;if(!window.navigator.onLine){v===e&&k("pending");return}ie.add(o),b.delete(o);let a=R.get(o)??0;v===e&&k("saving");try{let l=T(e)?await Ue(at(r),n.signal):await je(e,at(r),n.signal);if($)return;let u=se(e),h=(R.get(o)??0)!==a;if(T(e)){m=we(m,e),v=v===e?l.id:v,p=qe(p,e,l.id),b.delete(o),R.set(String(l.id),R.get(o)??0),R.delete(o);let E=h&&u?Ae(l,u):l;j(E,!1),h?(b.add(String(l.id)),p=G(p,E)):p=H(p,l.id)}else if(h&&u){let E=Ae(l,u);j(E,!1),b.add(o),p=G(p,E)}else j(l,!1),p=H(p,e);x(),C(),S(),O(),!h&&v===l.id&&k("saved")}catch(l){if($||l instanceof DOMException&&l.name==="AbortError")return;b.add(o),l instanceof Z&&l.status===409&&l.data.current&&!T(e)?await pt(e,r,l.data.current):(v===e&&k("failed",l instanceof Error?l.message:d("Save failed")),P(l instanceof Error?l.message:d("The note could not be saved."),!0))}finally{ie.delete(o);let l=T(e)&&v!==e?v:e;l!==null&&b.has(String(l))&&K(l,160)}}function mt(e){v=e,z.hidden=!0,S(),O(),window.setTimeout(()=>{D.focusInput?.()},0)}function he(e){let o=kt(s.userId,e?.color??s.colors[0]??"butter");e&&(o.title=F(d("Copy of %s"),e.title),o.body=e.body,o.tags=[...e.tags],o.favorite=!1,o.wordCount=ee(o.body),o.excerpt=e.excerpt),m=ce(m,o),v=o.id,b.add(String(o.id)),p=G(p,o),x(),C(),S(),O(),K(o.id,120),window.setTimeout(()=>{D.focusInput?.()},0)}async function Pe(e=!1){n.markLoading(),y.hidden=!1;try{let o=await $e(n.signal);if($)return;let r=new Map(o.notes.map(u=>[String(u.id),u])),a=!1,l=o.notes.map(u=>{let h=String(u.id),E=p.entries[String(u.id)];return E?Ze(E,u)?(p=H(p,u.id),b.delete(h),a=!0,u):(b.add(h),Qe(u,E)):(b.delete(h),u)});for(let u of Object.values(p.entries))T(u.id)&&!r.has(String(u.id))?(l.push(dt(u,s.userId)),b.add(String(u.id))):r.has(String(u.id))||(p=H(p,u.id),b.delete(String(u.id)),a=!0);a&&x(),m=l,v!==null&&!se(v)&&(v=null),v===null&&(v=m.find(u=>u.canEdit&&!u.archived)?.id??null),C(),S(),O();for(let u of b){let h=m.find(E=>String(E.id)===u);h&&K(h.id,220)}e&&P(d("Notes refreshed from WordPress."))}catch(o){if(o instanceof DOMException&&o.name==="AbortError")return;if(P(o instanceof Error?o.message:d("Could not load notes."),!0),m.length===0){for(let r of Object.values(p.entries))m.push(dt(r,s.userId));C(),S()}}finally{y.hidden=!0,n.markReady()}}function gt(){let e=f(i,"[data-notes-colors]");e.replaceChildren();for(let o of s.colors){let r=document.createElement("os-swatch");r.setAttribute("value",o),r.setAttribute("label",o),r.setAttribute("size","small"),r.setAttribute("preview",ot[o]??ot.butter),e.appendChild(r)}}async function vt(){let e=w();if(!e?.canEdit||T(e.id))return;z.hidden=!1,I.replaceChildren();let o=document.createElement("div");o.className="os-notes-app__history-loading",o.textContent=d("Reading WordPress revisions\u2026"),I.appendChild(o);try{let r=await Be(e.id,n.signal);if($||v!==e.id)return;ht(e,r)}catch(r){I.textContent=r instanceof Error?r.message:d("Could not load revision history.")}}function ht(e,o){if(I.replaceChildren(),o.length===0){let r=document.createElement("p");r.className="os-notes-app__history-empty",r.textContent=d("No earlier versions yet. Keep writing\u2014WordPress will remember them."),I.appendChild(r);return}for(let r of o){let a=document.createElement("article");a.className="os-notes-app__revision";let l=document.createElement("div"),u=document.createElement("strong");u.textContent=r.title||d("Untitled note");let h=document.createElement("time");h.dateTime=r.createdAt,h.textContent=Ce(r.createdAt),l.append(u,h);let E=document.createElement("p");E.textContent=r.body.trim().replace(/\s+/g," ").slice(0,220)||d("Empty note");let _=document.createElement("os-button");_.setAttribute("variant","secondary"),_.dataset.revisionId=String(r.id),_.textContent=d("Restore this version"),a.append(l,E,_),I.appendChild(a)}I.onclick=r=>{let a=r.target.closest("[data-revision-id]");a&&bt(e.id,Number(a.dataset.revisionId))}}async function bt(e,o){if(await Y().confirm({title:d("Restore this version?"),message:d("Your current words will remain available as another WordPress revision."),confirmLabel:d("Restore version")}))try{let a=await Ye(e,o,n.signal);j(a),p=H(p,e),x(),z.hidden=!0,P(d("Earlier version restored."))}catch(a){P(a instanceof Error?a.message:d("Restore failed."),!0)}}async function Et(){let e=w();if(!(!e?.canEdit||!await Y().confirm({title:d("Move note to Trash?"),message:d("You can restore it later from OpenStation Trash."),confirmLabel:d("Move to Trash"),cancelLabel:d("Keep note"),danger:!0})))try{T(e.id)||await Ke(e.id,n.signal),m=we(m,e.id),p=H(p,e.id),x(),v=Ne(m,W,X,A)[0]?.id??null,C(),S(),O(),P(d("Note moved to Trash."))}catch(r){P(r instanceof Error?r.message:d("Delete failed."),!0)}}function yt(){i.querySelectorAll("[data-notes-new], [data-notes-empty-new]").forEach(e=>e.addEventListener("click",()=>he())),f(i,"[data-notes-refresh]").addEventListener("click",()=>void Pe(!0)),f(i,"[data-notes-search]").addEventListener("os-input-change",e=>{X=Se(e),Ie()}),i.querySelectorAll("[data-notes-filter]").forEach(e=>{e.addEventListener("click",()=>{W=e.dataset.notesFilter??"all",A=null,C(),S()})}),me.addEventListener("click",e=>{let o=e.target.closest("[data-notes-tag]");o&&(A=A===o.dataset.notesTag?null:o.dataset.notesTag??null,W="all",C(),S())}),c.addEventListener("click",e=>{let o=e.target.closest("[data-note-id]"),r=m.find(a=>String(a.id)===o?.dataset.noteId);r&&mt(r.id)}),D.addEventListener("os-input-change",e=>{M({title:Se(e)||d("Untitled note")})}),fe.addEventListener("os-input-change",e=>{M({body:Se(e)})}),J.addEventListener("os-tag-suggest",e=>{let o=String(e.detail?.query??"").toLocaleLowerCase(),r=new Set(w()?.tags.map(a=>a.toLocaleLowerCase()));J.suggestions=Le(m).filter(a=>!r.has(a.label.toLocaleLowerCase())&&a.label.toLocaleLowerCase().includes(o)).slice(0,8).map(a=>({id:a.label,label:a.label}))}),J.addEventListener("os-tag-add",e=>{let o=String(e.detail?.tag?.label??"").trim(),r=w();!r||!o||r.tags.some(a=>a.toLocaleLowerCase()===o.toLocaleLowerCase())||M({tags:[...r.tags,o].slice(0,12)},!0)}),J.addEventListener("os-tag-remove",e=>{let o=String(e.detail?.tag?.label??""),r=w();r&&M({tags:r.tags.filter(a=>a!==o)},!0)}),f(i,"[data-notes-colors]").addEventListener("os-pick",e=>{let o=String(e.detail?.value??"butter");M({color:o},!0)}),De.addEventListener("os-checkbox-change",e=>{let o=it(e);M({onDesktop:o,public:o?w()?.public??!1:!1},!0)}),Me.addEventListener("os-checkbox-change",e=>{let o=it(e);M({public:o,onDesktop:o||w()?.onDesktop===!0},!0)}),oe.addEventListener("click",()=>{let e=w();e?.canEdit&&M({favorite:!e.favorite},!0)}),f(i,"[data-notes-duplicate]").addEventListener("click",()=>{let e=w();e&&he(e)}),ge.addEventListener("click",()=>{let e=w();e?.canEdit&&M({archived:!e.archived,onDesktop:e.archived?e.onDesktop:!1,public:e.archived?e.public:!1},!0)}),_e.addEventListener("click",()=>void Et()),xe.addEventListener("click",()=>void vt()),f(i,"[data-notes-history-close]").addEventListener("click",()=>{z.hidden=!0}),i.addEventListener("keydown",e=>{let o=e;if((o.metaKey||o.ctrlKey)&&o.key.toLocaleLowerCase()==="s"){o.preventDefault();let r=w();r&&Re(r.id)}if((o.metaKey||o.ctrlKey)&&o.key.toLocaleLowerCase()==="n"&&(o.preventDefault(),he()),(o.metaKey||o.ctrlKey)&&o.shiftKey&&o.key.toLocaleLowerCase()==="f"){o.preventDefault();let r=f(i,"[data-notes-search]"),a=r.shadowRoot?.querySelector("input");a?a.focus():r.focusInput?.()}o.stopPropagation()})}function Oe(){ve();for(let e of b){let o=m.find(r=>String(r.id)===e);o&&K(o.id,0)}}function Fe(){ve(),k("pending")}function Je(){ae()}return gt(),yt(),ve(),window.addEventListener("online",Oe),window.addEventListener("offline",Fe),window.addEventListener("pagehide",Je),await Pe(),()=>{ae(),$=!0;for(let e of re.values())window.clearTimeout(e);q!==null&&window.clearTimeout(q),Q!==null&&window.clearTimeout(Q),window.removeEventListener("online",Oe),window.removeEventListener("offline",Fe),window.removeEventListener("pagehide",Je)}}window.openStationNativeWindows=window.openStationNativeWindows??{};window.openStationNativeWindows[de]=xt;})();
