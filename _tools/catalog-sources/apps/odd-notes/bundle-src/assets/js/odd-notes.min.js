var js=Object.defineProperty;var Gs=(h,j,le)=>j in h?js(h,j,{enumerable:!0,configurable:!0,writable:!0,value:le}):h[j]=le;var lt=(h,j,le)=>Gs(h,typeof j!="symbol"?j+"":j,le);(function(){"use strict";function h(s,...e){return{__wpdHtml:!0,strings:s,values:e}}function j(s){return!!s&&s.__wpdHtml===!0}const le="$$wpd$$",z=/\$\$wpd\$\$(\d+)\$\$/g;function Ht(s){let e=s[0];for(let t=1;t<s.length;t++)e+=`${le}${t-1}$$`+s[t];return e}const dt=new WeakMap;function ct(s){const e=dt.get(s);if(e)return e;const t=document.createElement("template");t.innerHTML=Ht(s);const o=[],a=(g,b)=>{if(g.nodeType===Node.ELEMENT_NODE){const x=g;for(const N of Array.from(x.attributes)){const k=N.name,v=N.value,P=k[0];if(z.test(v))if(z.lastIndex=0,P==="@"){const w=z.exec(v);z.lastIndex=0,o.push({path:b,kind:"event",name:k.slice(1),valueIndex:w?Number(w[1]):0}),x.removeAttribute(k)}else if(P==="."){const w=z.exec(v);z.lastIndex=0,o.push({path:b,kind:"prop",name:k.slice(1),valueIndex:w?Number(w[1]):0}),x.removeAttribute(k)}else if(P==="?"){const w=z.exec(v);z.lastIndex=0,o.push({path:b,kind:"bool",name:k.slice(1),valueIndex:w?Number(w[1]):0}),x.removeAttribute(k)}else{const w=[],L=[];let A=0,_;for(z.lastIndex=0;(_=z.exec(v))!==null;)w.push(v.slice(A,_.index)),L.push(Number(_[1])),A=_.index+_[0].length;w.push(v.slice(A)),o.push({path:b,kind:"attr",name:k,template:w,valueIndices:L}),x.setAttribute(k,"")}}}const f=Array.from(g.childNodes);let $=0;for(let x=0;x<f.length;x++){const N=f[x],k=x+$;if(N.nodeType===Node.TEXT_NODE){const v=N.textContent||"";if(!z.test(v)){z.lastIndex=0;continue}z.lastIndex=0;const P=N.parentNode;let w=0,L;const A=[],_=[];for(z.lastIndex=0;(L=z.exec(v))!==null;){L.index>w&&A.push(document.createTextNode(v.slice(w,L.index)));const B=document.createTextNode("");A.push(B),_.push({path:[...b,k+A.length-1],kind:"node",valueIndex:Number(L[1])}),w=L.index+L[0].length}w<v.length&&A.push(document.createTextNode(v.slice(w)));for(const B of A)P.insertBefore(B,N);P.removeChild(N),$+=A.length-1,o.push(..._)}else a(N,[...b,k])}};a(t.content,[]);const d={template:t,buildParts:g=>{const b=[];for(const f of o){let $=g;for(const x of f.path)$=$.childNodes[x];f.kind==="node"?b.push({kind:"node",valueIndex:f.valueIndex,child:{anchor:$,state:null}}):f.kind==="attr"?b.push({kind:"attr",element:$,name:f.name,template:f.template,valueIndices:f.valueIndices}):f.kind==="event"?b.push({kind:"event",valueIndex:f.valueIndex,element:$,name:f.name}):f.kind==="prop"?b.push({kind:"prop",valueIndex:f.valueIndex,element:$,name:f.name}):f.kind==="bool"&&b.push({kind:"bool",valueIndex:f.valueIndex,element:$,name:f.name})}return b}};return dt.set(s,d),d}const ut=new WeakMap;function Kt(s,e){for(const t of s.nodes)if(t.parentNode!==e)return!1;return!0}function Vt(s,e){const t=ut.get(e);if(t&&t.strings===s.strings&&Kt(t,e)){ze(t.parts,s.values);return}const o=ct(s.strings),a=o.template.content.cloneNode(!0),c=o.buildParts(a),d=Array.from(a.childNodes);for(;e.firstChild;)e.removeChild(e.firstChild);e.appendChild(a),ze(c,s.values),ut.set(e,{strings:s.strings,parts:c,nodes:d})}function ze(s,e){for(const t of s)if(t.kind==="node")Ue(t.child,e[t.valueIndex]);else if(t.kind==="attr"){let o=t.template[0];for(let a=0;a<t.valueIndices.length;a++)o+=ht(e[t.valueIndices[a]]),o+=t.template[a+1];o!==t.last&&(t.last=o,o===""?t.element.removeAttribute(t.name):t.element.setAttribute(t.name,o))}else if(t.kind==="event"){const o=e[t.valueIndex];o!==t.current&&(t.current&&t.element.removeEventListener(t.name,t.current),o&&t.element.addEventListener(t.name,o),t.current=o)}else if(t.kind==="prop"){const o=e[t.valueIndex];o!==t.last&&(t.last=o,t.element[t.name]=o)}else if(t.kind==="bool"){const o=!!e[t.valueIndex];o!==t.last&&(t.last=o,o?t.element.setAttribute(t.name,""):t.element.removeAttribute(t.name))}}function Ue(s,e){if(e==null||e===!1){s.state&&(ee(s.state),s.state=null);return}if(Array.isArray(e)){Xt(s,e);return}if(j(e)){Yt(s,e);return}if(e instanceof Node){jt(s,e);return}Gt(s,ht(e))}function jt(s,e){const t=s.state;t?.shape==="node"&&t.node===e||(t&&ee(t),Ie(s,[e]),s.state={shape:"node",node:e})}function Gt(s,e){const t=s.state;if(t?.shape==="text"){t.text!==e&&(t.node.textContent=e,t.text=e);return}t&&ee(t);const o=document.createTextNode(e);Ie(s,[o]),s.state={shape:"text",node:o,text:e}}function Yt(s,e){const t=s.state;if(t?.shape==="template"&&t.strings===e.strings){ze(t.parts,e.values);return}t&&ee(t);const o=ct(e.strings),a=o.template.content.cloneNode(!0),c=o.buildParts(a),d=Array.from(a.childNodes);Ie(s,[a]),ze(c,e.values),s.state={shape:"template",strings:e.strings,parts:c,nodes:d}}function Xt(s,e){const t=s.state;if(t?.shape==="array"&&t.entries.length===e.length){for(let a=0;a<e.length;a++)Ue(t.entries[a],e[a]);return}t&&ee(t);const o=[];for(const a of e){const c=document.createTextNode("");Ie(s,[c]);const d={anchor:c,state:null};Ue(d,a),o.push(d)}s.state={shape:"array",entries:o}}function Ie(s,e){const t=s.anchor.parentNode;if(t)for(const o of e)t.insertBefore(o,s.anchor)}function ee(s){if(s.shape==="text"){s.node.remove();return}if(s.shape==="template"){for(const e of s.parts)e.kind==="node"&&e.child.state&&(ee(e.child.state),e.child.state=null);for(const e of s.nodes)e.parentNode&&e.parentNode.removeChild(e);return}if(s.shape==="node"){s.node.parentNode&&s.node.parentNode.removeChild(s.node);return}for(const e of s.entries)e.state&&ee(e.state),e.anchor.remove()}function ht(s){return s==null||s===!1?"":String(s)}const J=class J extends HTMLElement{constructor(){super(),this._renderScheduled=!1,this._propValues={},this.constructor.shadow?(this.attachShadow({mode:"open"}),this._renderRoot=this.shadowRoot):this._renderRoot=this,this._installPropAccessors()}static get observedAttributes(){return this.props.map(pt)}connectedCallback(){this._adoptStyles(),this.requestUpdate()}attributeChangedCallback(e,t,o){if(t===o)return;const a=Jt(e);this._propValues[a]=o,this.requestUpdate()}get classNames(){return Array.from(this.classList)}set classNames(e){if(e==null){this.removeAttribute("class");return}const o=(Array.isArray(e)?e:String(e).split(/\s+/)).map(a=>String(a).trim()).filter(a=>a!=="");this.className=o.join(" ")}requestUpdate(){this._scheduleRender()}emit(e,t){return this.dispatchEvent(new CustomEvent(e,{detail:t,bubbles:!0,composed:!0}))}_installPropAccessors(){const e=this.constructor;for(const t of e.props){if(Object.getOwnPropertyDescriptor(this,t))continue;const o=pt(t);Object.defineProperty(this,t,{get:()=>t in this._propValues?this._propValues[t]:this.getAttribute(o),set:a=>{let c;a==null||a===!1?c=null:a===!0?c="":c=String(a),this._propValues[t]=c,c===null?this.removeAttribute(o):this.setAttribute(o,c),this.requestUpdate()},enumerable:!0,configurable:!0})}}_scheduleRender(){this._renderScheduled||!this.isConnected||(this._renderScheduled=!0,queueMicrotask(()=>{this._renderScheduled=!1,this.isConnected&&Vt(this.render(),this._renderRoot)}))}_adoptStyles(){const e=this.constructor;if(e.styles.length!==0)if(e.shadow&&this.shadowRoot){const t=e.styles.map(o=>o.sheet).filter(o=>o!==null);if(this.shadowRoot.adoptedStyleSheets=t,t.length!==e.styles.length){for(const o of e.styles)if(!o.sheet){const a=document.createElement("style");a.textContent=o.cssText,this.shadowRoot.appendChild(a)}}}else this._adoptLightStyles(e)}_adoptLightStyles(e){if(!J._lightStylesAdopted.has(e)){J._lightStylesAdopted.add(e);for(const t of e.styles){const o=document.createElement("style");o.dataset.osUi=this.tagName.toLowerCase(),o.textContent=t.cssText,document.head.appendChild(o)}}}};J.props=[],J.styles=[],J.shadow=!0,J._lightStylesAdopted=new WeakSet;let U=J;function K(s,e){customElements.get(s)||customElements.define(s,e)}function pt(s){return s.replace(/[A-Z]/g,e=>"-"+e.toLowerCase())}function Jt(s){return s.replace(/-([a-z])/g,(e,t)=>t.toUpperCase())}const Qt=(()=>{try{return typeof new CSSStyleSheet().replaceSync=="function"}catch{return!1}})();function C(s,...e){let t=s[0];for(let o=1;o<s.length;o++){const a=e[o-1];if(typeof a=="string"||typeof a=="number")t+=String(a);else if(a&&a.__wpdCss)t+=a.cssText;else throw new TypeError("[os-ui] css`` interpolations must be strings, numbers, or other css`` results. Got: "+typeof a);t+=s[o]}if(Qt){const o=new CSSStyleSheet;return o.replaceSync(t),{__wpdCss:!0,sheet:o,cssText:t}}return{__wpdCss:!0,sheet:null,cssText:t}}function Zt(s){const e=[],t=[];let o=null,a=s.parentElement;for(;a&&!(a===document.body||a===document.documentElement);){const d=a.id||"";if(d.startsWith("wp-window-")){o=d.slice(10);break}if(a.tagName.toLowerCase()==="os-tabpanel"){const g=a.getAttribute("for");g&&t.unshift(g)}a=a.parentElement}o&&e.push(Oe(o));for(const d of t)e.push("tab-"+Oe(d));const c=s.getAttribute("label");return c&&e.push(Oe(c)),e.length===0?"os-unnamed":"os-"+e.filter(d=>d!=="").join("-")}function Oe(s){return s.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")}function gt(s){if(s.id)return s.id;const e=Zt(s);return s.id=e,e}const te=C`
	:host {
		--_holo-fill: var( --os-ui-holo-fill, ${"linear-gradient( 124deg, #afa2e8 0%, #f5a8ea 46%, #8ee9f7 100% )"} );
		--_holo-ink: var( --os-ui-holo-ink, #0c0b0f );
		--_holo-sheen: var(
			--os-ui-holo-sheen,
			linear-gradient(
				124deg,
				rgba( 159, 214, 255, 0.1 ) 0%,
				rgba( 236, 155, 255, 0.12 ) 34%,
				rgba( 242, 82, 252, 0.1 ) 58%,
				rgba( 147, 240, 198, 0.09 ) 100%
			)
		);
		--_holo-edge: var(
			--os-ui-holo-edge,
			linear-gradient(
				124deg,
				rgba( 154, 242, 255, 0.7 ) 0%,
				rgba( 236, 155, 255, 0.85 ) 38%,
				rgba( 242, 82, 252, 0.7 ) 62%,
				rgba( 159, 152, 255, 0.6 ) 100%
			)
		);
		--_holo-edge-quiet: var(
			--os-ui-holo-edge-quiet,
			linear-gradient(
				124deg,
				rgba( 154, 242, 255, 0.22 ) 0%,
				rgba( 236, 155, 255, 0.28 ) 38%,
				rgba( 242, 82, 252, 0.22 ) 62%,
				rgba( 159, 152, 255, 0.2 ) 100%
			)
		);
		--_holo-glow: var(
			--os-ui-holo-glow,
			0 0 0 1px rgba( 242, 82, 252, 0.28 ), 0 2px 10px rgba( 242, 82, 252, 0.22 )
		);
		--_holo-glow-strong: var(
			--os-ui-holo-glow-strong,
			0 0 0 1px rgba( 242, 82, 252, 0.42 ), 0 4px 18px rgba( 242, 82, 252, 0.38 ),
				0 1px 3px rgba( 12, 11, 15, 0.6 )
		);
		--_holo-track: var( --os-ui-holo-track, rgba( 255, 251, 255, 0.16 ) );
		--_holo-track-edge: var( --os-ui-holo-track-edge, #8c8f94 );
		/*
		 * The press ring's colour. The DIM Pulse, not Pulse: this one
		 * expands to ten pixels past the control and is on screen for
		 * a third of a second, which is exactly the "spread rather
		 * than stated" case the dim exists for.
		 */
		--_holo-ring-color: var( --os-ui-accent-dim, #2271b1 );
		/*
		 * One focus ring for the whole kit. Three layers, and each
		 * earns its place: a Void spacer so the ring never touches the
		 * control it is describing, the Pulse ring itself, and a soft
		 * bloom so the ring survives landing on a bright mesh — where a
		 * flat 2 px line would simply vanish into the pink.
		 */
		--_holo-focus: var(
			--os-ui-focus-ring,
			0 0 0 2px rgba( 12, 11, 15, 0.9 ), 0 0 0 4px #f252fc,
				0 0 12px 2px rgba( 242, 82, 252, 0.45 )
		);
		/*
		 * The field ring. A text input already has a border to
		 * thicken and it lives in a column of siblings, so the target
		 * ring above — built to survive a bright mesh — reads as an
		 * alarm on a settings form. This one tightens the field's own
		 * edge to Pulse and adds a soft halo outside it.
		 */
		--_holo-focus-field: var(
			--os-ui-focus-ring-field,
			0 0 0 1px #f252fc, 0 0 0 4px rgba( 242, 82, 252, 0.18 )
		);
		--_holo-t: var( --os-ui-holo-transition, 220ms );
		--_holo-t-fast: var( --os-ui-motion-fast, 140ms );
		--_holo-t-slow: var( --os-ui-motion-slow, 340ms );
		--_holo-t-ambient: var( --os-ui-motion-ambient, 12s );
		--_holo-spring: var(
			--os-ui-ease-spring,
			cubic-bezier( 0.32, 1.5, 0.55, 1 )
		);
		--_holo-ease: var( --os-ui-ease-out, cubic-bezier( 0.22, 0.9, 0.28, 1 ) );
		--_holo-loop: var( --os-ui-ease-loop, cubic-bezier( 0.45, 0, 0.55, 1 ) );
	}
`,es=C`
	.os-holo-fill {
		background-color: transparent;
		background-image: var( --_holo-fill );
		background-size: 220% 220%;
		background-position: 22% 28%;
		background-repeat: no-repeat;
		color: var( --_holo-ink );
		transition: background-position var( --_holo-t ) ease,
			box-shadow var( --_holo-t ) ease, filter var( --_holo-t ) ease;
	}

	.os-holo-fill:hover {
		background-position: 74% 66%;
	}

	.os-holo-fill:active {
		background-position: 88% 82%;
		filter: brightness( 0.94 );
	}

	/*
	 * The tilt is the whole effect, so when motion is unwelcome the
	 * mesh simply stops moving — it does not stop being a mesh. A
	 * control that lost its fill under reduced-motion would lose its
	 * state, not just its animation.
	 */
	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-fill {
			transition-duration: 1ms;
		}

		.os-holo-fill:hover,
		.os-holo-fill:active {
			background-position: 22% 28%;
		}
	}
`,ft=C`
	.os-holo-sheen {
		position: relative;
		isolation: isolate;
	}

	.os-holo-sheen::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: -1;
		border-radius: inherit;
		background-image: var( --_holo-sheen );
		background-size: 200% 200%;
		background-position: 20% 30%;
		opacity: 0;
		pointer-events: none;
		transition: opacity var( --_holo-t ) ease,
			background-position var( --_holo-t ) ease;
	}

	.os-holo-sheen:hover::before,
	.os-holo-sheen:focus-visible::before {
		opacity: 1;
		background-position: 76% 68%;
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-sheen::before {
			transition-duration: 1ms;
		}

		.os-holo-sheen:hover::before,
		.os-holo-sheen:focus-visible::before {
			background-position: 20% 30%;
		}
	}
`,bt=C`
	.os-holo-edge {
		position: relative;
	}

	.os-holo-edge::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		padding: 1px;
		background-image: var( --_holo-edge-quiet );
		opacity: 0;
		pointer-events: none;
		/*
		 * Two masks: one clipped to the content box, one covering the
		 * whole element. "exclude" keeps the difference — the 1 px
		 * padding ring — which is the border. The -webkit- pair comes
		 * first for Safari, which still ships the prefixed property and
		 * spells the operation "xor" rather than "exclude".
		 */
		-webkit-mask: linear-gradient( #000 0 0 ) content-box,
			linear-gradient( #000 0 0 );
		-webkit-mask-composite: xor;
		mask: linear-gradient( #000 0 0 ) content-box, linear-gradient( #000 0 0 );
		mask-composite: exclude;
		transition: opacity var( --_holo-t ) ease;
	}

	.os-holo-edge:hover::after,
	.os-holo-edge:focus-visible::after {
		background-image: var( --_holo-edge );
		opacity: 1;
	}

	.os-holo-edge.is-lit::after {
		background-image: var( --_holo-edge );
		opacity: 1;
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-edge::after {
			transition-duration: 1ms;
		}
	}
`,mt=C`
	.os-holo-glint {
		position: absolute;
		inset: 0;
		border-radius: inherit;
		/* Clips the band to the control's shape. On the span rather
		   than the control, so the control keeps its own overflow. */
		overflow: hidden;
		pointer-events: none;
	}

	.os-holo-glint::before {
		content: '';
		position: absolute;
		/*
		 * Oversized and rotated, so the band is a plain rectangle that
		 * happens to cross the box diagonally. Translating a clipped
		 * rectangle is a composited move; animating gradient stops
		 * would repaint every frame.
		 */
		top: -60%;
		bottom: -60%;
		width: 45%;
		inset-inline-start: -60%;
		background: linear-gradient(
			90deg,
			transparent 0%,
			rgba( 255, 251, 255, 0.14 ) 45%,
			rgba( 255, 251, 255, 0.22 ) 50%,
			rgba( 255, 251, 255, 0.14 ) 55%,
			transparent 100%
		);
		transform: rotate( 18deg ) translateX( 0 );
		opacity: 0;
	}

	/*
	 * Two forms, because the glint is stamped in two places.
	 *
	 * Inside a control (a button, a key) it is a child of that
	 * element, and the "hover >" form reaches it. Directly in a
	 * component's shadow root (a card, a tile) its parent is the
	 * shadow ROOT, which no selector matches — that form finds nothing
	 * there. The :host( :hover ) > form is the one that does, and the
	 * two are mutually exclusive in practice, so both can always be
	 * present.
	 */
	:hover > .os-holo-glint::before,
	:focus-visible > .os-holo-glint::before,
	:host( :hover ) > .os-holo-glint::before,
	:host( :focus-visible ) > .os-holo-glint::before {
		animation: os-holo-glint var( --_holo-t-slow ) var( --_holo-ease );
	}

	@keyframes os-holo-glint {
		0% {
			opacity: 0;
			transform: rotate( 18deg ) translateX( 0 );
		}

		15% {
			opacity: 1;
		}

		85% {
			opacity: 1;
		}

		100% {
			opacity: 0;
			/*
			 * Far enough that the band has cleared a very wide box.
			 * Viewport units rather than a percentage of the element:
			 * a percentage translate on a 45%-wide band means one thing
			 * on a 40px chip and another on a 900px row, and the chip's
			 * glint would finish before it was visible.
			 */
			transform: rotate( 18deg ) translateX( 240vw );
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		:hover > .os-holo-glint::before,
		:focus-visible > .os-holo-glint::before,
		:host( :hover ) > .os-holo-glint::before,
		:host( :focus-visible ) > .os-holo-glint::before {
			animation: none;
		}
	}
`,vt=C`
	.os-holo-ring {
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: 0 0 0 0 var( --_holo-ring-color );
		opacity: 0;
		pointer-events: none;
	}

	/* Both forms, for the same reason as the glint above. */
	:active:not( :disabled ) > .os-holo-ring,
	:host( :active:not( [ disabled ] ) ) > .os-holo-ring {
		animation: os-holo-ring var( --_holo-t-slow ) var( --_holo-ease );
	}

	@keyframes os-holo-ring {
		0% {
			opacity: 0.45;
			box-shadow: 0 0 0 0 var( --_holo-ring-color );
		}

		100% {
			opacity: 0;
			box-shadow: 0 0 0 10px transparent;
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		:active:not( :disabled ) > .os-holo-ring,
		:host( :active:not( [ disabled ] ) ) > .os-holo-ring {
			animation: none;
		}
	}
`,ts=C`
	.os-holo-shimmer {
		background-image: var( --_holo-fill );
		background-size: 300% 300%;
		background-repeat: no-repeat;
		animation: os-holo-shimmer 2.4s var( --_holo-loop ) infinite;
	}

	@keyframes os-holo-shimmer {
		0% {
			background-position: 0% 50%;
		}

		100% {
			background-position: 100% 50%;
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-shimmer {
			animation: none;
			background-position: 30% 50%;
		}
	}
`,ss=C`
	.os-holo-enter {
		animation: os-holo-enter var( --_holo-t ) var( --_holo-spring );
	}

	@keyframes os-holo-enter {
		from {
			opacity: 0;
			transform: scale( 0.96 );
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-enter {
			animation: none;
		}
	}
`,Be=C`
	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ),
	select,
	textarea {
		transition: background-color var( --_holo-t ) ease,
			border-color var( --_holo-t ) ease, box-shadow var( --_holo-t ) ease;
	}

	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ):hover:not(
			:disabled
		),
	select:hover:not( :disabled ),
	textarea:hover:not( :disabled ) {
		border-color: var( --os-ui-border-strong, #8c8f94 );
	}

	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ):focus,
	input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ):focus-visible,
	select:focus,
	select:focus-visible,
	textarea:focus,
	textarea:focus-visible {
		outline: none;
		border-color: var( --os-ui-accent, #2271b1 );
		box-shadow: var( --_holo-focus-field );
	}

	/*
	 * Placeholders go one step further down the Shade ramp than muted
	 * body text. At the same value they compete with a real entry and
	 * the field reads as filled when it is empty.
	 */
	input::placeholder,
	textarea::placeholder {
		color: var( --os-ui-fg-faint, #8c8f94 );
		opacity: 1;
	}

	/*
	 * Selection inside a field.
	 *
	 * A shadow root does not inherit the document's ::selection rule,
	 * so the shell-wide one in desktop.css cannot reach in here — this
	 * has to be restated, and it reads the same two tokens so the two
	 * cannot drift.
	 *
	 * It used to read --os-ui-accent-soft, which is a 14% wash meant
	 * for a hover tint. As a selection it was ~1.2:1 against the field
	 * behind it: technically present, effectively invisible. Both
	 * halves are set, because leaving the text colour to the UA is how
	 * a selection ends up as black-on-violet.
	 */
	input::selection,
	textarea::selection {
		background: var( --os-ui-selection-bg, rgba( 159, 152, 255, 0.6 ) );
		color: var( --os-ui-selection-fg, #fffbff );
	}

	@media ( prefers-reduced-motion: reduce ) {
		input:where( :not( [ type='checkbox' ] ):not( [ type='radio' ] ) ),
		select,
		textarea {
			transition-duration: 1ms;
		}
	}
`,wt=C`
	input[ type='checkbox' ],
	input[ type='radio' ] {
		appearance: none;
		-webkit-appearance: none;
		position: relative;
		flex: 0 0 auto;
		box-sizing: border-box;
		width: 16px;
		height: 16px;
		margin: 0;
		padding: 0;
		/*
		 * The unchecked box's boundary carries the whole control: an
		 * empty checkbox IS its outline. --_holo-track-edge rather
		 * than --os-ui-border-strong because the latter is Silver, at
		 * 2.03:1 against Obsidian — under the 3:1 WCAG 1.4.11 asks of
		 * a control boundary, and visibly so on a dark panel.
		 */
		border: 1px solid var( --_holo-track-edge );
		border-radius: 4px;
		background-color: var( --_holo-track );
		background-image: none;
		background-repeat: no-repeat;
		cursor: inherit;
		transition: background-color var( --_holo-t ) ease,
			border-color var( --_holo-t ) ease, box-shadow var( --_holo-t ) ease;
	}

	input[ type='radio' ] {
		border-radius: 50%;
	}

	input[ type='checkbox' ]:hover:not( :disabled ),
	input[ type='radio' ]:hover:not( :disabled ) {
		border-color: var( --os-ui-accent, #2271b1 );
	}

	input[ type='checkbox' ]:focus-visible,
	input[ type='radio' ]:focus-visible {
		outline: none;
		box-shadow: var( --_holo-focus );
	}

	/*
	 * Checked wears the accent, flat. The mesh is reserved for hero
	 * surfaces, and a checkbox in a settings list is not one: a
	 * column of forms where every ticked box is iridescent has no
	 * identity moments left to spend.
	 */
	input[ type='checkbox' ]:checked,
	input[ type='checkbox' ]:indeterminate,
	input[ type='radio' ]:checked {
		border-color: transparent;
		background-color: var( --os-ui-accent, #2271b1 );
	}

	input[ type='checkbox' ]:checked:focus-visible,
	input[ type='radio' ]:checked:focus-visible {
		box-shadow: var( --_holo-focus );
	}

	/*
	 * The tick. Two borders of a box, rotated — the oldest trick in
	 * CSS and still the only one that scales with the font and takes
	 * its colour from a custom property.
	 */
	input[ type='checkbox' ]:checked::after {
		content: '';
		position: absolute;
		inset-inline-start: 50%;
		top: 46%;
		width: 3.5px;
		height: 7.5px;
		border: solid var( --os-ui-fg-on-accent, #fff );
		border-width: 0 2px 2px 0;
		transform: translate( -50%, -50% ) rotate( 45deg );
		animation: os-holo-tick 180ms cubic-bezier( 0.3, 1.4, 0.6, 1 );
	}

	/* Indeterminate: a bar, not a tick. Same ink, same box. */
	input[ type='checkbox' ]:indeterminate::after {
		content: '';
		position: absolute;
		inset-inline-start: 50%;
		top: 50%;
		width: 8px;
		height: 2px;
		border: 0;
		border-radius: 1px;
		background: var( --os-ui-fg-on-accent, #fff );
		transform: translate( -50%, -50% );
	}

	input[ type='radio' ]:checked::after {
		content: '';
		position: absolute;
		inset-inline-start: 50%;
		top: 50%;
		width: 6px;
		height: 6px;
		border-radius: 50%;
		background: var( --os-ui-fg-on-accent, #fff );
		transform: translate( -50%, -50% );
		animation: os-holo-tick 180ms cubic-bezier( 0.3, 1.4, 0.6, 1 );
	}

	input[ type='checkbox' ]:disabled,
	input[ type='radio' ]:disabled {
		cursor: not-allowed;
	}

	/*
	 * The tick lands rather than appears. 180ms with a slight
	 * overshoot, which is short enough to feel like a response to the
	 * click and not like an animation being played at you.
	 */
	@keyframes os-holo-tick {
		from {
			opacity: 0;
			transform: translate( -50%, -50% ) rotate( 45deg ) scale( 0.4 );
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		input[ type='checkbox' ]:checked::after,
		input[ type='radio' ]:checked::after {
			animation: none;
		}
	}
`,yt=C`
	@keyframes os-holo-drift {
		0% {
			background-position: 16% 26%;
		}

		50% {
			background-position: 84% 74%;
		}

		100% {
			background-position: 16% 26%;
		}
	}

	.os-holo-alive {
		animation: os-holo-drift var( --_holo-t-ambient ) var( --_holo-loop ) infinite;
	}

	@media ( prefers-reduced-motion: reduce ) {
		.os-holo-alive {
			animation: none;
		}
	}
`;C`
	${te}
	${es}
	${ft}
	${bt}
	${mt}
	${vt}
	${ts}
	${ss}
	${Be}
	${wt}
	${yt}
`;const os=C`
	${te}
	${ft}
	${bt}
	${mt}
	${vt}
	${yt}

	:host {
		display: inline-flex;
	}
	:host( [ fill-cell ] ) {
		display: flex;
		width: 100%;
	}
	button {
		appearance: none;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 6px;
		padding: var( --os-ui-button-padding, 6px 12px );
		border-radius: var( --os-ui-button-border-radius, 6px );
		font: inherit;
		font-weight: 500;
		cursor: pointer;
		transition: background-color var( --_holo-t ) ease, color var( --_holo-t ) ease,
			border-color var( --_holo-t ) ease, box-shadow var( --_holo-t ) ease,
			transform 80ms ease;
		/* Ghost (default) */
		background: var( --os-ui-button-bg, transparent );
		/* Desktop-theme texture slot: unset resolves to none. Declared
		   on the base rule so every variant inherits it — the variant
		   rules below override background-COLOR only. */
		background-image: var( --os-ui-button-bg-image, none );
		background-repeat: var( --os-ui-button-bg-image-repeat, repeat );
		background-size: var( --os-ui-button-bg-image-size, auto );
		background-position: var( --os-ui-button-bg-image-position, center );
		color: var( --os-ui-button-fg, var( --os-ui-fg, #1d2327 ) );
		border: var(
			--os-ui-button-border,
			1px solid var( --os-ui-border, #c3c4c7 )
		);
	}
	:host( [ fill-cell ] ) button {
		width: 100%;
		min-height: var( --os-ui-button-min-height, 44px );
	}
	button:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}
	/*
	 * A press should move. One pixel is enough to read as travel and
	 * small enough that a toolbar of them does not look like it is
	 * bouncing; it is also the cheapest possible transform, so a grid
	 * of forty keypad buttons costs nothing to animate.
	 */
	button:active:not( :disabled ) {
		transform: translateY( 1px );
	}
	button:hover:not( :disabled ) {
		background-color: var( --os-ui-button-bg-hover, var( --os-ui-hover, rgba( 0, 0, 0, 0.04 ) ) );
	}
	/*
	 * One focus ring for the whole kit, replacing the browser default.
	 * It has to survive landing on a bright mesh, which a flat outline
	 * does not — see the note on --_holo-focus.
	 */
	button:focus-visible {
		outline: none;
		box-shadow: var( --_holo-focus );
	}
	/* Every lit layer is suppressed while the button is unavailable —
	   a disabled control that lights up under the pointer is
	   advertising an action it will not take. */
	button:disabled::before,
	button:disabled::after,
	button:disabled .os-holo-glint {
		opacity: 0 !important;
	}
	/* Primary */
	:host( [ variant='primary' ] ) button {
		background-color: var( --os-ui-button-bg, var( --wp-admin-theme-color, #2271b1 ) );
		color: var( --os-ui-button-fg, var( --os-ui-fg-on-accent, #fff ) );
		border: var( --os-ui-button-border, 1px solid transparent );
	}
	:host( [ variant='primary' ] ) button:hover:not( :disabled ) {
		filter: brightness( 1.06 );
		background-color: var( --os-ui-button-bg, var( --wp-admin-theme-color, #2271b1 ) );
		box-shadow: var( --_holo-glow );
	}
	/*
	 * Holo — the hero CTA. The mesh itself, with Void ink on it (every
	 * mesh in the brand is a light surface; see --os-ui-holo-ink), and
	 * the tilt-on-hover that makes it read as foil rather than as a
	 * pink rectangle. One per surface, at most.
	 */
	:host( [ variant='holo' ] ) button {
		background-color: transparent;
		background-image: var( --_holo-fill );
		background-size: 220% 220%;
		background-position: 22% 28%;
		background-repeat: no-repeat;
		color: var( --os-ui-button-fg, var( --_holo-ink ) );
		border: var( --os-ui-button-border, 1px solid transparent );
		box-shadow: var( --_holo-glow );
		font-weight: 600;
		transition: background-position var( --_holo-t ) ease,
			box-shadow var( --_holo-t ) ease, filter var( --_holo-t ) ease,
			transform 80ms ease;
	}
	:host( [ variant='holo' ] ) button:hover:not( :disabled ) {
		background-color: transparent;
		background-position: 74% 66%;
		box-shadow: var( --_holo-glow-strong );
	}
	:host( [ variant='holo' ] ) button:active:not( :disabled ) {
		background-position: 88% 82%;
		filter: brightness( 0.94 );
	}
	/* The sheen has nothing to add over a full mesh, and reads as haze. */
	:host( [ variant='holo' ] ) button::before {
		display: none;
	}
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ variant='holo' ] ) button:hover:not( :disabled ),
		:host( [ variant='holo' ] ) button:active:not( :disabled ) {
			background-position: 22% 28%;
		}
	}
	/* Secondary — quiet filled control. Neutral chrome, no underline.
	 * Semantic fit for "not the primary action but also not a
	 * destructive one" (AC / ± / % on a calculator; Cancel in a
	 * two-button dialog). */
	:host( [ variant='secondary' ] ) button {
		background-color: var( --os-ui-button-bg, var( --os-ui-hover, rgba( 0, 0, 0, 0.06 ) ) );
		color: var( --os-ui-button-fg, var( --os-ui-fg, #1d2327 ) );
		border: var( --os-ui-button-border, 1px solid transparent );
	}
	:host( [ variant='secondary' ] ) button:hover:not( :disabled ) {
		background-color: var( --os-ui-button-bg-hover, var( --os-ui-hover, rgba( 0, 0, 0, 0.1 ) ) );
	}
	/* Danger */
	:host( [ variant='danger' ] ) button {
		background-color: var( --os-ui-button-bg, transparent );
		color: var( --os-ui-button-fg, var( --os-ui-danger, #d63638 ) );
		border: var( --os-ui-button-border, 1px solid currentColor );
	}
	:host( [ variant='danger' ] ) button:hover:not( :disabled ) {
		background-color: var( --os-ui-danger, #d63638 );
		color: var( --os-ui-fg-on-accent, #fff );
	}
	/*
	 * Danger keeps its own edge. The iridescent hairline says "brand
	 * moment" and a delete button is the one place that is the wrong
	 * sentence — the border should stay red all the way through the
	 * hover, which is the only warning the user gets.
	 */
	:host( [ variant='danger' ] ) button::after {
		display: none;
	}
	/* Link */
	:host( [ variant='link' ] ) button {
		background-color: transparent;
		background-image: none;
		color: var( --os-ui-button-fg, var( --wp-admin-theme-color, #2271b1 ) );
		border: 0;
		padding: 0;
		text-decoration: underline;
	}
	/* No chrome means nothing to put an edge, a film, a highlight or a
	   ring on — a link is text, and text does not catch the light. */
	:host( [ variant='link' ] ) button::before,
	:host( [ variant='link' ] ) button::after,
	:host( [ variant='link' ] ) .os-holo-glint,
	:host( [ variant='link' ] ) .os-holo-ring {
		display: none;
	}
	:host( [ variant='link' ] ) button:active:not( :disabled ) {
		transform: none;
	}
	:host( [ busy ] ) button {
		pointer-events: none;
		opacity: 0.75;
	}
	/*
	 * A busy holo button keeps drifting. It is the one place in the kit
	 * where the ambient animation is load-bearing rather than
	 * decorative: the mesh moving is what says the work is still
	 * running, next to a spinner that says the same thing more quietly.
	 */
	:host( [ variant='holo' ][ busy ] ) button {
		animation: os-holo-drift 12s ease-in-out infinite;
	}
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ variant='holo' ][ busy ] ) button {
			animation: none;
		}
	}
	.os-button__spinner {
		box-sizing: border-box;
		display: inline-block;
		width: 12px;
		height: 12px;
		border: 2px solid currentColor;
		border-right-color: transparent;
		border-radius: 50%;
		animation: os-button-spin 0.6s linear infinite;
		flex-shrink: 0;
	}
	@keyframes os-button-spin {
		to {
			transform: rotate( 360deg );
		}
	}
`,we=class we extends U{render(){const e=this.disabled!==null,t=this.busy!==null,o=this.type||"button";return h`
			<button
				part="button"
				class="os-holo-edge os-holo-sheen"
				type=${o}
				?disabled=${e||t}
				aria-busy=${t?"true":"false"}
			>
				${t?h`<span class="os-button__spinner" aria-hidden="true"></span>`:""}
				<slot></slot>
				<!--
					The two motion layers. Elements rather than
					pseudo-elements because the sheen and the hairline
					have already taken this button's ::before and
					::after — see the pseudo-element budget note in
					src/ui/holo.ts. Both are inert and aria-hidden.
				-->
				<span class="os-holo-glint" aria-hidden="true"></span>
				<span class="os-holo-ring" aria-hidden="true"></span>
			</button>
		`}};we.props=["variant","disabled","type","busy","fill-cell"],we.styles=[os],we.help={title:"Button",summary:"Thin wrapper around <button> with consistent variant styling and a slot for the label.",status:"stable",props:[{name:"variant",type:"'holo' | 'primary' | 'secondary' | 'ghost' | 'danger' | 'link'",default:"ghost",description:"Visual weight of the button. Use primary for the single attention-grabbing action per surface, and holo — the Holomesh fill — only for a hero call to action."},{name:"disabled",type:"boolean attribute",description:"Disable pointer + keyboard interaction and dim the chrome."},{name:"type",type:"'button' | 'submit' | 'reset'",default:"button",description:"Forwarded to the underlying native <button>."},{name:"busy",type:"boolean attribute",description:"Marks the button as in-progress (e.g., awaiting a fetch)."},{name:"fill-cell",type:"boolean attribute",description:"Grow to fill the parent flex/grid cell. Useful for tiled keypads."}],slots:[{name:"(default)",description:"Button label."}],parts:[{name:"button",description:"Underlying <button> element."}],cssProps:[{name:"--os-ui-button-bg",description:"Background color."},{name:"--os-ui-button-bg-hover",description:"Hover wash (ghost + secondary variants)."},{name:"--os-ui-button-fg",description:"Text color."},{name:"--os-ui-button-border",description:"Border shorthand."},{name:"--os-ui-button-border-radius",default:"6px"},{name:"--os-ui-button-padding",default:"6px 12px"},{name:"--os-ui-button-min-height",description:"Minimum height when fill-cell is set."}],example:h`
			<os-cluster gap="8">
				<os-button variant="holo">Holo</os-button>
				<os-button variant="primary">Primary</os-button>
				<os-button variant="secondary">Secondary</os-button>
				<os-button variant="ghost">Ghost</os-button>
				<os-button variant="danger">Danger</os-button>
				<os-button variant="link">Link</os-button>
			</os-cluster>
		`};let We=we;K("os-button",We);const is=C`
	${te}
	${wt}

	:host {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		font-size: 12px;
		color: var( --os-ui-fg, #1d2327 );
		cursor: pointer;
		/*
		 * A label row is a target, not a text run. Selecting the text
		 * on a fast double-toggle leaves the row highlighted and is
		 * never what anyone meant by clicking it twice.
		 */
		-webkit-user-select: none;
		user-select: none;
	}
	label {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		cursor: pointer;
	}
	:host( [ disabled ] ) {
		opacity: 0.5;
		cursor: not-allowed;
	}
	:host( [ disabled ] ) label,
	:host( [ disabled ] ) input[ type='checkbox' ] {
		cursor: not-allowed;
	}
`,ye=class ye extends U{render(){const e=this.label||"",t=this.checked!==null,o=this.disabled!==null;return h`
			<label>
				<input
					type="checkbox"
					?checked=${t}
					?disabled=${o}
					@change=${a=>this._onChange(a)}
				/>
				<span class="os-checkbox-label__text">${e}</span>
			</label>
		`}_onChange(e){if(this.disabled!==null)return;const t=e.target.checked;t?this.setAttribute("checked",""):this.removeAttribute("checked"),this.emit("os-checkbox-change",{checked:t})}};ye.props=["label","checked","disabled"],ye.styles=[is],ye.help={title:"Checkbox label",summary:"Opinionated label-row variant of <os-checkbox>: label text + checkbox in a single aligned row. Use when you want the shipped layout without any layout work.",status:"stable",props:[{name:"label",type:"string",description:"Visible label text, paired with the checkbox via a native <label>."},{name:"checked",type:"boolean attribute",description:"Reflects and controls the checked state."},{name:"disabled",type:"boolean attribute",description:"When present, the checkbox is not interactive and dimmed."}],events:[{name:"os-checkbox-change",description:"Fires when the user toggles the checkbox.",detail:"{ checked: boolean }"}],cssProps:[{name:"--os-ui-fg",description:"Label colour."}],example:h`
			<os-checkbox-label label="Reduce motion" checked></os-checkbox-label>
		`};let He=ye;K("os-checkbox-label",He);const as=C`
	:host {
		display: inline-flex;
		align-items: center;
		gap: 6px;
		font-size: var( --os-ui-save-status-font-size, 11px );
		line-height: 1;
		color: var( --os-ui-save-status-fg, currentColor );
		vertical-align: middle;
		min-width: 0;
		opacity: 1;
		pointer-events: auto;
	}

	/*
	 * Indicator is ALWAYS visible — like a real modem's "ready" LED.
	 * Idle state shows a hollow ring tinted by the user's accent
	 * (so the title bar always reads "alive, ready"); activity
	 * phases swap to a solid fill plus the chaotic modem-blink with
	 * a soft glow. The accent ring keeps idle calm; the blink reads
	 * as data flowing.
	 *
	 * The ring honours --os-ui-save-status-idle-color first; falls
	 * back to a translucent admin-theme accent (color-mix blends
	 * the live --wp-admin-theme-color toward transparent at ~55%
	 * opacity) so the dot always picks up the active accent
	 * automatically.
	 */
	.os-save-status__indicator {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: var( --os-ui-save-status-size, 12px );
		height: var( --os-ui-save-status-size, 12px );
		border-radius: 50%;
		flex-shrink: 0;
		box-sizing: border-box;
		background: var( --os-ui-save-status-bg, transparent );
		border: 2px solid
			var(
				--os-ui-save-status-idle-color,
				color-mix(
					in srgb,
					var( --wp-admin-theme-color, #2271b1 ) 55%,
					transparent
				)
			);
		color: var( --wp-admin-theme-color, #2271b1 );
		transition:
			background-color 0.2s ease,
			border-color 0.2s ease,
			box-shadow 0.2s ease;
	}

	/* Phase: pending / saving — primary-tinted blinking dot.
	 * Default cadence is a smooth pulse; animation="modem" swaps in
	 * the burst-style activity blink that mimics a 1990s data modem
	 * — short bursts of stutters with quiet pauses between, never
	 * the same shape twice in a row. */
	:host( [ phase='pending' ] ) .os-save-status__indicator,
	:host( [ phase='saving' ] ) .os-save-status__indicator {
		background: var(
			--os-ui-save-status-bg,
			var( --wp-admin-theme-color, #2271b1 )
		);
		border-color: transparent;
		color: var( --wp-admin-theme-color, #2271b1 );
		animation: os-save-status-pulse 1.2s ease-in-out infinite;
	}

	/* Modem-light animation — chaotic on/off bursts that mimic a
	 * 1990s modem's TX/RX LED while data flows. Two cycles ride
	 * over each other (a fast "stutter" track + a slower "burst"
	 * track) so the pattern never repeats cleanly within a few
	 * seconds. The eye reads it as data, not a metronome. */
	:host( [ animation='modem' ][ phase='pending' ] ) .os-save-status__indicator,
	:host( [ animation='modem' ][ phase='saving' ] ) .os-save-status__indicator {
		background: var(
			--os-ui-save-status-bg,
			var( --wp-admin-theme-color, #2271b1 )
		);
		border-color: transparent;
		color: var( --wp-admin-theme-color, #2271b1 );
		/* Slow, sparse cadence — a real modem's data LED isn't a
		 * strobe. Two tracks at slightly-different periods drift
		 * against each other so the pattern never reads as
		 * metronomic; the combined pattern only truly repeats every
		 * 7.2s (the LCM of the 1.8s and 2.4s periods). */
		animation:
			os-save-status-modem-stutter 1.8s ease-in-out infinite,
			os-save-status-modem-glow    2.4s ease-in-out infinite;
	}

	/*
	 * Stutter track — controls the dot opacity. Sparse, irregular
	 * pattern: a single quick flash, a long quiet, a doublet, more
	 * quiet, a longer flash, fade. Fewer blinks per cycle than the
	 * previous "burst" design — the eye reads it as a calm,
	 * intermittent activity LED rather than a strobe.
	 *
	 * Off-cycle opacity is 0.2 (not 0) so the dot stays present as
	 * a "dim glow" between flashes — closer to how a real LED looks
	 * dimming/brightening than a hard binary cut.
	 */
	@keyframes os-save-status-modem-stutter {
		/* Quick flash. */
		0%, 4%    { opacity: 1; }

		/* Long quiet. */
		5%, 30%   { opacity: 0.22; }

		/* Doublet — two short on-pulses with a beat between. */
		31%, 36%  { opacity: 1; }
		37%, 39%  { opacity: 0.22; }
		40%, 44%  { opacity: 1; }

		/* Quiet. */
		45%, 67%  { opacity: 0.22; }

		/* Single longer flash — the "burst" of the cycle. */
		68%, 76%  { opacity: 1; }

		/* Long quiet through the end. */
		77%, 100% { opacity: 0.22; }
	}

	/*
	 * Glow track — small halo timed to the brighter peaks. Lower
	 * blur radius (4px) and zero spread so the halo decorates the
	 * dot without dominating the title bar. Three soft pulses per
	 * cycle, drifting against the stutter.
	 */
	@keyframes os-save-status-modem-glow {
		0%, 12%   { box-shadow: 0 0 0 0 transparent; }
		13%, 22%  { box-shadow: 0 0 4px 0 currentColor; }
		23%, 50%  { box-shadow: 0 0 0 0 transparent; }
		51%, 58%  { box-shadow: 0 0 4px 0 currentColor; }
		59%, 84%  { box-shadow: 0 0 0 0 transparent; }
		85%, 94%  { box-shadow: 0 0 5px 0 currentColor; }
		95%, 100% { box-shadow: 0 0 0 0 transparent; }
	}

	/* Reduced-motion: disable the chaotic blink. Keep a solid lit
	 * dot so the user still sees "something is happening" — same
	 * affordance, calmer animation. */
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ phase='pending' ] ) .os-save-status__indicator,
		:host( [ phase='saving' ] ) .os-save-status__indicator,
		:host( [ animation='modem' ][ phase='pending' ] ) .os-save-status__indicator,
		:host( [ animation='modem' ][ phase='saving' ] ) .os-save-status__indicator {
			animation: none;
			opacity: 0.85;
		}
	}

	/* Phase: saved — solid green, no pulse. The hollow ring
	 * "fills in" momentarily before auto-clearing back to idle.
	 *
	 * The second fallback is the SEMANTIC colour, not --os-ui-surface
	 * — routing it through the surface token was the classic
	 * fallback-chain collapse: the palette declares --os-ui-surface
	 * (Obsidian), so the green dot resolved to the same near-black as
	 * the panel behind it and vanished. */
	:host( [ phase='saved' ] ) .os-save-status__indicator {
		background: var( --os-ui-save-status-saved-bg, var( --os-ui-success-fg, #1d6f42 ) );
		border-color: transparent;
		color: var( --os-ui-save-status-saved-bg, var( --os-ui-success-fg, #1d6f42 ) );
	}

	/* Phase: failed — solid red, gentle attention pulse. */
	:host( [ phase='failed' ] ) .os-save-status__indicator {
		background: var( --os-ui-save-status-failed-bg, var( --os-ui-danger, #d63638 ) );
		border-color: transparent;
		color: var( --os-ui-save-status-failed-bg, var( --os-ui-danger, #d63638 ) );
		animation: os-save-status-pulse 0.8s ease-in-out 2;
	}

	/*
	 * Variant: 'ring' — the outline IS the indicator.
	 *
	 * The default treatment fills the dot for every phase that isn't
	 * idle, which makes three of the five states the same shape in
	 * three colours. The ring keeps the outline and moves the COLOUR
	 * through it, spending the filled state on exactly one thing:
	 * success. That's what makes "it worked" the only phase that
	 * reads at a glance, which is the phase users are actually
	 * waiting for.
	 *
	 * Built for the window title bar, where the ring replaced the app
	 * icon — a duplicate of the dock tile below it — so the only mark
	 * in that corner is one that means something.
	 */
	/*
	 * The ring carries NO resting fill, and this rule is what
	 * guarantees it.
	 *
	 * --os-ui-save-status-bg is the dot's background — the base rule
	 * reads it as the background, defaulting to transparent. A
	 * consumer setting it to colour the ring's in-flight outline
	 * therefore painted that colour as a solid idle fill: one token
	 * meaning two different things one rule apart, which is why the
	 * ring wants a name of its own (--os-ui-save-status-ring-color,
	 * below) and why the fill is pinned off here regardless of what
	 * anyone sets. Only saved reinstates a background, at higher
	 * specificity.
	 */
	:host( [ variant='ring' ] ) .os-save-status__indicator {
		background: transparent;
	}

	:host( [ variant='ring' ][ phase='pending' ] ) .os-save-status__indicator,
	:host( [ variant='ring' ][ phase='saving' ] ) .os-save-status__indicator {
		background: transparent;
		border-color: var(
			--os-ui-save-status-ring-color,
			var( --os-ui-save-status-bg, var( --wp-admin-theme-color, #2271b1 ) )
		);
		animation: os-save-status-ring-pulse 1.6s
			var( --os-ui-ease-loop, ease-in-out ) infinite;
	}

	/*
	 * Failure keeps the ring open and tints the glyph rather than
	 * the fill. A filled red disc and a filled accent disc are the
	 * same silhouette, and colour alone is not a distinction every
	 * user can make.
	 */
	/*
	 * Failure keeps the ring open and tints the glyph rather than
	 * the fill, and arrives with two quick swells — an alert, not a
	 * heartbeat, so it stops rather than continuing.
	 */
	:host( [ variant='ring' ][ phase='failed' ] ) .os-save-status__indicator {
		background: transparent;
		border-color: var(
			--os-ui-save-status-failed-bg,
			var( --os-ui-danger, #d63638 )
		);
		animation: os-save-status-ring-alert 0.62s
			var( --os-ui-ease-out, ease-out ) 1;
	}

	:host( [ variant='ring' ][ phase='failed' ] ) .os-save-status__glyph {
		color: var(
			--os-ui-save-status-failed-bg,
			var( --os-ui-danger, #d63638 )
		);
	}

	/*
	 * Success is the one state that fills, so it is also the one that
	 * gets a gesture: the ring overshoots slightly and settles, the
	 * way a thing does when it lands. The colours cross-fade under it
	 * on the base rule's own transition — border to transparent, fill
	 * to the accent — so the outline doesn't blink out and reappear as
	 * a disc.
	 */
	:host( [ variant='ring' ][ phase='saved' ] ) .os-save-status__indicator {
		background: var(
			--os-ui-save-status-saved-bg,
			var( --wp-admin-theme-color, #2271b1 )
		);
		border-color: transparent;
		animation: os-save-status-ring-land 0.42s
			var( --os-ui-ease-out, ease-out ) 1;
	}

	/*
	 * The glyph arrives after the shape it sits in — a check that
	 * fades up into a ring already filling reads as one event, where
	 * both appearing together reads as a swap. The backwards fill-mode
	 * holds the 0% frame during the delay, so the glyph can't flash at
	 * full size before the animation starts.
	 */
	:host( [ variant='ring' ][ phase='saved' ] ) .os-save-status__glyph,
	:host( [ variant='ring' ][ phase='failed' ] ) .os-save-status__glyph {
		animation: os-save-status-glyph-in 0.24s
			var( --os-ui-ease-out, ease-out ) 0.08s 1 backwards;
	}

	/*
	 * The pulse breathes the ring rather than blinking it — opacity
	 * and scale only, both compositor properties, and neither
	 * reaching zero. A ring that disappears between beats reads as a
	 * fault; one that breathes reads as work continuing.
	 */
	@keyframes os-save-status-ring-pulse {
		0%, 100% { opacity: 0.45; scale: 0.9; }
		50%      { opacity: 1;    scale: 1; }
	}

	/* Overshoot and settle. Small — 6% — because the ring is 16px in
	 * a title bar, and at that size a big bounce reads as a wobble. */
	@keyframes os-save-status-ring-land {
		0%   { scale: 0.82; }
		55%  { scale: 1.06; }
		100% { scale: 1; }
	}

	/* Two swells, decaying. Never past 1.08 for the same reason. */
	@keyframes os-save-status-ring-alert {
		0%   { scale: 0.9;  }
		25%  { scale: 1.08; }
		50%  { scale: 0.98; }
		75%  { scale: 1.04; }
		100% { scale: 1;    }
	}

	@keyframes os-save-status-glyph-in {
		0%   { opacity: 0; scale: 0.4; }
		100% { opacity: 1; scale: 1;   }
	}

	/*
	 * Reduced motion: every phase still changes colour and fill, and
	 * none of them move. The ring holds lit instead of breathing, and
	 * the outcomes arrive without their gesture — the gesture is
	 * emphasis, the fill and the glyph are the information.
	 */
	@media ( prefers-reduced-motion: reduce ) {
		:host( [ variant='ring' ] ) .os-save-status__indicator,
		:host( [ variant='ring' ] ) .os-save-status__glyph {
			animation: none;
			opacity: 1;
			scale: 1;
		}
	}

	@keyframes os-save-status-pulse {
		0%, 100% { opacity: 0.55; transform: scale( 0.9 ); }
		50%      { opacity: 1;    transform: scale( 1 ); }
	}

	/* Mode: 'pill' shows a label next to the dot. Defaults to 'dot'
	 * (no label, indicator only) for the smallest footprint. */
	:host( [ mode='pill' ] ) .os-save-status {
		display: inline-flex;
		align-items: center;
		gap: 6px;
		padding: 2px 10px;
		border-radius: 999px;
		background: var( --os-ui-save-status-pill-bg, transparent );
		font-weight: 500;
		white-space: nowrap;
	}
	:host( [ mode='pill' ][ phase='saving' ] ) .os-save-status,
	:host( [ mode='pill' ][ phase='pending' ] ) .os-save-status {
		background: var( --os-ui-save-status-pill-bg, var( --os-ui-hover, rgba( 0, 0, 0, 0.04 ) ) );
		color: var( --os-ui-save-status-pill-fg, var( --os-ui-fg-muted, #50575e ) );
	}
	:host( [ mode='pill' ][ phase='saved' ] ) .os-save-status {
		background: var( --os-ui-save-status-pill-bg, rgba( 30, 132, 73, 0.12 ) );
		color: var( --os-ui-save-status-pill-fg, var( --os-ui-success-fg, #1d6f42 ) );
	}
	:host( [ mode='pill' ][ phase='failed' ] ) .os-save-status {
		background: var( --os-ui-save-status-pill-bg, rgba( 214, 54, 56, 0.12 ) );
		color: var( --os-ui-save-status-pill-fg, var( --os-ui-danger-hover, #a02622 ) );
	}

	.os-save-status__label {
		min-width: 0;
		max-width: 200px;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	/* Mode: 'icon' renders a glyph (check / cross) inside the dot
	 * for the saved / failed phases. The svg inherits color from the
	 * indicator's foreground. */
	:host( [ phase='saved' ] ) .os-save-status__glyph,
	:host( [ phase='failed' ] ) .os-save-status__glyph {
		display: inline-block;
		color: var( --os-ui-fg-on-accent, #fff );
		/* Proportional to the indicator so a resized ring keeps its
		 * glyph in the same relationship to the stroke. */
		width: calc( var( --os-ui-save-status-size, 12px ) * 0.66 );
		height: calc( var( --os-ui-save-status-size, 12px ) * 0.66 );
	}
	.os-save-status__glyph {
		display: none;
	}
	.os-save-status__glyph svg {
		display: block;
		width: 100%;
		height: 100%;
	}
`,xt="os-settings-save-lifecycle",ns=2200,rs=6e3,xe=class xe extends U{constructor(){super(...arguments),this._autoTimer=null,this._docListener=null}connectedCallback(){super.connectedCallback(),this.auto!==null&&this._installAutoListener()}disconnectedCallback(){this._removeAutoListener(),this._autoTimer!==null&&(window.clearTimeout(this._autoTimer),this._autoTimer=null)}attributeChangedCallback(e,t,o){if(super.attributeChangedCallback(e,t,o),(e==="auto"||e==="event")&&(this._removeAutoListener(),this.auto!==null&&this._installAutoListener()),e==="phase"){this._scheduleAutoClear();const a={phase:this.phase??"idle",error:this.error??void 0};this.emit("os-save-status-change",a)}}render(){const e=this.phase??"idle",t=this.mode??"dot",a=(this.error??"")||this._labelForPhase(e);return a?this.setAttribute("title",a):this.removeAttribute("title"),this.setAttribute("aria-live",e==="failed"?"assertive":"polite"),this.setAttribute("role",e==="failed"?"alert":"status"),h`
			<span class="os-save-status">
				<span class="os-save-status__indicator" aria-hidden="true">
					<span class="os-save-status__glyph">${this._renderGlyph(e)}</span>
				</span>
				${t==="pill"?h`<span class="os-save-status__label"
							>${this._labelForPhase(e)}</span
					  >`:h``}
			</span>
		`}_renderGlyph(e){return e==="saved"?ls():e==="failed"?ds():""}_labelForPhase(e){switch(e){case"pending":case"saving":return this["saving-label"]??"Saving…";case"saved":return this["saved-label"]??"Saved";case"failed":return(this.error??"")||"Couldn’t save";default:return this["idle-label"]??""}}_installAutoListener(){const e=this.event||xt;this._docListener=t=>{const o=t.detail;!o||typeof o.phase!="string"||(this.phase=o.phase,o.error?this.error=o.error:o.phase!=="failed"&&this.error&&this.removeAttribute("error"))},document.addEventListener(e,this._docListener)}_removeAutoListener(){if(!this._docListener)return;const e=this.event||xt;document.removeEventListener(e,this._docListener),this._docListener=null}_scheduleAutoClear(){this._autoTimer!==null&&(window.clearTimeout(this._autoTimer),this._autoTimer=null);const e=this.phase??"idle",t=this._autoClearMsFor(e);t<=0||(this._autoTimer=window.setTimeout(()=>{this._autoTimer=null,this.phase="idle"},t))}_autoClearMsFor(e){if(e==="saved"){const t=this["auto-clear-saved-ms"];return parseInt(t||"",10)||ns}if(e==="failed"){const t=this["auto-clear-failed-ms"];return parseInt(t||"",10)||rs}return 0}};xe.props=["phase","mode","variant","animation","auto","event","error","saving-label","saved-label","idle-label","auto-clear-saved-ms","auto-clear-failed-ms"],xe.styles=[as],xe.help={title:"Save status",summary:'Tiny status indicator for "is this change saved yet?" affordances. Three layouts (dot / icon / pill), five phases, optional auto-listen to a save-lifecycle CustomEvent so every input in the panel inherits feedback for free.',status:"stable",props:[{name:"phase",type:"'idle' | 'pending' | 'saving' | 'saved' | 'failed'",default:"idle",description:"Current lifecycle phase. Set manually for one-off integrations, or rely on `auto` to populate it from a CustomEvent."},{name:"mode",type:"'dot' | 'icon' | 'pill'",default:"dot",description:"Layout. `dot` is the smallest (10×10 colored dot); `icon` adds a glyph inside on saved/failed; `pill` adds an inline label."},{name:"variant",type:"'dot' (default) | 'ring'",description:"Treatment. The default fills the indicator for every non-idle phase; `ring` keeps the outline open and moves the colour through it, filling only on `saved` — so success is the one state that reads at a glance. Failure tints the glyph rather than the fill, since a filled red disc and a filled accent disc are the same silhouette. This is what the window title bar wears."},{name:"animation",type:"'pulse' | 'modem'",default:"pulse",description:"Animation cadence during the saving phase. `pulse` (default) is a smooth ease-in-out; `modem` is an irregular activity-LED blink with a soft glow — suits a 'data-flowing' affordance in window title bars."},{name:"auto",type:"boolean attribute",description:'Subscribe to a CustomEvent on `document` and populate phase + error from its detail. Default event name is `os-settings-save-lifecycle`; override with `event="…"`.'},{name:"event",type:"string",default:"os-settings-save-lifecycle",description:"CustomEvent name to listen on when `auto` is set."},{name:"error",type:"string",description:"Error message shown in `pill` mode and exposed as the host title attribute (so dot/icon modes still surface the message via tooltip)."},{name:"saving-label",type:"string",default:"Saving…",description:"Pill-mode label shown during `pending` / `saving`."},{name:"saved-label",type:"string",default:"Saved",description:"Pill-mode label shown during `saved`."},{name:"idle-label",type:"string",description:'Optional pill-mode label shown during `idle` (e.g. "All changes saved"). When unset, the pill collapses to invisible while idle.'},{name:"auto-clear-saved-ms",type:"integer",default:"2200",description:"How long the `saved` phase stays visible before auto-fading back to `idle`."},{name:"auto-clear-failed-ms",type:"integer",default:"6000",description:"How long the `failed` phase stays visible before auto-fading back to `idle`."}],events:[{name:"os-save-status-change",description:"Fires when the phase changes (manually or via auto-listen).",detail:"{ phase, error }"}],cssProps:[{name:"--os-ui-save-status-size",description:"Indicator diameter (default 12px). The glyph scales with it."},{name:"--os-ui-save-status-idle-color",description:"Ring colour at rest."},{name:"--os-ui-save-status-bg",description:'Indicator background color (saving/pending phase). Under `variant="ring"` set `--os-ui-save-status-ring-color` instead — this one is a *background*, and the ring has none until it succeeds.'},{name:"--os-ui-save-status-ring-color",description:'`variant="ring"` only: the outline colour while a request is in flight. Falls back to `--os-ui-save-status-bg`.'},{name:"--os-ui-save-status-saved-bg",description:"Indicator background on saved."},{name:"--os-ui-save-status-failed-bg",description:"Indicator background on failed."},{name:"--os-ui-save-status-pill-bg",description:"Pill background (mode=pill)."},{name:"--os-ui-save-status-pill-fg",description:"Pill foreground (mode=pill)."}],example:h`
			<os-cluster gap="12">
				<os-save-status phase="pending"></os-save-status>
				<os-save-status phase="saving"></os-save-status>
				<os-save-status phase="saved"></os-save-status>
				<os-save-status phase="failed"></os-save-status>
				<os-save-status mode="pill" phase="saving"></os-save-status>
				<os-save-status mode="pill" phase="saved"></os-save-status>
				<os-save-status mode="pill" phase="failed" error="Network error."></os-save-status>
				<os-save-status variant="ring" mode="icon" phase="saving"></os-save-status>
				<os-save-status variant="ring" mode="icon" phase="saved"></os-save-status>
				<os-save-status variant="ring" mode="icon" phase="failed"></os-save-status>
			</os-cluster>
		`};let Ke=xe;K("os-save-status",Ke);function ls(){return h`
		<svg
			viewBox="0 0 12 12"
			aria-hidden="true"
			focusable="false"
			fill="none"
			stroke="currentColor"
			stroke-width="2"
			stroke-linecap="round"
			stroke-linejoin="round"
		>
			<path d="M2.5 6 L5 8.5 L9.5 4" />
		</svg>
	`}function ds(){return h`
		<svg
			viewBox="0 0 12 12"
			aria-hidden="true"
			focusable="false"
			fill="currentColor"
		>
			<path
				d="M5 2 H7 V7 H5 z M5 8.5 H7 V10.5 H5 z"
			/>
		</svg>
	`}const cs=C`
	:host {
		display: inline-block;
		--_color: var(
			--os-ui-spinner-color,
			var( --wp-admin-theme-color, #21759b )
		);
		/*
		 * Falls back to the ON-ACCENT foreground, not to the accent.
		 * The mark is drawn OVER the disc, and the disc resolves to
		 * --wp-admin-theme-color; the palette sets that and --os-ui-accent
		 * to the same Pulse, so pointing both aliases at an accent
		 * painted the W in the disc's own colour and the spinner became
		 * a plain filled circle. The pre-brand literals (#21759b disc,
		 * #fff mark) had hidden that they were ever the same idea.
		 */
		--_accent: var(
			--os-ui-spinner-accent,
			var( --os-ui-fg-on-accent, #fff )
		);
		--_size: var( --os-ui-spinner-size, 48px );
		width: var( --_size );
		height: var( --_size );
		/* SVG inner elements use currentColor for the primary fill /
		   stroke; inheriting the host's text color makes a single
		   variable drive every ring. */
		color: var( --_color );
		vertical-align: middle;
		line-height: 0;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	/* The inline indicator lives beside a line of text: text-sized by
	   default, and tinted from the text rather than from the admin
	   theme color, so it can't lose contrast against a surface the
	   component knows nothing about. Both are plain defaults — the
	   size / color attributes still win, since those reflect onto
	   inline styles on the host. */
	:host( [ preset='inline' ] ) {
		--os-ui-spinner-color: currentColor;
		--os-ui-spinner-size: 16px;
	}

	.root,
	.root svg {
		display: block;
		width: 100%;
		height: 100%;
	}

	/* The W mark inside the disc — accent color, defaults to white,
	   configurable via the host's --os-ui-spinner-accent (or the
	   shorthand "accent" attribute). */
	.root svg .mark {
		fill: var( --_accent );
	}

	@keyframes os-spinner-spin {
		to {
			transform: rotate( 360deg );
		}
	}
	@keyframes os-spinner-scale {
		0%,
		100% {
			transform: scale( 1 );
		}
		50% {
			transform: scale( 1.045 );
		}
	}
	@keyframes os-spinner-opacity {
		0%,
		100% {
			opacity: 1;
		}
		50% {
			opacity: 0.7;
		}
	}

	@media ( prefers-reduced-motion: reduce ) {
		.root svg [ style*='animation' ] {
			animation: none !important;
		}
	}
`,kt=Object.freeze({classic:{sp1:12,sp2:24,sp3:40,a1:28,a2:15,a3:8,gap:4,dir2:1,dir3:-1,pulse:"none",dots:0},comet:{sp1:8,sp2:14,sp3:26,a1:50,a2:28,a3:12,gap:3,dir2:1,dir3:1,pulse:"none",dots:5},orbit:{sp1:10,sp2:10,sp3:32,a1:50,a2:50,a3:8,gap:5,dir2:-1,dir3:-1,pulse:"opacity",dots:3},pulse:{sp1:6,sp2:18,sp3:30,a1:20,a2:12,a3:6,gap:4,dir2:1,dir3:-1,pulse:"both",dots:8},inline:{sp1:8,sp2:8,sp3:8,a1:25,a2:25,a3:25,gap:0,dir2:1,dir3:1,pulse:"none",dots:0}}),F=61.26,R=61.26,_t=58.453,us='<path d="m8.708 61.26c0 20.802 12.089 38.779 29.619 47.298l-25.069-68.686c-2.916 6.536-4.55 13.769-4.55 21.388z"/><path d="m96.74 58.608c0-6.495-2.333-10.993-4.334-14.494-2.664-4.329-5.161-7.995-5.161-12.324 0-4.831 3.664-9.328 8.825-9.328.233 0 .454.029.681.042-9.35-8.566-21.807-13.796-35.489-13.796-18.36 0-34.513 9.42-43.91 23.688 1.233.037 2.395.063 3.382.063 5.497 0 14.006-.667 14.006-.667 2.833-.167 3.167 3.994.337 4.329 0 0-2.847.335-6.015.501l19.138 56.925 11.501-34.493-8.188-22.434c-2.83-.166-5.511-.501-5.511-.501-2.832-.166-2.5-4.496.332-4.329 0 0 8.679.667 13.843.667 5.496 0 14.006-.667 14.006-.667 2.835-.167 3.168 3.994.337 4.329 0 0-2.853.335-6.015.501l18.992 56.494 5.242-17.517c2.272-7.269 4.001-12.49 4.001-16.989z"/><path d="m62.184 65.857-15.768 45.819c4.708 1.384 9.687 2.141 14.846 2.141 6.12 0 11.989-1.058 17.452-2.979-.141-.225-.269-.464-.374-.724z"/><path d="m107.376 36.046c.226 1.674.354 3.471.354 5.404 0 5.333-.996 11.328-3.996 18.824l-16.053 46.413c15.624-9.111 26.133-26.038 26.133-45.426.001-9.137-2.333-17.729-6.438-25.215z"/>',ke=class ke extends U{constructor(){super(...arguments),this._paintScheduled=!1}connectedCallback(){super.connectedCallback(),this._schedulePaint()}render(){return h`<div class="root" part="root"></div>`}requestUpdate(){super.requestUpdate(),this._schedulePaint()}_schedulePaint(){this._paintScheduled||!this.isConnected||(this._paintScheduled=!0,queueMicrotask(()=>{this._paintScheduled=!1,this.isConnected&&this._paint()}))}_paint(){this._syncCssVars();const e=this.shadowRoot?.querySelector(".root");e&&(e.innerHTML=this._buildSvg())}_syncCssVars(){const e=(t,o,a)=>{const c=this.getAttribute(t);c===null?this.style.removeProperty(o):this.style.setProperty(o,a?a(c):c)};e("color","--os-ui-spinner-color"),e("accent","--os-ui-spinner-accent"),e("size","--os-ui-spinner-size",t=>/^-?\d+(\.\d+)?$/.test(t.trim())?`${t}px`:t)}_effectiveConfig(){const e=this.getAttribute("preset")??"classic",t=kt[e]??kt.classic,o=(d,g)=>{const b=this.getAttribute(d);if(b===null)return g;const f=parseFloat(b);return Number.isFinite(f)?f:g},a=(d,g)=>{const b=this.getAttribute(d);if(b===null)return g;const f=b.toLowerCase();return f==="-1"||f==="ccw"||f==="reverse"?-1:1},c=()=>{const d=this.getAttribute("pulse");return d==="scale"||d==="opacity"||d==="both"||d==="none"?d:t.pulse};return{sp1:o("sp1",t.sp1),sp2:o("sp2",t.sp2),sp3:o("sp3",t.sp3),a1:o("a1",t.a1),a2:o("a2",t.a2),a3:o("a3",t.a3),gap:o("gap",t.gap),dir2:a("dir2",t.dir2),dir3:a("dir3",t.dir3),pulse:c(),dots:Math.max(0,Math.floor(o("dots",t.dots)))}}_buildInlineSvg(e){const t=Ct(this.getAttribute("label")??"Loading"),o=9,a=(e.sp1/10).toFixed(2);return`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" role="img" aria-label="${t}"><circle cx="12" cy="12" r="${o}" fill="none" stroke="currentColor" stroke-width="2.4" stroke-opacity="0.22"/><circle cx="12" cy="12" r="${o}" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-dasharray="${Pe(o,e.a1)}" style="transform-origin:12px 12px;animation: os-spinner-spin ${a}s linear infinite"/></svg>`}_buildSvg(){const e=this._effectiveConfig();if(this.getAttribute("preset")==="inline")return this._buildInlineSvg(e);const t=Ct(this.getAttribute("label")??"Loading"),o=e.gap*3+14,a=-o,c=122.52+o*2,d=_t+e.gap+2,g=d+e.gap+2,b=g+e.gap+1.5,f=`animation: os-spinner-spin ${(e.sp1/10).toFixed(2)}s linear infinite`,$=`animation: os-spinner-spin ${(e.sp2/10).toFixed(2)}s linear infinite${e.dir2<0?" reverse":""}`,x=`animation: os-spinner-spin ${(e.sp3/10).toFixed(2)}s linear infinite${e.dir3<0?" reverse":""}`,N=(e.sp1*1.8/10).toFixed(1),k=(e.sp1*2.3/10).toFixed(1);let v="";e.pulse==="scale"?v=`animation: os-spinner-scale ${N}s ease-in-out infinite`:e.pulse==="opacity"?v=`animation: os-spinner-opacity ${k}s ease-in-out infinite`:e.pulse==="both"&&(v=`animation: os-spinner-scale ${N}s ease-in-out infinite, os-spinner-opacity ${k}s ease-in-out infinite`);let P="";if(e.dots>0){const w=b+e.gap+1,L=2*Math.PI*w,A=1.6,_=(e.sp1*.65/10).toFixed(2);for(let B=0;B<e.dots;B++){const Le=-(B/e.dots)*L;P+=`<circle cx="${F}" cy="${R}" r="${w.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="${A}" stroke-dasharray="${A.toFixed(2)} ${(L-A).toFixed(2)}" stroke-dashoffset="${Le.toFixed(2)}" stroke-linecap="round" stroke-opacity="0.65" style="transform-origin:${F}px ${R}px;animation: os-spinner-spin ${_}s linear infinite"/>`}}return`<svg xmlns="http://www.w3.org/2000/svg" viewBox="${a} ${a} ${c} ${c}" role="img" aria-label="${t}"><g style="transform-origin:${F}px ${R}px${v?";"+v:""}"><circle cx="${F}" cy="${R}" r="${_t}" fill="currentColor"/><g class="mark">${us}</g></g><circle cx="${F}" cy="${R}" r="${d.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="0.6" stroke-opacity="0.2"/><circle cx="${F}" cy="${R}" r="${d.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="2.2" stroke-dasharray="${Pe(d,e.a1)}" stroke-linecap="round" style="transform-origin:${F}px ${R}px;${f}"/><circle cx="${F}" cy="${R}" r="${g.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="0.5" stroke-opacity="0.15"/><circle cx="${F}" cy="${R}" r="${g.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="1.6" stroke-opacity="0.8" stroke-dasharray="${Pe(g,e.a2)}" stroke-linecap="round" style="transform-origin:${F}px ${R}px;${$}"/><circle cx="${F}" cy="${R}" r="${b.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="0.4" stroke-opacity="0.12"/><circle cx="${F}" cy="${R}" r="${b.toFixed(2)}" fill="none" stroke="currentColor" stroke-width="1.0" stroke-opacity="0.6" stroke-dasharray="${Pe(b,e.a3)}" stroke-linecap="round" style="transform-origin:${F}px ${R}px;${x}"/>`+P+"</svg>"}};ke.props=["preset","size","color","accent","sp1","sp2","sp3","a1","a2","a3","gap","dir2","dir3","pulse","dots","label"],ke.styles=[cs],ke.help={title:"Spinner",summary:"Animated WordPress-mark loading indicator with five curated presets and full per-attribute overrides. The `inline` preset swaps the mark for a bare arc sized for text-adjacent use. CSS variables drive disc + accent colors and size; reduced-motion preferences are respected.",status:"stable",props:[{name:"preset",type:'"classic" | "comet" | "orbit" | "pulse" | "inline"',default:"classic",description:"Visual personality. Every other attribute defaults to the preset's value and can be overridden individually. `inline` is the odd one out: a bare rotating arc with no WordPress mark, defaulting to 16px and to `currentColor`, for use beside a line of text where the mark would be illegible."},{name:"size",type:"integer (px) or CSS length",default:'48 (16 for preset="inline")',description:"Sets `--os-ui-spinner-size`. Bare numbers are treated as px; pass a CSS length (e.g. `2em`) to opt into ems / rems."},{name:"color",type:"CSS color",description:"Disc + ring + dot color. Sets `--os-ui-spinner-color`. Default inherits the WP admin theme color."},{name:"accent",type:"CSS color",default:"#fff",description:"Color of the W mark inside the disc. Sets `--os-ui-spinner-accent`. Default white — change for dark-on-light or themed marks."},{name:"sp1, sp2, sp3",type:"integer (deciseconds)",description:"Per-ring rotation duration in tenths-of-a-second (12 → 1.2s). Higher = slower."},{name:"a1, a2, a3",type:"integer (0-100)",description:"Per-ring arc length as a percentage of the ring circumference."},{name:"gap",type:"integer",description:"Gap between concentric rings (units approximate to px at 120-viewport)."},{name:"dir2, dir3",type:'"1" | "-1" | "cw" | "ccw"',description:"Per-ring direction; ring 1 is always clockwise."},{name:"pulse",type:'"none" | "scale" | "opacity" | "both"',description:"Pulse animation applied to the disc + W mark."},{name:"dots",type:"integer",description:"Outer trailing dot count. Sensible values: 0, 3, 5, 8."},{name:"label",type:"string",default:"Loading",description:'Accessible name for the SVG (`role="img"` + `aria-label`).'}],cssProps:[{name:"--os-ui-spinner-color",default:"var(--wp-admin-theme-color, #21759b)"},{name:"--os-ui-spinner-accent",default:"var(--os-ui-accent, #fff)"},{name:"--os-ui-spinner-size",default:"48px"}],example:h`<os-spinner preset="comet" size="80"></os-spinner>`};let Ve=ke;function Pe(s,e){const t=2*Math.PI*s,o=e/100*t,a=t-o;return`${o.toFixed(2)} ${a.toFixed(2)}`}function Ct(s){return String(s).replace(/&/g,"&amp;").replace(/"/g,"&quot;").replace(/</g,"&lt;").replace(/>/g,"&gt;")}K("os-spinner",Ve);const hs=C`
	${te}

	:host {
		display: block;
		width: 100%;
		aspect-ratio: 4 / 3;
	}
	:host( [ size='small' ] ) {
		display: inline-block;
		width: 32px;
		height: 32px;
		aspect-ratio: 1 / 1;
		flex: 0 0 auto;
	}
	/*
	 * Accent variant: the rounded square, at the guide's inner radius.
	 *
	 * A circle is the obvious shape for a colour chip and the wrong one
	 * here. Everything else in this panel that can be picked is a
	 * rounded rectangle (wallpaper tiles, theme cards, layout cards),
	 * so a row of discs read as a legend rather than as a row of
	 * choices. Slightly smaller than the plain small size for the same
	 * reason: a colour is the least of the decisions on this page.
	 */
	:host( [ variant='accent' ] ) {
		display: inline-block;
		width: 28px;
		height: 28px;
		aspect-ratio: 1 / 1;
		flex: 0 0 auto;
	}
	/*
	 * Both selectors name the small size as well, which is not
	 * redundant: the circle comes from the size-only button rule
	 * further down this sheet, and at equal specificity the later rule
	 * wins. Naming both attributes puts this one above it whatever the
	 * order.
	 */
	:host( [ size='small' ][ variant='accent' ] ) button {
		border-radius: 8px;
	}
	/*
	 * The accent chip's ring holds the surface colour as a gap between
	 * chip and ring, so the ring reads as drawn AROUND the colour
	 * rather than retinting its edge, which matters more here than on
	 * any tile, because the colour is the entire content.
	 */
	:host( [ size='small' ][ variant='accent' ] )
		button[ aria-pressed='true' ] {
		box-shadow: 0 0 0 2px var( --os-ui-surface-sunken, #f0f0f1 ),
			0 0 0 4px var( --os-ui-accent, #2271b1 );
	}
	/*
	 * Keyboard focus outranks the selection ring while it is visible.
	 * The focus ring is a strict superset of the selection ring's
	 * shape (gap, accent, bloom), so nothing is lost while it shows;
	 * without this the selected chip is the one chip focus cannot be
	 * seen on, because the selected rules above outrank the plain
	 * focus rule below.
	 */
	:host( [ size='small' ][ variant='accent' ] )
		button[ aria-pressed='true' ]:focus-visible {
		box-shadow: var( --_holo-focus );
	}
	/*
	 * Wallpaper variant: 16:10 aspect (the mockup's desk proportion),
	 * and positions slotted overlay content (e.g. a label chip) at the
	 * bottom-left so it reads like a photo-corner caption. Caller owns
	 * the label's own visual treatment; we just place it.
	 */
	:host( [ variant='wallpaper' ] ) {
		aspect-ratio: 16 / 10;
	}
	:host( [ variant='wallpaper' ] ) button {
		display: flex;
		align-items: flex-end;
		justify-content: flex-start;
		padding: 8px;
		overflow: hidden;
	}
	button {
		appearance: none;
		/* Anchor absolutely-positioned slotted overlays (the wallpaper
		 * grid's live-preview layer) to the tile. */
		position: relative;
		width: 100%;
		height: 100%;
		padding: 0;
		border-radius: 10px;
		border: 2px solid transparent;
		cursor: pointer;
		/* The tile's base, visible wherever the swatch image is
		   transparent or still loading. A fixed light grey punched a
		   hole in a dark station, so it follows the surface ramp. */
		background-color: var( --os-ui-surface-sunken, #eee );
		background-size: cover;
		background-position: center;
		transition: transform 0.15s ease, border-color 0.15s ease,
			box-shadow 0.15s ease;
	}
	:host( [ size='small' ] ) button {
		border-radius: 50%;
	}
	button:hover {
		transform: scale( 1.04 );
	}
	button:focus-visible {
		outline: none;
		box-shadow: var( --_holo-focus );
	}
	/*
	 * Chosen. A flat accent ring, not the mesh: the brand reserves
	 * meshes for hero surfaces, and in OpenStation Preferences that
	 * budget is spent on the sidebar's selected-row edge. A grid of
	 * mesh-ringed tiles beside it is wallpaper.
	 *
	 * box-shadow rather than border so the ring sits OUTSIDE the
	 * tile, which matters for a wallpaper swatch: the artwork is the
	 * content, and a ring drawn on top of it would crop the thing the
	 * user is choosing.
	 */
	button[ aria-pressed='true' ] {
		border-color: transparent;
		box-shadow: 0 0 0 2px var( --os-ui-accent, #2271b1 );
	}
	/* See the accent-variant note: focus wins while it is visible. */
	button[ aria-pressed='true' ]:focus-visible {
		box-shadow: var( --_holo-focus );
	}
	/*
	 * Wallpaper variant uses a softer lift to pair with the
	 * larger visible surface — hover scale on a 200 px tile can
	 * feel cartoonish.
	 */
	:host( [ variant='wallpaper' ] ) button:hover {
		transform: translateY( -2px );
	}
`,_e=class _e extends U{render(){const e=this.selected!==null,t=this.label||"",o=this.preview||"";return h`
			<button
				type="button"
				aria-pressed=${e?"true":"false"}
				aria-label=${t}
				title=${t}
				style="background: ${o}"
				@click=${()=>this._onPick()}
			>
				<slot></slot>
			</button>
		`}_onPick(){this.emit("os-pick",{value:this.value})}};_e.props=["value","label","selected","preview","size","variant"],_e.styles=[hs],_e.help={title:"Swatch",summary:"Selectable color/wallpaper tile. Renders as an aria-pressed button with a background driven by the preview attribute.",status:"stable",props:[{name:"value",type:"string",description:"Identifier emitted on the os-pick event when the swatch is clicked."},{name:"label",type:"string",description:"aria-label + title for the button."},{name:"selected",type:"boolean attribute",description:"Marks the swatch as the active choice within a swatch-grid."},{name:"preview",type:"CSS background value",description:"Raw CSS background (color, gradient, url()) painted on the tile."},{name:"size",type:"string",description:"Visual size hint (e.g. sm, md, lg). Consumed by the stylesheet."},{name:"variant",type:"string",description:"Optional visual variant (e.g. color vs wallpaper)."}],slots:[{name:"(default)",description:"Optional overlay content rendered inside the tile."}],events:[{name:"os-pick",description:"Fires when the swatch is clicked.",detail:"{ value: string }"}],example:h`
			<os-swatch-grid label="Accent">
				<os-swatch value="red" preview="#ef4444" label="Red" selected></os-swatch>
				<os-swatch value="blue" preview="#3b82f6" label="Blue"></os-swatch>
				<os-swatch value="green" preview="#10b981" label="Green"></os-swatch>
			</os-swatch-grid>
		`};let je=_e;K("os-swatch",je);const ps=C`
	:host {
		display: grid;
		grid-template-columns: repeat(
			var( --os-ui-swatch-grid-cols, 4 ),
			1fr
		);
		gap: 12px;
	}
	:host( [ mode='row' ] ) {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 10px;
	}
`,Ce=class Ce extends U{render(){const e=this.label||"",t=this.columns||"";return t&&this.style.setProperty("--os-ui-swatch-grid-cols",t),this.setAttribute("role","radiogroup"),e&&this.setAttribute("aria-label",e),h`<slot></slot>`}};Ce.props=["label","columns","mode"],Ce.styles=[ps],Ce.help={title:"Swatch grid",summary:"Flex grid container for <os-swatch> children. Emits radiogroup semantics so screen readers announce the tiles as a unit.",status:"stable",props:[{name:"label",type:"string",description:'aria-label describing the group (e.g. "Accent color").'},{name:"columns",type:"CSS grid track template",description:"Overrides the default column track via --os-ui-swatch-grid-cols."},{name:"mode",type:"string",description:"Optional rendering variant forwarded to child swatches."}],slots:[{name:"(default)",description:"<os-swatch> children."}],cssProps:[{name:"--os-ui-swatch-grid-cols",description:"Grid column template."}],example:h`
			<os-swatch-grid label="Wallpaper">
				<os-swatch value="a" preview="linear-gradient(135deg,#f093fb,#f5576c)"></os-swatch>
				<os-swatch value="b" preview="linear-gradient(135deg,#4facfe,#00f2fe)"></os-swatch>
				<os-swatch value="c" preview="linear-gradient(135deg,#43e97b,#38f9d7)"></os-swatch>
			</os-swatch-grid>
		`};let Ge=Ce;K("os-swatch-grid",Ge);const gs=C`
	:host {
		display: inline-flex;
		max-width: 100%;
	}

	.os-tag-input {
		display: inline-flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var( --os-ui-tag-input-gap, 4px );
		padding: var( --os-ui-tag-input-padding, 2px );
		min-height: 24px;
		max-width: 100%;
	}

	.os-tag-input__chips {
		display: inline-flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var( --os-ui-tag-input-gap, 4px );
		min-width: 0;
	}

	/* The "+ Add" trigger is intentionally minimal — a single small
	 * button that doesn't compete with the chips. It expands into
	 * the inline input when clicked. */
	.os-tag-input__add {
		appearance: none;
		display: inline-flex;
		align-items: center;
		gap: 3px;
		padding: 1px 8px;
		min-height: 22px;
		font: inherit;
		font-size: 11px;
		font-weight: 500;
		line-height: 1;
		color: var( --os-ui-tag-input-add-fg, var( --os-ui-fg-muted, #50575e ) );
		background: transparent;
		border: 1px dashed var( --os-ui-tag-input-add-border, var( --os-ui-border, #c3c4c7 ) );
		border-radius: 999px;
		cursor: pointer;
		transition:
			background-color 0.12s ease,
			color 0.12s ease,
			border-color 0.12s ease;
	}
	.os-tag-input__add:hover:not( :disabled ) {
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.04 ) );
		color: var( --os-ui-tag-input-add-fg-hover, var( --os-ui-fg, #1d2327 ) );
		border-color: var( --os-ui-tag-input-add-border-hover, var( --os-ui-border-strong, #8c8f94 ) );
	}
	.os-tag-input__add:focus-visible {
		outline: none;
		border-style: solid;
		box-shadow: var(
			--os-ui-focus-ring,
			0 0 0 2px rgba( 12, 11, 15, 0.9 ), 0 0 0 4px #2271b1
		);
	}
	.os-tag-input__add:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}
	.os-tag-input__add svg {
		display: block;
	}

	/* Editor — the inline input + the floating suggestions popover. */
	.os-tag-input__editor {
		position: relative;
		display: inline-flex;
		align-items: center;
		flex: 0 1 auto;
		min-width: 120px;
	}

	.os-tag-input__input {
		appearance: none;
		font: inherit;
		font-size: 12px;
		line-height: 1.4;
		padding: 2px 8px;
		border: 1px solid var( --os-ui-tag-input-input-border, var( --os-ui-border, #2271b1 ) );
		border-radius: 999px;
		background: var( --os-ui-tag-input-input-bg, var( --os-ui-surface, #fff ) );
		color: var( --os-ui-tag-input-input-fg, var( --os-ui-fg, #1d2327 ) );
		min-width: 80px;
		max-width: 240px;
	}
	.os-tag-input__input:focus {
		outline: none;
		border-color: var( --os-ui-accent, #2271b1 );
		box-shadow: var(
			--os-ui-focus-ring-field,
			0 0 0 1px #2271b1, 0 0 0 4px rgba( 34, 113, 177, 0.18 )
		);
	}

	/* Popover — pinned to the editor, full width by default. Z-index
	 * is locked above os-table sticky-header (which sits at z-index
	 * ~3) but below the global toast layer (z-index ~10000). */
	.os-tag-input__suggestions {
		position: absolute;
		top: calc( 100% + 4px );
		left: 0;
		min-width: 220px;
		max-width: 320px;
		max-height: 240px;
		overflow-y: auto;
		padding: 4px 0;
		background: var( --os-ui-tag-input-pop-bg, var( --os-ui-surface, #fff ) );
		color: var( --os-ui-tag-input-pop-fg, var( --os-ui-fg, #1d2327 ) );
		border: 1px solid var( --os-ui-tag-input-pop-border, var( --os-ui-border, #c3c4c7 ) );
		border-radius: 8px;
		box-shadow:
			0 6px 16px rgba( 0, 0, 0, 0.08 ),
			0 1px 2px rgba( 0, 0, 0, 0.06 );
		z-index: 50;
	}

	.os-tag-input__suggestion-item {
		display: flex;
		align-items: center;
		gap: 6px;
		padding: 6px 12px;
		font-size: 13px;
		cursor: pointer;
		user-select: none;
	}
	.os-tag-input__suggestion-item[ aria-selected='true' ] {
		background: color-mix(
			in srgb,
			var( --wp-admin-theme-color, #2271b1 ) 10%,
			transparent
		);
		color: var( --wp-admin-theme-color, #2271b1 );
	}

	.os-tag-input__suggestion-create {
		font-style: italic;
		color: var( --os-ui-tag-input-create-fg, var( --os-ui-fg-muted, #50575e ) );
		border-top: 1px solid var( --os-ui-tag-input-pop-divider, var( --os-ui-border, #f0f0f1 ) );
	}
	.os-tag-input__suggestion-create[ aria-selected='true' ] {
		color: var( --wp-admin-theme-color, #2271b1 );
	}

	.os-tag-input__suggestion-empty,
	.os-tag-input__suggestion-loading {
		display: flex;
		align-items: center;
		gap: 8px;
		padding: 8px 12px;
		font-size: 12px;
		color: var( --os-ui-tag-input-pop-muted, var( --os-ui-fg-muted, #646970 ) );
	}

	.os-tag-input__suggestion-spinner {
		display: inline-block;
		width: 10px;
		height: 10px;
		border-radius: 50%;
		border: 2px solid currentColor;
		border-top-color: transparent;
		animation: os-tag-input-spin 0.8s linear infinite;
	}

	@keyframes os-tag-input-spin {
		to {
			transform: rotate( 360deg );
		}
	}

	:host( [ disabled ] ) .os-tag-input {
		opacity: 0.6;
		pointer-events: none;
	}
`,fs=C`
	${te}

	:host {
		display: inline-flex;
		max-width: 100%;
		vertical-align: middle;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	.os-chip {
		display: inline-flex;
		align-items: center;
		gap: var( --os-ui-chip-gap, 4px );
		padding: var( --os-ui-chip-padding, 2px 8px );
		border-radius: var( --os-ui-chip-radius, 999px );
		font-size: var( --os-ui-chip-font-size, 12px );
		line-height: var( --os-ui-chip-line-height, 1.6 );
		font-weight: var( --os-ui-chip-font-weight, 500 );
		background: var( --os-ui-chip-bg, var( --os-ui-surface, #f0f0f1 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-fg, #1d2327 ) );
		border: var( --os-ui-chip-border, 1px solid transparent );
		max-width: 100%;
		box-sizing: border-box;
		transition:
			background-color 0.12s ease,
			color 0.12s ease,
			border-color 0.12s ease,
			transform 0.12s ease,
			opacity 0.12s ease;
	}

	/* Tones — same surface as <os-badge> for consistency. */
	:host( [ tone='accent' ] ) .os-chip {
		background: var(
			--os-ui-chip-bg,
			color-mix( in srgb, var( --wp-admin-theme-color, #2271b1 ) 14%, transparent )
		);
		color: var( --os-ui-chip-fg, var( --wp-admin-theme-color, #2271b1 ) );
	}
	:host( [ tone='positive' ] ) .os-chip {
		background: var( --os-ui-chip-bg, rgba( 30, 132, 73, 0.14 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-success-fg, #1d6f42 ) );
	}
	:host( [ tone='warning' ] ) .os-chip {
		background: var( --os-ui-chip-bg, rgba( 217, 119, 6, 0.18 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-warning-fg, #8a4a06 ) );
	}
	:host( [ tone='danger' ] ) .os-chip {
		background: var( --os-ui-chip-bg, rgba( 214, 54, 56, 0.14 ) );
		color: var( --os-ui-chip-fg, var( --os-ui-danger-hover, #a02622 ) );
	}

	/* Pending shimmer — used by os-tag-input while a REST mutation
	 * is in flight. Subtle pulse so the user sees "this chip isn't
	 * settled yet" without alarming animation. */
	:host( [ pending ] ) .os-chip {
		opacity: 0.65;
		animation: os-chip-pulse 1.2s ease-in-out infinite;
	}

	@keyframes os-chip-pulse {
		0%, 100% { opacity: 0.55; }
		50%      { opacity: 0.95; }
	}

	.os-chip__label {
		max-width: var( --os-ui-chip-label-max, 220px );
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.os-chip__icon {
		display: inline-flex;
		align-items: center;
		flex-shrink: 0;
	}
	.os-chip__icon::slotted( * ) {
		display: inline-flex;
	}

	.os-chip__dismiss {
		appearance: none;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		width: 16px;
		height: 16px;
		margin-inline-start: 2px;
		padding: 0;
		border: 0;
		border-radius: 50%;
		background: transparent;
		color: inherit;
		cursor: pointer;
		opacity: 0.55;
		transition: opacity 0.12s ease, background-color 0.12s ease;
	}
	.os-chip__dismiss:hover,
	.os-chip__dismiss:focus-visible {
		opacity: 1;
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.12 ) );
		outline: none;
	}
	.os-chip__dismiss:focus-visible {
		box-shadow: var( --_holo-focus );
	}
	.os-chip__dismiss[ disabled ] {
		opacity: 0.35;
		cursor: not-allowed;
	}
	.os-chip__dismiss svg {
		display: block;
		width: 10px;
		height: 10px;
	}

	:host( [ disabled ] ) .os-chip {
		opacity: 0.55;
		cursor: not-allowed;
	}

	/* Compact density — half the horizontal padding. Used in dense
	 * lists like the posts-window Tags column. */
	:host( [ size='compact' ] ) .os-chip {
		padding: var( --os-ui-chip-padding, 1px 6px );
		font-size: var( --os-ui-chip-font-size, 11px );
	}

	/*
	 * The holographic hairline, and ONLY on a selected chip.
	 *
	 * Chips arrive in rows of eight and twelve — a tag column, a
	 * filter bar, a category picker — so the treatment here has to be
	 * the one that costs nothing when repeated. An edge on the one
	 * chip the user has chosen reads instantly in a row of otherwise
	 * flat pills; an edge on all of them reads as noise.
	 *
	 * Drawn on a mask-composited ::after rather than a border, because
	 * border-color takes a colour and this is a gradient. Same
	 * technique as .os-holo-edge; written out here because the chip's
	 * frame belongs to .os-chip, an inner element rather than the
	 * host, and the shared class hangs its ring on whatever carries
	 * the class.
	 */
	:host( [ selected ] ) .os-chip {
		position: relative;
		background: var( --os-ui-chip-bg, var( --os-ui-accent-soft, rgba( 34, 113, 177, 0.14 ) ) );
		color: var( --os-ui-chip-fg, var( --os-ui-fg, #1d2327 ) );
	}
	:host( [ selected ] ) .os-chip::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		padding: 1px;
		background-image: var( --_holo-edge );
		pointer-events: none;
		-webkit-mask: linear-gradient( #000 0 0 ) content-box,
			linear-gradient( #000 0 0 );
		-webkit-mask-composite: xor;
		mask: linear-gradient( #000 0 0 ) content-box, linear-gradient( #000 0 0 );
		mask-composite: exclude;
	}
`,$e=class $e extends U{constructor(){super(...arguments),this._onHostKeyDown=e=>{if(this.dismissible!==null&&(e.key==="Backspace"||e.key==="Delete")){if(e.preventDefault(),this.disabled!==null)return;const a=this.label??"";this.emit("os-chip-dismiss",{label:a})}}}connectedCallback(){super.connectedCallback(),this.addEventListener("keydown",this._onHostKeyDown)}disconnectedCallback(){this.removeEventListener("keydown",this._onHostKeyDown)}render(){const e=this.label??"",t=this.dismissible!==null,o=this.disabled!==null;return h`
			<span part="chip" class="os-chip">
				<span class="os-chip__icon">
					<slot name="icon"></slot>
				</span>
				<span class="os-chip__label">
					${e===""?h`<slot></slot>`:e}
				</span>
				${t?h`
							<button
								part="dismiss"
								class="os-chip__dismiss"
								type="button"
								aria-label=${`Remove ${e||"chip"}`}
								?disabled=${o}
								@click=${a=>this._onDismiss(a)}
							>
								${bs()}
							</button>
					  `:h``}
			</span>
		`}_onDismiss(e){if(e.stopPropagation(),this.disabled!==null)return;const o=this.label??"";this.emit("os-chip-dismiss",{label:o})}};$e.props=["label","tone","size","dismissible","disabled","pending","selected"],$e.styles=[fs],$e.help={title:"Chip",summary:"Labelled pill primitive with optional leading icon and trailing dismiss button. Tones mirror <os-badge>; pair with <os-tag-input> for full add/remove ergonomics.",status:"stable",props:[{name:"label",type:"string",description:"Visible text. Falls back to the default slot when omitted."},{name:"tone",type:"'neutral' | 'accent' | 'positive' | 'warning' | 'danger'",default:"neutral",description:"Color variant. Mirrors <os-badge> tones."},{name:"size",type:"'default' | 'compact'",default:"default",description:"Vertical density. Compact halves horizontal padding for dense lists."},{name:"dismissible",type:"boolean attribute",description:"Renders a trailing × button. Click / Enter / Space emits os-chip-dismiss."},{name:"disabled",type:"boolean attribute",description:"Visually mutes the chip and blocks the dismiss button. Useful while a parent is mid-update."},{name:"pending",type:"boolean attribute",description:"Renders a subtle pulse animation while a REST mutation is in flight. Auto-applied by <os-tag-input>; safe to set by hand."},{name:"selected",type:"boolean attribute",description:"Marks the chip as chosen: an accent wash plus the kit’s iridescent hairline. Deliberately the only state that gets the hairline — chips arrive in rows of a dozen, and an edge on all of them is noise."}],slots:[{name:"(default)",description:"Fallback label when `label` is unset."},{name:"icon",description:"Leading icon (Dashicon, SVG, image). Inherits text color."}],parts:[{name:"chip",description:"The pill container."},{name:"dismiss",description:"The trailing × button (when `dismissible`)."}],events:[{name:"os-chip-dismiss",description:"Fires when the dismiss button is activated. Detail carries the chip's label so a delegated listener can act without DOM walking.",detail:"{ label: string }"}],cssProps:[{name:"--os-ui-chip-bg",description:"Background color."},{name:"--os-ui-chip-fg",description:"Text color."},{name:"--os-ui-chip-border",description:"Border shorthand."},{name:"--os-ui-chip-padding",description:"Padding shorthand.",default:"2px 8px"},{name:"--os-ui-chip-radius",description:"Corner radius.",default:"999px"},{name:"--os-ui-chip-label-max",description:"Max width of the inner label before ellipsis.",default:"220px"}],example:h`
			<os-cluster gap="6">
				<os-chip label="Neutral"></os-chip>
				<os-chip label="Accent" tone="accent"></os-chip>
				<os-chip label="Positive" tone="positive"></os-chip>
				<os-chip label="Warning" tone="warning"></os-chip>
				<os-chip label="Danger" tone="danger"></os-chip>
				<os-chip label="Dismissible" dismissible></os-chip>
			</os-cluster>
		`};let Ye=$e;K("os-chip",Ye);function bs(){return h`
		<svg
			viewBox="0 0 12 12"
			width="10"
			height="10"
			aria-hidden="true"
			focusable="false"
			fill="none"
			stroke="currentColor"
			stroke-width="1.5"
			stroke-linecap="round"
		>
			<path d="M3 3 L9 9 M9 3 L3 9" />
		</svg>
	`}const Ae=class Ae extends U{constructor(){super(...arguments),this._value=[],this._suggestions=[],this._suggestionsLoading=!1,this._query="",this._highlight=-1,this._focusedChip=-1,this._onDocumentPointerDown=e=>{!this.isOpen||e.composedPath().includes(this)||this.closeInput()}}get _input(){const e=this.shadowRoot;return e?e.querySelector(".os-tag-input__input"):null}get value(){return this._value}set value(e){this._value=Array.isArray(e)?e.slice():[],this._focusedChip>=this._value.length&&(this._focusedChip=-1),this.requestUpdate()}get suggestions(){return this._suggestions}set suggestions(e){this._suggestions=Array.isArray(e)?e.slice():[],this._highlight=this._suggestions.length>0?0:-1,this._suggestionsLoading=!1,this.requestUpdate()}get suggestionsLoading(){return this._suggestionsLoading}set suggestionsLoading(e){this._suggestionsLoading=!!e,this.requestUpdate()}get query(){return this._query}get isOpen(){return this.open!==null}openInput(){this.isOpen||(this.open="",this._query="",this._highlight=-1,this.emit("os-tag-open",{}),queueMicrotask(()=>{this._input?.focus(),this._emitSuggest("")}))}closeInput(){this.isOpen&&(this.open=null,this._query="",this._suggestions=[],this._highlight=-1,this._suggestionsLoading=!1,this.emit("os-tag-close",{}),this.requestUpdate())}connectedCallback(){super.connectedCallback(),document.addEventListener("pointerdown",this._onDocumentPointerDown,!0)}disconnectedCallback(){document.removeEventListener("pointerdown",this._onDocumentPointerDown,!0)}render(){const e=this.isOpen,t=this.disabled!==null,o=this.readonly!==null,a=this.removable!==null||this.removable===null&&!o,c=this.creatable!==null,d=this["add-label"]||"Add",g=this.placeholder||"Add a tag…";return h`
			<span
				class="os-tag-input"
				role="group"
				aria-label=${this.label??""}
			>
				${this._renderChips(a,t)}
				${this._renderTrailing({isOpen:e,readonly:o,disabled:t,placeholder:g,creatable:c,addLabel:d})}
			</span>
		`}_renderTrailing(e){return e.isOpen?this._renderEditor(e.placeholder,e.creatable):e.readonly||e.disabled?h``:this._renderTrigger(e.addLabel)}_renderChips(e,t){const o=this._value;return o.length===0?h``:h`
			<span class="os-tag-input__chips" role="list">
				${o.map((a,c)=>{const d=a.tone??"neutral";return h`
						<os-chip
							role="listitem"
							size="compact"
							tone=${d}
							label=${a.label}
							?dismissible=${e&&!t}
							?disabled=${t}
							?pending=${!!a.pending}
							tabindex=${c===this._focusedChip?"0":"-1"}
							data-idx=${String(c)}
							@os-chip-dismiss=${g=>this._onChipDismiss(g,a)}
							@focus=${()=>this._focusedChip=c}
						></os-chip>
					`})}
			</span>
		`}_renderTrigger(e){const t=this.disabled!==null;return h`
			<button
				type="button"
				class="os-tag-input__add"
				aria-label=${e}
				aria-haspopup="listbox"
				aria-expanded="false"
				?disabled=${t}
				@click=${()=>this.openInput()}
			>
				${ms()}
				<span>${e}</span>
			</button>
		`}_renderEditor(e,t){const o=this._suggestions.length>0||this._suggestionsLoading||t&&this._query.trim().length>0;return h`
			<span class="os-tag-input__editor">
				<input
					class="os-tag-input__input"
					type="text"
					autocomplete="off"
					autocapitalize="off"
					spellcheck="false"
					placeholder=${e}
					.value=${this._query}
					aria-autocomplete="list"
					aria-expanded=${o?"true":"false"}
					aria-activedescendant=${this._highlight>=0?`os-tag-suggestion-${this._highlight}`:""}
					@input=${a=>this._onInput(a)}
					@keydown=${a=>this._onInputKeyDown(a)}
					@blur=${a=>this._onInputBlur(a)}
				/>
				${o?this._renderSuggestions(t):h``}
			</span>
		`}_renderSuggestions(e){const t=this._query.trim(),o=this._suggestions,a=e&&t.length>0&&!o.some(c=>c.label.toLowerCase()===t.toLowerCase())&&!this._value.some(c=>c.label.toLowerCase()===t.toLowerCase());return h`
			<div
				class="os-tag-input__suggestions"
				role="listbox"
			>
				${this._suggestionsLoading?h`
							<div class="os-tag-input__suggestion-loading">
								<span class="os-tag-input__suggestion-spinner" aria-hidden="true"></span>
								<span>Searching…</span>
							</div>
					  `:h``}
				${o.length===0&&!this._suggestionsLoading&&!a?h`
							<div class="os-tag-input__suggestion-empty">
								${t.length>0?"No matches.":"Type to search."}
							</div>
					  `:h``}
				${o.map((c,d)=>{const g=d===this._highlight;return h`
						<div
							id=${`os-tag-suggestion-${d}`}
							role="option"
							aria-selected=${g?"true":"false"}
							class="os-tag-input__suggestion-item"
							@mousedown=${b=>{b.preventDefault(),this._addSuggestion(c,!1)}}
							@mouseenter=${()=>{this._highlight=d,this.requestUpdate()}}
						>
							<span>${c.label}</span>
						</div>
					`})}
				${a?h`
							<div
								id=${`os-tag-suggestion-${o.length}`}
								role="option"
								aria-selected=${this._highlight===o.length?"true":"false"}
								class="os-tag-input__suggestion-item os-tag-input__suggestion-create"
								@mousedown=${c=>{c.preventDefault(),this._addSuggestion({label:t},!0)}}
								@mouseenter=${()=>{this._highlight=o.length,this.requestUpdate()}}
							>
								Create "${t}"
							</div>
					  `:h``}
			</div>
		`}_onChipDismiss(e,t){e.stopPropagation(),this.emit("os-tag-remove",{tag:t})}_onInput(e){const t=e.target.value;this._query=t,this._emitSuggest(t)}_emitSuggest(e){const t=parseInt(this["min-query"]||"0",10)||0;if(e.length<t){this._suggestions=[],this._suggestionsLoading=!1,this.requestUpdate();return}this._suggestionsLoading=!0,this.requestUpdate(),this.emit("os-tag-suggest",{query:e})}_onInputKeyDown(e){const t=this.creatable!==null,o=this._suggestions,a=this._query.trim(),c=t&&a.length>0&&!o.some(g=>g.label.toLowerCase()===a.toLowerCase())&&!this._value.some(g=>g.label.toLowerCase()===a.toLowerCase()),d=o.length+(c?1:0);switch(e.key){case"ArrowDown":{if(d===0)return;e.preventDefault(),this._highlight=this._highlight+1>=d?0:this._highlight+1,this.requestUpdate();return}case"ArrowUp":{if(d===0)return;e.preventDefault(),this._highlight=this._highlight<=0?d-1:this._highlight-1,this.requestUpdate();return}case"Enter":{if(e.preventDefault(),this._highlight>=0&&this._highlight<o.length){this._addSuggestion(o[this._highlight],!1);return}if(this._highlight===o.length&&c){this._addSuggestion({label:a},!0);return}if(c&&a.length>0){this._addSuggestion({label:a},!0);return}return}case"Escape":{e.preventDefault(),this.closeInput();return}case"Backspace":{if(this._query===""&&this._value.length>0){e.preventDefault();const g=this._value.length-1;this._focusedChip===g?(this.emit("os-tag-remove",{tag:this._value[g]}),this._focusedChip=-1):(this._focusedChip=g,this.requestUpdate())}return}default:this._focusedChip!==-1&&(this._focusedChip=-1)}}_onInputBlur(e){queueMicrotask(()=>{this.shadowRoot?.activeElement||this.closeInput()})}_addSuggestion(e,t){if(this._value.some(a=>a.label.toLowerCase()===e.label.toLowerCase())){this._query="",this._highlight=-1,this._suggestions=[],this.requestUpdate(),this._input?.focus();return}this.emit("os-tag-add",{tag:e,isNew:t}),this._query="",this._highlight=-1,this._suggestions=[],this._suggestionsLoading=!1,this.requestUpdate(),queueMicrotask(()=>{this._input?.focus()})}};Ae.props=["label","placeholder","add-label","creatable","removable","disabled","readonly","size","min-query","open"],Ae.styles=[gs],Ae.help={title:"Tag input",summary:"Multi-tag picker with autocomplete and free-form creation. Purely presentational — emits os-tag-suggest / os-tag-add / os-tag-remove and lets the consumer drive REST + optimistic UI.",status:"stable",props:[{name:"label",type:"string",description:"Accessible label for the inline input."},{name:"placeholder",type:"string",description:"Native placeholder for the inline input.",default:"Add a tag…"},{name:"add-label",type:"string",description:'Label of the trigger button. The button draws its own plus icon, so this is text only — passing "+ Add" puts two pluses on screen.',default:"Add"},{name:"creatable",type:"boolean attribute",description:"Allow Enter on a non-matching query to emit `os-tag-add` with `isNew: true`. Off by default — opt in for taxonomies the user is allowed to extend."},{name:"removable",type:"boolean attribute",description:"Show × on every chip and emit `os-tag-remove` on click. On by default; switch off for read-only views."},{name:"disabled",type:"boolean attribute",description:"Disables the entire control. Chips render but the trigger / input / dismiss buttons are inert."},{name:"readonly",type:"boolean attribute",description:'Hides the "+" trigger and chip × buttons. Same as setting `creatable=false` and `removable=false` together.'},{name:"size",type:"'default' | 'compact'",default:"default",description:"Density preset. Compact suits dense table cells."},{name:"min-query",type:"integer (string)",default:"0",description:"Minimum query length before `os-tag-suggest` fires. Set to 1 or 2 for taxonomies with thousands of terms."},{name:"open",type:"boolean attribute",description:"Two-way reflected: present while the inline input is showing. Setting it externally opens / closes the picker."}],events:[{name:"os-tag-suggest",description:"Fires when the user types in the input. Consumer fetches suggestions and assigns them back via `el.suggestions = […]`.",detail:"{ query: string }"},{name:"os-tag-add",description:"Fires when the user picks a suggestion or, with `creatable`, presses Enter on a free-form value. Consumer mutates `value`.",detail:"{ tag: OsTagItem; isNew: boolean }"},{name:"os-tag-remove",description:"Fires when × on a chip is activated. Consumer mutates `value`.",detail:"{ tag: OsTagItem }"},{name:"os-tag-open",description:"Fires when the inline input opens.",detail:"{}"},{name:"os-tag-close",description:"Fires when the inline input closes.",detail:"{}"}],cssProps:[{name:"--os-ui-tag-input-gap",description:"Gap between chips / between chips and trigger.",default:"4px"},{name:"--os-ui-tag-input-padding",description:"Padding around the chip row.",default:"2px"},{name:"--os-ui-tag-input-add-fg",description:'Foreground color of the "+ Add" trigger.'},{name:"--os-ui-tag-input-pop-bg",description:"Suggestions popover background."}],example:h`
			<os-tag-input
				label="Tags"
				placeholder="Add a tag…"
				creatable
			></os-tag-input>
		`};let Xe=Ae;K("os-tag-input",Xe);function ms(){return h`
		<svg
			viewBox="0 0 12 12"
			width="9"
			height="9"
			aria-hidden="true"
			focusable="false"
			fill="none"
			stroke="currentColor"
			stroke-width="2"
			stroke-linecap="round"
		>
			<path d="M6 2 L6 10 M2 6 L10 6" />
		</svg>
	`}const vs=C`
	${te}
	${Be}

	/*
	 * Host is block-level flex so the field fills its parent cell
	 * (grid row col=N, flex container, plain block container). An
	 * inline-flex default would leave the native <input> at its
	 * intrinsic width while the host spans the full cell, which
	 * looks wrong inside a os-row.
	 */
	:host {
		display: flex;
		flex-direction: column;
		gap: 4px;
		font-size: 13px;
		color: var( --os-ui-fg, #1d2327 );
		min-width: 0;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	.os-text-field__label {
		font-size: 12px;
		color: var( --os-ui-fg-muted, #646970 );
	}

	.os-text-field__row {
		position: relative;
		display: flex;
		align-items: center;
		width: 100%;
	}

	input {
		appearance: none;
		-webkit-appearance: none;
		display: block;
		width: 100%;
		min-width: 0;
		box-sizing: border-box;
		padding: 7px 10px;
		background: var( --os-window-bg, #fff );
		border: 1px solid var( --os-ui-border, #dcdcde );
		border-radius: 6px;
		font: inherit;
		font-size: 13px;
		color: var( --os-ui-fg, #1d2327 );
	}

	/* Suffix slot for units / currency badges — rendered when the
	 * component has a suffix attribute. Inline-end anchored so RTL
	 * locales flip automatically via logical properties. */
	.os-text-field__suffix {
		position: absolute;
		inset-inline-end: 10px;
		top: 50%;
		transform: translateY( -50% );
		pointer-events: none;
		font-size: 12px;
		color: var( --os-ui-fg-muted, #646970 );
	}

	/* Reveal (show/hide) toggle — only rendered on password-type fields
	 * that carry the reveal attribute. Sits at the inline-end of the
	 * row; the input grows extra padding when the button is present so
	 * typed text doesn't slide under it. */
	.os-text-field__row--has-reveal input {
		padding-inline-end: 36px;
	}

	.os-text-field__reveal {
		position: absolute;
		inset-inline-end: 0;
		top: 0;
		bottom: 0;
		width: 34px;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 0;
		border: none;
		background: transparent;
		color: var( --os-ui-fg-muted, #646970 );
		cursor: pointer;
		border-radius: 0 6px 6px 0;
		transition: color 0.12s ease;
	}
	.os-text-field__reveal:hover {
		color: var( --os-ui-accent, #2271b1 );
	}
	.os-text-field__reveal:focus-visible {
		outline: none;
		color: var( --os-ui-accent, #2271b1 );
		/* Inset, because the button sits flush inside the field's own
		   border — a ring outside it would trace the field, not the
		   button, and read as the wrong thing having focus. */
		box-shadow: inset 0 0 0 2px var( --os-ui-accent, #2271b1 );
		border-radius: 0 6px 6px 0;
	}
	.os-text-field__reveal:disabled {
		opacity: 0.45;
		cursor: not-allowed;
	}

	/* CSS-only password mask. We render type="password" declarations
	 * as actual type="text" inputs so Chrome / Edge / Firefox password
	 * managers never recognise them as credentials (they were ignoring
	 * autocomplete="new-password" and still offering to save / update
	 * the password for fields that are really API keys). The dots are
	 * applied via -webkit-text-security (the original Webkit extension,
	 * Chromium / Safari support it; Firefox 119+ ships the standard
	 * text-security). Older Firefox versions fall back to the input's
	 * letter-spacing trick — wide enough that the user sees the value
	 * exists but the characters bunch into an unreadable run.
	 *
	 * The reveal toggle simply removes this class. */
	.os-text-field__input--masked {
		-webkit-text-security: disc;
		text-security: disc;
	}
	@supports not ( ( -webkit-text-security: disc ) or ( text-security: disc ) ) {
		.os-text-field__input--masked {
			font-family: text-security-disc, "password", monospace;
			letter-spacing: 0.2em;
		}
	}

	input:disabled {
		opacity: 0.55;
		cursor: not-allowed;
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.03 ) );
	}

	/*
	 * Invalid outranks the shared focus ring — see the note on
	 * :where() in holoField. An invalid field focuses in red, because
	 * the ring is the only thing on screen saying so at that moment.
	 */
	input[ aria-invalid='true' ],
	input[ aria-invalid='true' ]:hover:not( :disabled ) {
		border-color: var( --os-ui-danger, #d63638 );
	}
	input[ aria-invalid='true' ]:focus,
	input[ aria-invalid='true' ]:focus-visible {
		border-color: var( --os-ui-danger, #d63638 );
		box-shadow: 0 0 0 1px var( --os-ui-danger, #d63638 ),
			0 0 0 4px rgba( 214, 54, 56, 0.18 );
	}

	/* Hide the native spinner on number inputs — the suffix slot and
	 * the keypad (when present) already handle increment / decrement.
	 * Callers that need spinners can unset this by restyling. */
	input[ type='number' ]::-webkit-inner-spin-button,
	input[ type='number' ]::-webkit-outer-spin-button {
		-webkit-appearance: none;
		margin: 0;
	}
	input[ type='number' ] {
		-moz-appearance: textfield;
	}
`,Ee=class Ee extends U{constructor(){super(...arguments),this._revealed=!1}connectedCallback(){super.connectedCallback(),gt(this)}render(){const e=this.label||"",t=this.value??"",o=this.placeholder||"",a=this.disabled!==null,c=this.readonly!==null,d=this.autocomplete,g=this.type||"text",b=g==="password";let f=d||"off";b&&(!d||f==="off")&&(f="new-password");const $=this.maxlength,x=this.minlength,N=this.pattern||"",k=this.name||"",v=this.suffix||"",P=this.invalid!==null,w=this.reveal!==null,L=g==="password",A=L&&!(w&&this._revealed);let _;L||w&&this._revealed?_="text":_=g;const B=w?"os-text-field__row os-text-field__row--has-reveal":"os-text-field__row",Le=A?"os-text-field__input os-text-field__input--masked":"os-text-field__input",oe=`${this.id||"os-unnamed"}__input`;return h`
			${e?h`<label
						class="os-text-field__label"
						for=${oe}
					>${e}</label>`:h``}
			<span class=${B}>
				<input
					id=${oe}
					class=${Le}
					type=${_}
					.value=${t}
					placeholder=${o}
					?disabled=${a}
					?readonly=${c}
					autocomplete=${f}
					maxlength=${$??""}
					minlength=${x??""}
					pattern=${N}
					name=${k}
					aria-invalid=${P?"true":"false"}
					aria-label=${e||""}
					@input=${G=>this._onInput(G)}
					@change=${G=>this._onChange(G)}
					@keydown=${G=>this._onKeyDown(G)}
				/>
				${v?h`<span class="os-text-field__suffix">${v}</span>`:h``}
				${w?this._renderRevealButton(a):h``}
			</span>
		`}_renderRevealButton(e){const t=this._revealed?"Hide":"Show";return h`
			<button
				type="button"
				class="os-text-field__reveal"
				aria-label=${t}
				aria-pressed=${this._revealed?"true":"false"}
				?disabled=${e}
				tabindex="0"
				@click=${()=>this._onToggleReveal()}
			>
				${this._revealed?ys():ws()}
			</button>
		`}_onToggleReveal(){this._revealed=!this._revealed,this.requestUpdate()}_onInput(e){const t=e.target;this.value=t.value,this.emit("os-input-change",{value:t.value})}_onChange(e){const t=e.target;this.emit("os-input-commit",{value:t.value})}_onKeyDown(e){if(e.key==="Enter"&&!e.shiftKey&&!e.altKey&&!e.metaKey){const t=e.target;this.emit("os-submit",{value:t.value})}}};Ee.props=["label","value","placeholder","disabled","readonly","autocomplete","type","maxlength","minlength","pattern","name","suffix","invalid","reveal"],Ee.styles=[vs],Ee.help={title:"Text field",summary:"Labelled text input primitive. Two-way reflects `value`, emits os-input-change per keystroke, os-input-commit on blur/change, and os-submit on Enter. Optional password reveal toggle.",status:"stable",props:[{name:"label",type:"string",description:"Visible label above the input."},{name:"value",type:"string",description:"Current input value; reflected two-way."},{name:"placeholder",type:"string",description:"Native placeholder string."},{name:"disabled",type:"boolean attribute",description:"Disables the native input."},{name:"readonly",type:"boolean attribute",description:"Marks the input readonly."},{name:"autocomplete",type:"string",default:"off",description:"Forwarded to the native input autocomplete attribute."},{name:"type",type:"string",default:"text",description:"Native input type (text, password, email, search, tel, url)."},{name:"maxlength",type:"integer (string)",description:"Native maxlength."},{name:"minlength",type:"integer (string)",description:"Native minlength."},{name:"pattern",type:"regex string",description:"Native validation pattern."},{name:"name",type:"string",description:"Forwarded to the native input for form submission."},{name:"suffix",type:"string",description:"Text rendered inside the right edge of the input row."},{name:"invalid",type:"boolean attribute",description:"Marks the field aria-invalid and applies the error style."},{name:"reveal",type:"boolean attribute",description:'On type="password" fields, adds an eye-icon toggle that flips the input between hidden and visible text.'}],events:[{name:"os-input-change",description:"Fires on every input keystroke.",detail:"{ value: string }"},{name:"os-input-commit",description:"Fires on the native change event (blur / Enter).",detail:"{ value: string }"},{name:"os-submit",description:"Fires when the user presses Enter (without Shift/Alt/Meta).",detail:"{ value: string }"}],cssProps:[{name:"--os-ui-fg",description:"Text colour."},{name:"--os-ui-fg-muted",description:"Label + suffix colour."},{name:"--os-ui-border",description:"Input outline."},{name:"--os-window-bg",description:"Input background."}],example:h`
			<os-stack gap="8">
				<os-text-field label="Note title" value="Untitled" placeholder="Name this note"></os-text-field>
				<os-text-field type="password" reveal label="API key"></os-text-field>
			</os-stack>
		`};let Je=Ee;K("os-text-field",Je);function ws(){return h`
		<svg
			viewBox="0 0 16 16"
			width="14"
			height="14"
			fill="none"
			stroke="currentColor"
			stroke-width="1.5"
			stroke-linecap="round"
			stroke-linejoin="round"
			aria-hidden="true"
			focusable="false"
		>
			<path d="M1 8C1 8 3.5 3 8 3s7 5 7 5-2.5 5-7 5S1 8 1 8z" />
			<circle cx="8" cy="8" r="2" />
		</svg>
	`}function ys(){return h`
		<svg
			viewBox="0 0 16 16"
			width="14"
			height="14"
			fill="none"
			stroke="currentColor"
			stroke-width="1.5"
			stroke-linecap="round"
			stroke-linejoin="round"
			aria-hidden="true"
			focusable="false"
		>
			<path d="M1 8C1 8 3.5 3 8 3s7 5 7 5-2.5 5-7 5S1 8 1 8z" />
			<circle cx="8" cy="8" r="2" />
			<line x1="2" y1="2" x2="14" y2="14" />
		</svg>
	`}const xs=C`
	${te}
	${Be}

	:host {
		display: flex;
		flex-direction: column;
		gap: 4px;
		font-size: 13px;
		color: var( --os-ui-fg, #1d2327 );
		min-width: 0;
	}
	:host( [ hidden ] ) {
		display: none;
	}

	.os-textarea__label {
		font-size: 12px;
		color: var( --os-ui-fg-muted, #646970 );
	}

	textarea {
		appearance: none;
		-webkit-appearance: none;
		display: block;
		width: 100%;
		min-width: 0;
		box-sizing: border-box;
		padding: 8px 10px;
		background: var( --os-window-bg, #fff );
		border: 1px solid var( --os-ui-border, #dcdcde );
		border-radius: 6px;
		font: inherit;
		font-size: 13px;
		line-height: 1.45;
		color: var( --os-ui-fg, #1d2327 );
		resize: vertical;
	}

	textarea:disabled {
		opacity: 0.55;
		cursor: not-allowed;
		background: var( --os-ui-hover, rgba( 0, 0, 0, 0.03 ) );
	}

	/* Outranks the shared focus ring — see the :where() note in holoField. */
	textarea[ aria-invalid='true' ],
	textarea[ aria-invalid='true' ]:hover:not( :disabled ) {
		border-color: var( --os-ui-danger, #d63638 );
	}
	textarea[ aria-invalid='true' ]:focus,
	textarea[ aria-invalid='true' ]:focus-visible {
		border-color: var( --os-ui-danger, #d63638 );
		box-shadow: 0 0 0 1px var( --os-ui-danger, #d63638 ),
			0 0 0 4px rgba( 214, 54, 56, 0.18 );
	}

	/* Auto-grow mode: hide native resize affordance — we manage rows. */
	:host( [ auto-grow ] ) textarea {
		resize: none;
		overflow: hidden;
	}
`,Se=class Se extends U{constructor(){super(...arguments),this._textareaEl=null}connectedCallback(){super.connectedCallback(),gt(this)}render(){const e=this._attr("label")||"",t=this._attr("value")??"",o=this._attr("placeholder")||"",a=this._boolAttr("disabled"),c=this._boolAttr("readonly"),d=this._attr("aria-label")||e,g=this._attr("name")||"",b=Number(this._attr("rows"))||3,f=this._attr("maxlength"),$=this._attr("minlength"),x=this._boolAttr("invalid"),k=`${this.id||"os-unnamed"}__field`;return h`
			${e?h`<label class="os-textarea__label" for=${k}>${e}</label>`:h``}
			<textarea
				id=${k}
				part="textarea"
				.value=${t}
				placeholder=${o}
				?disabled=${a}
				?readonly=${c}
				rows=${b}
				maxlength=${f??""}
				minlength=${$??""}
				name=${g}
				aria-invalid=${x?"true":"false"}
				aria-label=${d||""}
				@input=${v=>this._onInput(v)}
				@change=${v=>this._onChange(v)}
				@keydown=${v=>this._onKeyDown(v)}
			></textarea>
		`}_attr(e){return this.getAttribute(e)}_boolAttr(e){return this.getAttribute(e)!==null}_onInput(e){const t=e.target;this._textareaEl=t,this.setAttribute("value",t.value),this.emit("os-input-change",{value:t.value}),this._boolAttr("auto-grow")&&this._autosize(t)}_onChange(e){const t=e.target;this.emit("os-input-commit",{value:t.value})}_onKeyDown(e){if(this._boolAttr("submit-on-enter")&&e.key==="Enter"&&!e.shiftKey&&!e.altKey&&!e.metaKey&&!e.ctrlKey){e.preventDefault();const t=e.target;this.emit("os-submit",{value:t.value})}}_autosize(e){const t=Number(this._attr("max-rows"))||8,o=window.getComputedStyle(e),a=parseFloat(o.fontSize)||13,c=o.lineHeight,d=c==="normal"?a*1.45:parseFloat(c)||a*1.45,g=parseFloat(o.paddingTop)||0,b=parseFloat(o.paddingBottom)||0,f=d*t+g+b;e.style.height="auto";const $=Math.min(e.scrollHeight,f);e.style.height=`${$}px`}refreshAutosize(){this._textareaEl&&this._boolAttr("auto-grow")&&this._autosize(this._textareaEl)}focusInput(){(this.shadowRoot??this).querySelector("textarea")?.focus()}clear(){this.setAttribute("value","");const t=(this.shadowRoot??this).querySelector("textarea");t&&(t.value="",this._boolAttr("auto-grow")&&this._autosize(t))}};Se.props=["label","value","placeholder","disabled","readonly","ariaLabel","name","rows","maxlength","minlength","invalid","autoGrow","maxRows","submitOnEnter"],Se.styles=[xs],Se.help={title:"Textarea",summary:"Multi-line text input. Same event shape as os-text-field. Optional auto-grow up to max-rows; optional submit-on-enter (Enter sends, Shift+Enter newlines).",status:"stable",props:[{name:"label",type:"string",description:"Visible label above the textarea."},{name:"value",type:"string",description:"Current value; reflected two-way."},{name:"placeholder",type:"string",description:"Native placeholder."},{name:"disabled",type:"boolean attribute"},{name:"readonly",type:"boolean attribute"},{name:"aria-label",type:"string",description:"Accessible label when no visible label is rendered."},{name:"name",type:"string",description:"Forwarded to native textarea for form submission."},{name:"rows",type:"integer (string)",default:"3",description:"Initial visible row count."},{name:"maxlength",type:"integer (string)"},{name:"minlength",type:"integer (string)"},{name:"invalid",type:"boolean attribute",description:"Sets aria-invalid + error styling."},{name:"auto-grow",type:"boolean attribute",description:"Grows up to max-rows as the user types."},{name:"max-rows",type:"integer (string)",default:"8"},{name:"submit-on-enter",type:"boolean attribute",description:"Enter fires os-submit; Shift+Enter inserts a newline."}],events:[{name:"os-input-change",description:"Fires on every keystroke.",detail:"{ value: string }"},{name:"os-input-commit",description:"Fires on blur / native change.",detail:"{ value: string }"},{name:"os-submit",description:"Fires on Enter (without Shift) when submit-on-enter is set.",detail:"{ value: string }"}],example:h`
			<os-textarea label="Message" rows="3" auto-grow max-rows="8" submit-on-enter></os-textarea>
		`};let Qe=Se;K("os-textarea",Qe);const Ze="odd-app-odd-notes";class $t extends Error{constructor(t,o,a={}){super(t);lt(this,"status");lt(this,"data");this.name="NotesApiError",this.status=o,this.data=a}}function fe(){const s=window.wp?.os;if(!s)throw new Error("ODD Notes requires the OpenStation public API.");return s}function At(){const s=fe().getWindowConfig(Ze);if(!s?.restBase||!s.restNonce)throw new Error("Notes window config is missing.");return s}async function de(s,e={}){const t=At(),o=new URL(s.replace(/^\//,""),t.restBase),a=await fe().fetch(o,{method:e.method??"GET",credentials:"same-origin",signal:e.signal,headers:{Accept:"application/json","X-WP-Nonce":t.restNonce,...e.body!==void 0?{"Content-Type":"application/json"}:{}},body:e.body!==void 0?JSON.stringify(e.body):void 0},{windowId:Ze,source:"odd-app-odd-notes",silent:e.silent});if(!a.ok){let c=`${a.status} ${a.statusText}`,d={};try{const g=await a.json();c=g.message||c,d=g.data??{}}catch{}throw new $t(c,a.status,d)}return await a.json()}function ks(s){return de("notes",{signal:s})}function _s(s,e){return de("notes",{method:"POST",body:s,signal:e})}function Cs(s,e,t){return de(`notes/${s}`,{method:"PATCH",body:e,signal:t})}function $s(s,e){return de(`notes/${s}`,{method:"DELETE",signal:e})}async function As(s,e){return(await de(`notes/${s}/revisions`,{signal:e,silent:!0})).revisions}function Es(s,e,t){return de(`notes/${s}/revisions/${e}/restore`,{method:"POST",signal:t})}const Ss="odd-app-odd-notes/drafts/v1";function et(s){return`${Ss}/${s}`}function tt(){return{entries:{}}}function Ls(s){try{const e=window.localStorage.getItem(et(s));if(!e)return tt();const t=JSON.parse(e);return t&&typeof t.entries=="object"&&t.entries?{entries:t.entries}:tt()}catch{return tt()}}function Ts(s,e){try{if(Object.keys(e.entries).length===0){window.localStorage.removeItem(et(s));return}window.localStorage.setItem(et(s),JSON.stringify(e))}catch{}}function Ns(s){return{id:s.id,title:s.title,body:s.body,color:s.color,tags:[...s.tags],favorite:s.favorite,archived:s.archived,onDesktop:s.onDesktop,public:s.public,version:s.version,updatedAtMs:s.updatedAtMs,clientUpdatedAt:Date.now()}}function be(s,e){return{entries:{...s.entries,[String(e.id)]:Ns(e)}}}function me(s,e){const t={...s.entries};return delete t[String(e)],{entries:t}}function Ds(s,e,t){const o=s.entries[String(e)];if(!o)return s;const a={...s.entries};return delete a[String(e)],a[String(t)]={...o,id:t},{entries:a}}function zs(s,e){return{...s,...e,id:s.id,excerpt:e.body.trim().replace(/\s+/g," ").slice(0,150),wordCount:ve(e.body)}}function ve(s){const e=s.trim().match(/\S+/g);return e?e.length:0}function O(s){return typeof s=="string"&&s.startsWith("local-")}function Et(s,e){const t=s.filter(o=>o.id!==e.id);return[e,...t].sort((o,a)=>Date.parse(a.updatedAt)-Date.parse(o.updatedAt))}function St(s,e){return s.filter(t=>t.id!==e)}function Lt(s,e,t,o){const a=t.trim().toLocaleLowerCase(),c=o?.toLocaleLowerCase()??null;return s.filter(d=>{if((e==="archive"?!d.archived:d.archived)||e==="favorite"&&(!d.favorite||!d.canEdit)||e==="desktop"&&(!d.onDesktop||!d.canEdit)||e==="shared"&&d.canEdit||e==="all"&&!d.canEdit||c&&!d.tags.some(b=>b.toLocaleLowerCase()===c))return!1;if(!a)return!0;const g=[d.title,d.body,d.ownerName,...d.tags].join(`
`).toLocaleLowerCase();return a.split(/\s+/).every(b=>g.includes(b.replace(/^#/,"")))})}function Is(s){return{all:s.filter(e=>e.canEdit&&!e.archived).length,favorite:s.filter(e=>e.canEdit&&e.favorite&&!e.archived).length,desktop:s.filter(e=>e.canEdit&&e.onDesktop&&!e.archived).length,shared:s.filter(e=>!e.canEdit&&!e.archived).length,archive:s.filter(e=>e.canEdit&&e.archived).length}}function Tt(s){const e=new Map;for(const t of s)if(!(!t.canEdit||t.archived))for(const o of t.tags){const a=o.toLocaleLowerCase(),c=e.get(a);e.set(a,{label:c?.label??o,count:(c?.count??0)+1})}return[...e.values()].sort((t,o)=>t.label.localeCompare(o.label))}const Ps=650,Nt={butter:"#fff3a6",blush:"#ffd9e0",sky:"#cde9ff",mint:"#d4f5dc",lilac:"#e6ddff",peach:"#ffe1c4"};function p(s){return window.wp?.i18n?.__(s,"odd-outlandish-desktop-decorator")??s}function se(s,...e){return window.wp?.i18n?.sprintf(s,...e)??s.replace(/%[sd]/g,()=>String(e.shift()??""))}function m(s,e){const t=s.querySelector(e);if(!t)throw new Error(`Notes template is missing ${e}.`);return t}function Dt(s,e){s.value=e,s.setAttribute("value",e)}function st(s){return String(s.detail?.value??"")}function zt(s){return!!s.detail?.checked}function It(s,e,t){s.toggleAttribute("checked",e),s.toggleAttribute("disabled",t)}function Fs(s,e){const t=new Date().toISOString();return{id:`local-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,title:p("Untitled note"),body:"",excerpt:"",color:e,tags:[],favorite:!1,archived:!1,onDesktop:!1,public:!1,ownerId:s,ownerName:p("You"),ownerAvatar:"",canEdit:!0,createdAt:t,updatedAt:t,updatedAtMs:Date.now(),version:0,wordCount:0}}function Pt(s){return{title:s.title,body:s.body,color:s.color,tags:[...s.tags],favorite:s.favorite,archived:s.archived,onDesktop:s.onDesktop,public:s.public,...s.version>0?{version:s.version}:{},...s.updatedAtMs>0?{updatedAtMs:s.updatedAtMs}:{}}}function ot(s,e){return{...s,title:e.title,body:e.body,excerpt:e.body.trim().replace(/\s+/g," ").slice(0,150),color:e.color,tags:[...e.tags],favorite:e.favorite,archived:e.archived,onDesktop:e.onDesktop,public:e.public,wordCount:ve(e.body)}}function Ft(s,e){const t=new Date(s.clientUpdatedAt).toISOString();return{id:s.id,title:s.title,body:s.body,excerpt:s.body.trim().replace(/\s+/g," ").slice(0,150),color:s.color,tags:[...s.tags],favorite:s.favorite,archived:s.archived,onDesktop:s.onDesktop,public:s.public,ownerId:e,ownerName:p("You"),ownerAvatar:"",canEdit:!0,createdAt:t,updatedAt:t,updatedAtMs:s.clientUpdatedAt,version:s.version??0,wordCount:ve(s.body)}}function it(s){const e=Date.parse(s);if(Number.isNaN(e))return"";const t=e-Date.now(),o=new Intl.RelativeTimeFormat(void 0,{numeric:"auto"}),a=Math.abs(t);return a<6e4?p("just now"):a<36e5?o.format(Math.round(t/6e4),"minute"):a<864e5?o.format(Math.round(t/36e5),"hour"):a<2592e6?o.format(Math.round(t/864e5),"day"):new Intl.DateTimeFormat(void 0,{month:"short",day:"numeric",year:new Date(s).getFullYear()===new Date().getFullYear()?void 0:"numeric"}).format(new Date(s))}async function Rs(s,e){const t=At(),o=m(s,"[data-notes-app]"),a=m(o,"[data-notes-list]"),c=m(o,"[data-notes-loading]"),d=m(o,"[data-notes-empty]"),g=m(o,"[data-notes-editor]"),b=m(o,".os-notes-app__writing"),f=m(o,"[data-notes-title]"),$=m(o,"[data-notes-body]"),x=m(o,"[data-notes-tags]"),N=m(o,"[data-notes-desktop]"),k=m(o,"[data-notes-public]"),v=m(o,"[data-notes-save-status]"),P=m(o,"[data-notes-stats]"),w=m(o,"[data-notes-owner]"),L=m(o,"[data-notes-toast]"),A=m(o,"[data-notes-history-panel]"),_=m(o,"[data-notes-history-list]"),B=m(o,"[data-notes-list-title]"),Le=m(o,"[data-notes-list-summary]"),Fe=m(o,"[data-notes-tag-list]"),oe=m(o,"[data-notes-favorite]"),G=m(o,"[data-notes-archive]"),Rt=m(o,"[data-notes-history]"),Mt=m(o,"[data-notes-delete]");let E=[],S=null,ce="all",V=null,Te="",y=Ls(t.userId),ue=!1,Ne=null,De=null;const Re=new Map,Me=new Set,D=new Set,ie=new Map;await Promise.all([customElements.whenDefined("os-tag-input"),customElements.whenDefined("os-swatch")]);function qe(i){return i===null?void 0:E.find(n=>n.id===i)}function M(){return qe(S)}function Q(i,n=""){v.setAttribute("phase",i),n?v.setAttribute("error",n):v.removeAttribute("error")}function ae(i,n=!1){L.textContent=i,L.hidden=!1,L.toggleAttribute("data-error",n),Ne!==null&&window.clearTimeout(Ne),Ne=window.setTimeout(()=>{L.hidden=!0},4200)}function ne(){Ts(t.userId,y)}function at(){const i=window.navigator.onLine;m(o,"[data-notes-online-dot]").toggleAttribute("data-offline",!i),m(o,"[data-notes-online-label]").textContent=p(i?"WordPress connected":"Offline — drafts safe here")}function Y(){const i=Is(E);for(const[n,r]of Object.entries(i)){const l=o.querySelector(`[data-notes-count="${n}"]`);l&&(l.textContent=String(r))}o.querySelectorAll("[data-notes-filter]").forEach(n=>{n.classList.toggle("is-active",n.dataset.notesFilter===ce&&V===null)}),Fe.replaceChildren();for(const n of Tt(E)){const r=document.createElement("button");r.type="button",r.dataset.notesTag=n.label,r.classList.toggle("is-active",V?.toLocaleLowerCase()===n.label.toLocaleLowerCase());const l=document.createElement("span");l.textContent="#";const u=document.createElement("span");u.textContent=n.label;const T=document.createElement("small");T.textContent=String(n.count),r.append(l,u,T),Fe.appendChild(r)}}function W(){const i=Lt(E,ce,Te,V),n={all:p("All notes"),favorite:p("Favorites"),desktop:p("On desktop"),shared:p("Shared notes"),archive:p("Archive")};if(B.textContent=V?`#${V}`:n[ce],Le.textContent=se(p(Te?"%d matching notes":"%d notes"),i.length),a.replaceChildren(),i.length===0){const r=document.createElement("div");r.className="os-notes-app__no-results";const l=document.createElement("span");l.className="dashicons dashicons-search",l.setAttribute("aria-hidden","true");const u=document.createElement("p");u.textContent=p(Te||V?"No notes match this view.":"Nothing here yet."),r.append(l,u),a.appendChild(r);return}for(const r of i){const l=document.createElement("button");l.type="button",l.className="os-notes-app__note-card",l.dataset.noteId=String(r.id),l.dataset.noteColor=r.color,l.classList.toggle("is-selected",r.id===S),l.classList.toggle("is-local",O(r.id)),l.setAttribute("aria-pressed",r.id===S?"true":"false");const u=document.createElement("span");u.className="os-notes-app__note-marker",u.setAttribute("aria-hidden","true");const T=document.createElement("span");T.className="os-notes-app__note-card-body";const I=document.createElement("span");I.className="os-notes-app__note-card-top";const H=document.createElement("strong");if(H.textContent=r.title||p("Untitled note"),I.appendChild(H),r.favorite){const q=document.createElement("span");q.className="dashicons dashicons-star-filled",q.setAttribute("aria-label",p("Favorite")),I.appendChild(q)}const Z=document.createElement("span");Z.className="os-notes-app__note-excerpt",Z.textContent=r.excerpt||p("A new note, waiting for words.");const ge=document.createElement("span");ge.className="os-notes-app__note-meta";const Wt=document.createElement("span");if(Wt.textContent=it(r.updatedAt),ge.appendChild(Wt),r.canEdit){if(r.onDesktop){const q=document.createElement("span");q.className="os-notes-app__desktop-pill",q.textContent=r.public?p("Public"):p("Desktop"),ge.appendChild(q)}}else{const q=document.createElement("span");q.className="os-notes-app__shared-pill",q.textContent=r.ownerName,ge.appendChild(q)}for(const q of r.tags.slice(0,2)){const rt=document.createElement("span");rt.className="os-notes-app__tag-pill",rt.textContent=`#${q}`,ge.appendChild(rt)}T.append(I,Z,ge),l.append(u,T),a.appendChild(l)}}function he(){const i=M();if(!i){d.hidden=!1,g.hidden=!0,A.hidden=!0;return}d.hidden=!0,g.hidden=!1,g.toggleAttribute("data-readonly",!i.canEdit),b.dataset.notesPaperColor=i.color,Dt(f,i.title),Dt($,i.body),f.toggleAttribute("readonly",!i.canEdit),$.toggleAttribute("readonly",!i.canEdit),x.value=i.tags.map(r=>({id:r,label:r})),x.toggleAttribute("readonly",!i.canEdit),It(N,i.onDesktop,!i.canEdit),It(k,i.public,!i.canEdit||!i.onDesktop),oe.toggleAttribute("disabled",!i.canEdit),G.toggleAttribute("disabled",!i.canEdit),Rt.toggleAttribute("disabled",!i.canEdit||O(i.id)),Mt.toggleAttribute("disabled",!i.canEdit),oe.setAttribute("title",i.favorite?p("Remove from favorites"):p("Add to favorites"));const n=oe.querySelector(".dashicons");if(n&&(n.className=i.favorite?"dashicons dashicons-star-filled":"dashicons dashicons-star-empty"),G.setAttribute("title",i.archived?p("Return to notes"):p("Archive")),w.replaceChildren(),i.canEdit){const r=document.createElement("span");r.className="dashicons dashicons-cloud-saved",r.setAttribute("aria-hidden","true");const l=document.createElement("span");l.textContent=O(i.id)?p("Local draft"):se(p("Edited %s"),it(i.updatedAt)),w.append(r,l)}else{if(i.ownerAvatar){const l=document.createElement("img");l.src=i.ownerAvatar,l.alt="",w.appendChild(l)}const r=document.createElement("span");r.textContent=se(p("Shared by %s"),i.ownerName),w.appendChild(r)}P.textContent=se(p("%d words · %d characters"),i.wordCount,i.body.length),o.querySelectorAll("[data-notes-colors] os-swatch").forEach(r=>{r.toggleAttribute("selected",r.getAttribute("value")===i.color)}),!D.has(String(i.id))&&!Me.has(String(i.id))&&Q(O(i.id)?"pending":"idle")}function Ms(){De!==null&&window.clearTimeout(De),De=window.setTimeout(()=>{Y(),W()},120)}function re(i,n=!0){E=Et(E,i),n&&(Y(),W(),he())}function X(i,n=!1){const r=M();if(!r?.canEdit)return;const l={...r,...i,updatedAt:new Date().toISOString(),updatedAtMs:Date.now()};l.excerpt=l.body.trim().replace(/\s+/g," ").slice(0,150),l.wordCount=ve(l.body),re(l,n);const u=String(l.id);ie.set(u,(ie.get(u)??0)+1),D.add(u),y=be(y,l),ne(),Q("pending"),P.textContent=se(p("%d words · %d characters"),l.wordCount,l.body.length),Ms(),pe(l.id)}function pe(i,n=Ps){const r=String(i),l=Re.get(r);l!==void 0&&window.clearTimeout(l),Re.set(r,window.setTimeout(()=>{Re.delete(r),qt(i)},n))}async function qs(i,n,r){const l=await fe().confirm({title:p("This note changed elsewhere"),message:p("Keep the words from this window? Choose “Use WordPress copy” to discard this local draft."),confirmLabel:p("Keep this draft"),cancelLabel:p("Use WordPress copy")});if(!ue){if(l){const u=ot(r,n);re(u),D.add(String(i)),y=be(y,u),ne(),pe(i,0);return}re(r),D.delete(String(i)),y=me(y,i),ne(),Q("saved")}}async function qt(i){const n=String(i);if(Me.has(n)||!D.has(n))return;const r=qe(i);if(!r?.canEdit)return;if(!window.navigator.onLine){S===i&&Q("pending");return}Me.add(n),D.delete(n);const l=ie.get(n)??0;S===i&&Q("saving");try{const u=O(i)?await _s(Pt(r),e.signal):await Cs(i,Pt(r),e.signal);if(ue)return;const T=qe(i),I=(ie.get(n)??0)!==l;if(O(i)){E=St(E,i),S=S===i?u.id:S,y=Ds(y,i,u.id),D.delete(n),ie.set(String(u.id),ie.get(n)??0),ie.delete(n);const H=I&&T?ot(u,T):u;re(H,!1),I?(D.add(String(u.id)),y=be(y,H)):y=me(y,u.id)}else if(I&&T){const H=ot(u,T);re(H,!1),D.add(n),y=be(y,H)}else re(u,!1),y=me(y,i);ne(),Y(),W(),he(),!I&&S===u.id&&Q("saved")}catch(u){if(ue||u instanceof DOMException&&u.name==="AbortError")return;D.add(n),u instanceof $t&&u.status===409&&u.data.current&&!O(i)?await qs(i,r,u.data.current):(S===i&&Q("failed",u instanceof Error?u.message:p("Save failed")),ae(u instanceof Error?u.message:p("The note could not be saved."),!0))}finally{Me.delete(n);const u=O(i)&&S!==i?S:i;u!==null&&D.has(String(u))&&pe(u,160)}}function Us(i){S=i,A.hidden=!0,W(),he(),window.setTimeout(()=>{f.focusInput?.()},0)}function nt(i){const n=Fs(t.userId,i?.color??t.colors[0]??"butter");i&&(n.title=se(p("Copy of %s"),i.title),n.body=i.body,n.tags=[...i.tags],n.favorite=!1,n.wordCount=ve(n.body),n.excerpt=i.excerpt),E=Et(E,n),S=n.id,D.add(String(n.id)),y=be(y,n),ne(),Y(),W(),he(),pe(n.id,120),window.setTimeout(()=>{f.focusInput?.()},0)}async function Ut(i=!1){e.markLoading(),c.hidden=!1;try{const n=await ks(e.signal);if(ue)return;const r=new Map(n.notes.map(u=>[String(u.id),u])),l=n.notes.map(u=>{const T=y.entries[String(u.id)];return T?(D.add(String(u.id)),zs(u,T)):u});for(const u of Object.values(y.entries))O(u.id)&&!r.has(String(u.id))&&(l.push(Ft(u,t.userId)),D.add(String(u.id)));E=l,S!==null&&!qe(S)&&(S=null),S===null&&(S=E.find(u=>u.canEdit&&!u.archived)?.id??null),Y(),W(),he();for(const u of D){const T=E.find(I=>String(I.id)===u);T&&pe(T.id,220)}i&&ae(p("Notes refreshed from WordPress."))}catch(n){if(n instanceof DOMException&&n.name==="AbortError")return;if(ae(n instanceof Error?n.message:p("Could not load notes."),!0),E.length===0){for(const r of Object.values(y.entries))E.push(Ft(r,t.userId));Y(),W()}}finally{c.hidden=!0,e.markReady()}}function Os(){const i=m(o,"[data-notes-colors]");i.replaceChildren();for(const n of t.colors){const r=document.createElement("os-swatch");r.setAttribute("value",n),r.setAttribute("label",n),r.setAttribute("size","sm"),r.setAttribute("preview",Nt[n]??Nt.butter),i.appendChild(r)}}async function Bs(){const i=M();if(!i?.canEdit||O(i.id))return;A.hidden=!1,_.replaceChildren();const n=document.createElement("div");n.className="os-notes-app__history-loading",n.textContent=p("Reading WordPress revisions…"),_.appendChild(n);try{const r=await As(i.id,e.signal);if(ue||S!==i.id)return;Ws(i,r)}catch(r){_.textContent=r instanceof Error?r.message:p("Could not load revision history.")}}function Ws(i,n){if(_.replaceChildren(),n.length===0){const r=document.createElement("p");r.className="os-notes-app__history-empty",r.textContent=p("No earlier versions yet. Keep writing—WordPress will remember them."),_.appendChild(r);return}for(const r of n){const l=document.createElement("article");l.className="os-notes-app__revision";const u=document.createElement("div"),T=document.createElement("strong");T.textContent=r.title||p("Untitled note");const I=document.createElement("time");I.dateTime=r.createdAt,I.textContent=it(r.createdAt),u.append(T,I);const H=document.createElement("p");H.textContent=r.body.trim().replace(/\s+/g," ").slice(0,220)||p("Empty note");const Z=document.createElement("os-button");Z.setAttribute("variant","secondary"),Z.dataset.revisionId=String(r.id),Z.textContent=p("Restore this version"),l.append(u,H,Z),_.appendChild(l)}_.onclick=r=>{const l=r.target.closest("[data-revision-id]");l&&Hs(i.id,Number(l.dataset.revisionId))}}async function Hs(i,n){if(await fe().confirm({title:p("Restore this version?"),message:p("Your current words will remain available as another WordPress revision."),confirmLabel:p("Restore version")}))try{const l=await Es(i,n,e.signal);re(l),y=me(y,i),ne(),A.hidden=!0,ae(p("Earlier version restored."))}catch(l){ae(l instanceof Error?l.message:p("Restore failed."),!0)}}async function Ks(){const i=M();if(!(!i?.canEdit||!await fe().confirm({title:p("Move note to Trash?"),message:p("You can restore it later from OpenStation Trash."),confirmLabel:p("Move to Trash"),cancelLabel:p("Keep note"),danger:!0})))try{O(i.id)||await $s(i.id,e.signal),E=St(E,i.id),y=me(y,i.id),ne(),S=Lt(E,ce,Te,V)[0]?.id??null,Y(),W(),he(),ae(p("Note moved to Trash."))}catch(r){ae(r instanceof Error?r.message:p("Delete failed."),!0)}}function Vs(){o.querySelectorAll("[data-notes-new], [data-notes-empty-new]").forEach(i=>i.addEventListener("click",()=>nt())),m(o,"[data-notes-refresh]").addEventListener("click",()=>void Ut(!0)),m(o,"[data-notes-search]").addEventListener("os-input-change",i=>{Te=st(i),W()}),o.querySelectorAll("[data-notes-filter]").forEach(i=>{i.addEventListener("click",()=>{ce=i.dataset.notesFilter??"all",V=null,Y(),W()})}),Fe.addEventListener("click",i=>{const n=i.target.closest("[data-notes-tag]");n&&(V=V===n.dataset.notesTag?null:n.dataset.notesTag??null,ce="all",Y(),W())}),a.addEventListener("click",i=>{const n=i.target.closest("[data-note-id]"),r=E.find(l=>String(l.id)===n?.dataset.noteId);r&&Us(r.id)}),f.addEventListener("os-input-change",i=>{X({title:st(i)||p("Untitled note")})}),$.addEventListener("os-input-change",i=>{X({body:st(i)})}),x.addEventListener("os-tag-suggest",i=>{const n=String(i.detail?.query??"").toLocaleLowerCase(),r=new Set(M()?.tags.map(l=>l.toLocaleLowerCase()));x.suggestions=Tt(E).filter(l=>!r.has(l.label.toLocaleLowerCase())&&l.label.toLocaleLowerCase().includes(n)).slice(0,8).map(l=>({id:l.label,label:l.label}))}),x.addEventListener("os-tag-add",i=>{const n=String(i.detail?.tag?.label??"").trim(),r=M();!r||!n||r.tags.some(l=>l.toLocaleLowerCase()===n.toLocaleLowerCase())||X({tags:[...r.tags,n].slice(0,12)},!0)}),x.addEventListener("os-tag-remove",i=>{const n=String(i.detail?.tag?.label??""),r=M();r&&X({tags:r.tags.filter(l=>l!==n)},!0)}),m(o,"[data-notes-colors]").addEventListener("os-pick",i=>{const n=String(i.detail?.value??"butter");X({color:n},!0)}),N.addEventListener("os-checkbox-change",i=>{const n=zt(i);X({onDesktop:n,public:n?M()?.public??!1:!1},!0)}),k.addEventListener("os-checkbox-change",i=>{const n=zt(i);X({public:n,onDesktop:n||M()?.onDesktop===!0},!0)}),oe.addEventListener("click",()=>{const i=M();i?.canEdit&&X({favorite:!i.favorite},!0)}),m(o,"[data-notes-duplicate]").addEventListener("click",()=>{const i=M();i&&nt(i)}),G.addEventListener("click",()=>{const i=M();i?.canEdit&&X({archived:!i.archived,onDesktop:i.archived?i.onDesktop:!1,public:i.archived?i.public:!1},!0)}),Mt.addEventListener("click",()=>void Ks()),Rt.addEventListener("click",()=>void Bs()),m(o,"[data-notes-history-close]").addEventListener("click",()=>{A.hidden=!0}),o.addEventListener("keydown",i=>{const n=i;if((n.metaKey||n.ctrlKey)&&n.key.toLocaleLowerCase()==="s"){n.preventDefault();const r=M();r&&qt(r.id)}(n.metaKey||n.ctrlKey)&&n.key.toLocaleLowerCase()==="n"&&(n.preventDefault(),nt()),(n.metaKey||n.ctrlKey)&&n.shiftKey&&n.key.toLocaleLowerCase()==="f"&&(n.preventDefault(),m(o,"[data-notes-search]").focusInput?.()),n.stopPropagation()})}function Ot(){at();for(const i of D){const n=E.find(r=>String(r.id)===i);n&&pe(n.id,0)}}function Bt(){at(),Q("pending")}return Os(),Vs(),at(),window.addEventListener("online",Ot),window.addEventListener("offline",Bt),await Ut(),()=>{ue=!0;for(const i of Re.values())window.clearTimeout(i);Ne!==null&&window.clearTimeout(Ne),De!==null&&window.clearTimeout(De),window.removeEventListener("online",Ot),window.removeEventListener("offline",Bt)}}window.openStationNativeWindows=window.openStationNativeWindows??{},window.openStationNativeWindows[Ze]=Rs})();
