# create-odd-bundle

Scaffold a local, Apps-only ODD bundle:

```sh
npx create-odd-bundle my-app
```

The command creates `my-app/` with a strict app manifest, local HTML entry, and
monochrome SVG icon. The scaffold accepts only the current `app` type.
